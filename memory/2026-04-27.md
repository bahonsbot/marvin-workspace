- Home-improvement pass:
  - What changed: clarified `docs/runbooks/workspace-home-improvement-pass.md` with a quick selection rubric and a copyable daily memory note template.
  - Why: reduce ambiguity during autonomy-triggered cleanup passes and make it easier to avoid drifting into normal backlog work.
  - Expected benefit: faster, safer home-improvement execution with more consistent memory logging.
  - Rollback: revert the added rubric and template section in `docs/runbooks/workspace-home-improvement-pass.md`.

## Morning Meeting Decision [11:39]
- Report: self-improvement
- Finding: SI-2026-0427-001 / stale Nexos caveat in `TOOLS.md`
- Decision: Approved
- Problem: `TOOLS.md` still implied Nexos may be expected background runtime config even though Nexos was fully removed and verified absent on Apr 25.
- Risk: low, but stale operational notes could mislead future troubleshooting.
- Action taken: updated the Nexos provider note to say Nexos config was removed and verified absent from `openclaw.json`, `models.json`, and `auth-profiles.json`; new Nexos entries should now be treated as drift.
- Verification: `grep` confirmed the old “Treat Nexos as background runtime config” wording is absent and the updated note is present.
- Rollback: restore the prior single-line Nexos provider note from git if needed.

## Morning Meeting Decision [12:01]
- Report: self-improvement
- Finding: SI-2026-0427-002 / daily-memory startup scope in `AGENTS.md`
- Decision: Adjusted
- Problem: the report proposed reading today's plus the previous 3 daily notes by default, but that would add too much unnecessary context.
- Risk: low, but over-reading stale daily notes can waste context and reintroduce old noise.
- Action taken: updated `AGENTS.md` startup item 3 to keep today's daily memory primary; read yesterday's note in the morning/afternoon when useful for continuity; in the evening, today's note is usually enough; older notes are only pulled when referenced, needed for continuity, or risk is high.
- Verification: `grep` confirmed the updated startup memory-scope wording in `AGENTS.md`.
- Rollback: restore the prior item 3 wording from git if needed.

## Morning Meeting Decision [12:04]
- Report: self-improvement
- Finding: SI-2026-0427-003 / hardcoded Mission Control Next.js version in `TOOLS.md`
- Decision: Approved
- Problem: `TOOLS.md` hardcoded Mission Control's Next.js version, which can drift after package updates.
- Risk: low, mostly future troubleshooting confusion if the runtime package changes and the ops note is not updated.
- Action taken: changed the Mission Control app stack note to point to `projects/mission-control/package.json` as source of truth, while recording that `next 16.2.0` was verified current on Apr 27, 2026.
- Verification: `node -p "require('./projects/mission-control/package.json').dependencies.next"` returned `16.2.0`; `grep` confirmed the updated `TOOLS.md` wording.
- Rollback: restore the previous hardcoded app-stack line from git if needed.

## Morning Meeting Decision [12:07]
- Report: self-improvement
- Finding: SI-2026-0427-004 / `TOOLS.md` Search Scope Clarifier wording
- Decision: Adjusted
- Problem: the report recommended removing the subsection, but the content is actually operational tool-routing guidance and belongs in `TOOLS.md`; only the wording was self-contradictory.
- Risk: info-level; unclear routing guidance could blur QMD memory recall versus live web research.
- Action taken: kept the Search Scope Clarifier in `TOOLS.md` and rewrote it to distinguish QMD, `web_search`/Brave, and Mission Control web research without saying the section belongs elsewhere.
- Verification: `grep` confirmed the old contradictory wording is absent and the updated clarifier is present.
- Rollback: restore the prior subsection from git if needed.

## Morning Meeting Decision [12:08]
- Report: self-improvement
- Finding: SI-2026-0427-005 / `SOUL.md` cross-reference to `IDENTITY.md`
- Decision: Approved
- Problem: `IDENTITY.md` points to `SOUL.md` as active identity guidance, but `SOUL.md` did not briefly state that relationship.
- Risk: info-level; future readers could miss that `IDENTITY.md` is legacy/secondary.
- Action taken: added one short Continuity note in `SOUL.md`: `IDENTITY.md` is a secondary legacy reference; active identity guidance lives here.
- Verification: `grep` confirmed the new short note under `SOUL.md` Continuity.
- Rollback: remove the added one-line note if needed.

## Morning Meeting Cleanup [12:12]
- Request: mirror useful content from `projects/_ops/mission-control-dashboard-learnings-2026-04-22.md` into structured learnings, then remove the cluttering ops note.
- Action taken: added structured `.learnings/errors.md` entries for the two reusable lessons: (1) do not store ad-hoc ops learnings with sensitive access details in project docs, and (2) keep Mission Control Dashboard distinct from the native OpenClaw Control UI/Gateway when reasoning about routing/exposure.
- Removed: `projects/_ops/mission-control-dashboard-learnings-2026-04-22.md`.
- Verification: file deletion and new learning entries confirmed by git status/grep.
- Notes: sensitive access details from the old note were intentionally not copied into learnings.

## Morning Meeting Closeout [12:22]
- Overnight review summary: nightly security review was clean; Platform Health Council reported all 9 areas healthy; daily-task-generator added 0 new tasks.
- Home-improvement result: `docs/runbooks/workspace-home-improvement-pass.md` was clarified with a selection rubric and daily-memory note template.
- Dreaming result: Memory Dreaming Promotion ran successfully; Deep Sleep repaired recall artifacts, ranked 10 candidates, and promoted 0 into `MEMORY.md`; no malformed durable-memory promotion was observed.
- Morning Meeting actions completed:
  - updated `TOOLS.md` stale Nexos note to treat any reappearance as drift;
  - adjusted `AGENTS.md` daily-memory startup scope to prefer today, add yesterday only when useful in morning/afternoon, and pull older notes only when referenced/needed/high-risk;
  - updated `TOOLS.md` Mission Control app-stack note to point to `projects/mission-control/package.json` as version source of truth;
  - adjusted `TOOLS.md` Search Scope Clarifier as operational tool-routing guidance;
  - added a short `SOUL.md` note that `IDENTITY.md` is secondary legacy reference;
  - mirrored useful content from the stray Mission Control dashboard learnings file into `.learnings/errors.md` and removed the stray `_ops` file.
- Commit plan: focused commit only for morning-meeting docs/learnings cleanup; leave unrelated workspace drift untouched.

## Mission Control Chat audit + focused promotion [15:55-16:08 GMT+8]
- Philippe approved the focused Chat batch for Dashboard promotion after Lab looked good on desktop and mobile; task-toast intrusion was explicitly accepted as a later follow-up.
- Promoted only the focused Chat files from Lab to Dashboard:
  - `app/general/chat/page.tsx`
  - `app/globals.css`
  - `components/chat/MissionControlChatSurface.tsx`
  - `components/chat/chat-composer.tsx`
  - `components/chat/chat-transcript-adjacent.tsx`
  - `components/shell/AppShellClient.tsx`
  - `components/shell/shell.module.css`
- Changes promoted:
  - Chat first paint no longer blocks on full transcript/history hydration; transcript hydrates after page load.
  - Chat status is split into connection/runtime state and run state, reducing contradictory `Ready` vs active transcript signals.
  - Top Chat controls are less overloaded; context/connection details are compressed behind clearer controls, especially on mobile.
  - Internal labels such as `agent:main:main` are softened to user-facing labels like Marvin/Selected chat where appropriate.
  - Chat mobile layout now uses an explicit `chat-shell-grid` class so mobile rules reliably hide the sidebar and give Chat the full viewport.
  - Composer mobile layout wraps controls instead of clipping the send button.
  - Task toast summaries suppress obvious raw/internal command payloads; broader toast placement is deferred.
- Validation:
  - Dashboard `npm run lint` passed.
  - Dashboard `npm run build` passed; expected `.next` artifacts verified present: `build-manifest.json`, `prerender-manifest.json`, `routes-manifest.json`, `server/pages-manifest.json`, `server/app-paths-manifest.json`.
  - Dashboard restart completed after pausing supervisor with `.preview-runtime/maintenance.lock` to avoid port collision.
  - `mission-control-service-health.sh` passed.
  - `mission-control-lane-smoke.sh` passed for dashboard/live, lab, and preview lanes.
  - Local Dashboard timing after promotion: `/general/chat` around 0.7s response, compared with prior ~12-16s Lab baseline before the first-paint fix.
  - Rendered Dashboard desktop and mobile screenshots verified no obvious Chat blockers: `artifacts/mission-control-audit/chat-dashboard-promoted-desktop-final.png` and `artifacts/mission-control-audit/chat-dashboard-promoted-mobile-final.png`.
- Operational note:
  - During Lab and Dashboard restarts, the service supervisor auto-restarted bundles while manual restart was in progress, causing port collisions (`EADDRINUSE`) and temporary proxy/Next disconnects. The safe recovery pattern was to create the lane maintenance lock, stop the service, clear only lane-owned ports, start, validate, then remove the lock.
- Deferred:
  - Task toast intrusion on Chat, especially mobile, remains accepted for later page/task-notification work.
  - The Tasks-page manual autonomous execution errors remain parked for the Tasks page audit.

## OpenClaw loop-pattern audit [21:36 GMT+8]
- What changed: wrote `projects/_ops/openclaw-loop-pattern-audit-2026-04-27.md` documenting a concrete no-progress retry cluster in the current autonomous task session.
- Evidence: after a failed local `docs/tools/loop-detection.md` read, the session repeated several near-equivalent `exec`-based log probes that returned only trivial or empty output before finally pivoting to direct session inspection.
- Root-cause hypothesis: the run lacked a strong pivot gate after a missing reference file and consecutive low-information diagnostic results.
- Recommendation: add a bounded no-progress pivot guard plus a prompt rule to switch to the prepared research packet, current-session transcript, or narrower target after two empty/low-information probes.
- Rollback: remove `projects/_ops/openclaw-loop-pattern-audit-2026-04-27.md` if this audit is superseded.

## Mission Control Agents promotion to Dashboard [22:15-22:24 GMT+8]
- Philippe approved promoting the improved Lab Agents page to Dashboard after mobile QA.
- Promoted only the focused Agents changes, not a wholesale Lab copy:
  - `projects/mission-control/components/shell/AppShellClient.tsx`
  - `projects/mission-control/components/agents/AgentSection.tsx`
  - `projects/mission-control/components/agents/AgentSeatCard.tsx`
  - `projects/mission-control/lib/adapters/agents.ts`
  - `projects/mission-control/app/globals.css`
- Behavior promoted:
  - `/general/agents` gets `agents-shell-grid` / `agents-shell-main` classes.
  - On mobile, the desktop sidebar is hidden for Agents.
  - Teams/Specialists rails are constrained inside a rail frame and now actually scroll horizontally.
  - Mobile rails show a “Swipe sideways to see all agents” hint.
  - Visible card actions are limited to `Open chat` and `Open workspace`; memory/context links are no longer shown in the card action cluster.
- Validation:
  - Dashboard `npm run lint` passed.
  - Dashboard `npm run build` passed; known existing warnings only (`middleware` convention, dynamic autonomous runner trace, NFT trace via files route).
  - Required `.next` artifacts verified present.
  - Dashboard restarted with `.preview-runtime/maintenance.lock` pattern.
  - Dashboard service health passed.
  - Route checks passed: `/general/agents`, `/general/tasks`, `/general/home`, `/general/chat` all HTTP 200.
  - `scripts/mission-control-lane-smoke.sh` passed for dashboard/live, lab, and preview.
  - Mobile CDP at 390px confirmed no page-level horizontal overflow, sidebar hidden, Teams rail scrolls (`scrollLeft 0 -> 180`), Specialists rail scrolls (`scrollLeft 0 -> 180`), and visible actions are only chat/workspace.
  - Screenshot saved: `artifacts/mission-control-audit/agents-dashboard-promoted-mobile.png`.
- Notes:
  - Existing unrelated Dashboard Chat diffs remain untouched and outside this promotion batch.


## Mission Control mobile audit: Skills, Crons, Memory, Files in Lab [22:28-22:49 GMT+8]
- Philippe asked to continue the Mission Control audit with mobile-only optimization for Skills, Crons, Memory, and Files. Desktop versions were explicitly considered acceptable and should not receive real layout changes.
- Scope accepted: Lab-first only; hide desktop left sidebar on mobile; improve usable width, especially Memory and Files preview/editor areas; make Skills more mobile-friendly than a long awkward vertical list/grid collapse.
- Implementation stayed in `projects/mission-control-lab`; Dashboard was not promoted in this segment.
- Lab changes made:
  - Added mobile route handling/classes for Skills, Crons, Memory, and Files so their desktop sidebar is hidden on mobile and their main surface can use the full viewport width.
  - Tuned shared/page mobile CSS so these operational pages do not collapse into a narrow strip at 390px.
  - Memory/Files two-column layouts now collapse cleanly and preserve editor/preview usability on mobile.
  - Files directory table remains horizontally scrollable where columns need more width, while the preview/editor area is usable within the mobile viewport.
  - Skills received a more mobile-friendly single-column/card treatment instead of the cramped grid/list hybrid.
- Validation:
  - Lab `npm run lint` passed.
  - Lab `npm run build` passed with only known existing warnings.
  - Lab restart initially hit stale Lab-owned port/PID state; recovery used the Lab lane’s own restart/cleanup path only, then health passed.
  - Lab lane smoke passed.
  - Mobile screenshots captured for QA:
    - `artifacts/mission-control-audit/mobile-pages/skills-lab-mobile-final.png`
    - `artifacts/mission-control-audit/mobile-pages/crons-lab-mobile-final.png`
    - `artifacts/mission-control-audit/mobile-pages/memory-lab-mobile-final.png`
    - `artifacts/mission-control-audit/mobile-pages/files-lab-mobile-final.png`
- Notes:
  - Files search placeholder still truncates slightly on mobile, but the input remains usable; recommended leaving it unless Philippe flags it during QA.
  - Next step, if Philippe accepts Lab QA: promote the focused mobile-only changes to Dashboard, preserving desktop behavior and avoiding unrelated Dashboard Chat/root workspace diffs.

## Mission Control mobile workspace pages promoted to Dashboard [22:49-23:02 GMT+8]
- Philippe approved the Lab mobile fixes for Skills, Crons, Memory, and Files and requested promotion to the live Dashboard, plus a clean daily-memory/learnings closeout for the session.
- Promoted only the focused mobile workspace changes from Lab to Dashboard:
  - `projects/mission-control/components/shell/AppShellClient.tsx`
  - `projects/mission-control/components/pages/SkillsWorkspaceClient.tsx`
  - `projects/mission-control/app/globals.css`
- Behavior promoted:
  - Skills, Crons, Memory, and Files now get `mobile-workspace-shell-grid` / `mobile-workspace-shell-main` route classes.
  - On mobile, these routes hide the desktop sidebar and use the full viewport width.
  - Memory and Files two-column workspaces collapse cleanly so editor/preview sections are usable.
  - Files keeps its directory table horizontally scrollable inside the content shell when columns need more width.
  - Skills uses mobile data hooks/CSS so narrow screens get a cleaner single-column card treatment instead of a cramped hybrid grid/list.
- Dashboard validation:
  - `npm run lint` passed.
  - `npm run build` passed with known existing warnings only: deprecated `middleware` convention, dynamic autonomous-runner trace, and NFT trace involving files route/adapters.
  - Dashboard restarted using the `.preview-runtime/maintenance.lock` guarded service pattern.
  - Dashboard service health passed.
  - Route checks passed: `/general/skills`, `/general/crons`, `/general/memory`, `/general/files`, `/general/agents`, `/general/tasks`, `/general/home`, `/general/chat` all returned HTTP 200 after retry.
  - `scripts/mission-control-lane-smoke.sh` passed for dashboard/live, lab, and preview.
  - Mobile CDP at 390px confirmed sidebar hidden and no page-level horizontal overflow for Skills, Crons, Memory, and Files. Files table scroll container moved from `scrollLeft 0 -> 160`, as expected.
  - Screenshot artifacts saved:
    - `artifacts/mission-control-audit/mobile-pages/skills-dashboard-mobile-final.png`
    - `artifacts/mission-control-audit/mobile-pages/crons-dashboard-mobile-final.png`
    - `artifacts/mission-control-audit/mobile-pages/memory-dashboard-mobile-final.png`
    - `artifacts/mission-control-audit/mobile-pages/files-dashboard-mobile-final.png`
  - Image inspection confirmed all four live Dashboard mobile pages are usable; only minor note was dismissible task-toast overlap on Skills, already a known separate issue.
- Operational note:
  - The first immediate post-restart route sweep briefly failed on `/general/memory` because local proxy connection was not ready; service health was already recovering. Re-running route checks after health passed succeeded. Treat immediate post-restart local sweeps as retryable only after checking service health/logs.
- Workspace hygiene:
  - Existing unrelated Dashboard Chat diffs remain untouched and unstaged.
  - The promotion intentionally avoided any desktop redesign.

## Mission Control Trading Lab planning [23:08 GMT+7]
- Philippe asked for a clean Trading-section rebuild plan, using uploaded stock dashboard / Zero Sum / portfolio tracker screenshots as product references and keeping all new Mission Control features Lab-first until explicit promotion approval.
- No Mission Control source code was modified for this Trading task.
- Wrote the planning artifact: `projects/mission-control-lab/docs/trading-section-lab-implementation-plan-2026-04-27.md`.
- Plan recommendation: rebuild Trading as **BOILER ROOM**, start with Lab shell identity + static desktop/mobile prototype before wiring real providers; keep Dashboard untouched.
- Visual direction: calm institutional finance terminal, cream paper/dark ink, muted market colors, dense but readable tables/charts, with dark portfolio/X-ray exposure treatment where useful.
- Data direction: separate market/research data, broker/account data, and execution data; start with cached free/low-cost research data such as Yahoo/yfinance/defeat-beta-style + SEC + existing Market Intel, consider FMP only after field needs are proven, use IBKR primarily for account/positions/execution rather than broad free research data.
- Open approval point: before implementation, confirm first slice as Phase 1 shell branding/layout foundation plus static Phase 2 Overview/Ticker prototype in `projects/mission-control-lab` only.

## Mission Control Trading plan correction [23:24 GMT+7]
- Philippe corrected the Trading plan: the Atreus left-side menu pages **Health**, **Analytics**, and **News** should be explicit first-class Trading pages.
- Philippe also said to remove the “existing Mission Control Market Intel redesigned” direction because Trading should follow Atreus as much as possible instead of clinging to old artifacts.
- Updated `projects/mission-control-lab/docs/trading-section-lab-implementation-plan-2026-04-27.md` accordingly: added Health/Analytics/News to content plan, route map, initial nav, and data contracts; removed `/trading/market-intel` and Market Intel redesign language.

## Mission Control Trading nav consolidation [23:31 GMT+7]
- Philippe agreed to consolidate Trading nav so Analytics absorbs Charts/Technical through tabs, and News absorbs Reports through tabs.
- Updated `projects/mission-control-lab/docs/trading-section-lab-implementation-plan-2026-04-27.md` with the consolidated sidebar: Overview, Portfolio, Watchlist, Health, Analytics, News, Screener, Bots.
- Added recommended first implementation slice: Phase 1 plus a static Overview skeleton and placeholder page shells; no market-data dependencies or IBKR integration in the first slice.

## Mission Control Trading Lab first implementation slice [23:45 GMT+7]
- Philippe approved starting the Lab-only Trading build.
- Implemented the first BOILER ROOM slice in `projects/mission-control-lab` only; Dashboard was not touched.
- Added Trading domain shell metadata so Trading shows `BOILER ROOM` while General remains `MARVIN’S ROOM`.
- Replaced the Trading sidebar with the consolidated Atreus-style nav: Overview, Portfolio, Watchlist, Health, Analytics, News, Screener, Bots.
- Added a static Trading Overview skeleton with market tape, account value/P&L, sample chart, health preview, watchlist, exposure preview, news/calendar preview, and section links.
- Added placeholder shells for Portfolio, Watchlist, Health, Analytics, News, Screener, Bots, Signals secondary route, and ticker detail `/trading/ticker/[symbol]`.
- Analytics includes planned tabs for Performance, Attribution, Exposure, Dividends, Charts / Technical; News includes Market News, Watchlist News, Earnings / Calendar, Reports, Filings.
- Validation passed: `npm run lint` and `npm run build` in `projects/mission-control-lab`. Build retained existing known warnings about deprecated middleware convention, dynamic autonomous runner trace, and NFT tracing via files route.

## Mission Control Trading Lab runtime verified after stale bundle [23:58 GMT+7]
- Philippe reported Lab still showed the old Trading section after the first BOILER ROOM commit.
- Diagnosis: source and committed files were correct, but the Lab runtime was serving a stale built bundle. Local `3005/trading` still showed old markers (`Market Intel`, `Bot / Dispatch`), while the intended Lab lane uses `.lab-runtime` with public port `3015` and internal Next `3017`.
- Used the Lab maintenance-lock flow only: created `.lab-runtime/maintenance.lock`, ran `scripts/lab-stop.sh`, `scripts/lab-build.sh`, `scripts/lab-start.sh`, then `scripts/mission-control-service-health.sh`, and removed the lock.
- Verification after restart:
  - `http://127.0.0.1:3015/trading` showed new BOILER ROOM markers: `BOILER ROOM`, `Health`, `Analytics`, `Screener`, `Bots`, `Trading Overview`.
  - `https://lab.motiondisplay.cloud/trading` with Basic Auth also showed the new BOILER ROOM markers.
  - `3005` remained stale/old and should not be treated as the Lab user-facing route for this lane without checking the lane env/proxy.
- Philippe confirmed the new Lab Trading scaffolding works. Current state is intentionally rough scaffolding; tomorrow's likely work is visual polishing and painting toward the Atreus/stock-dashboard screenshots.
