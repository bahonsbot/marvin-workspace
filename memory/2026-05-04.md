- Home-improvement pass:
  - What changed: Added `scripts/check_runbook_index.py` to validate `docs/runbooks/README.md` against the current runbook files and noted the check in the runbooks index usage notes.
  - Why: Runbook additions or renames can drift away from the index and create avoidable lookup friction during autonomous/operator work.
  - Expected benefit: Faster detection of missing, stale, or duplicate runbook index entries with a lightweight local verification step.
  - Rollback: Remove `scripts/check_runbook_index.py` and the added usage-note bullet in `docs/runbooks/README.md`.

## Morning Meeting Decision [12:56]
- Report: nightly-security-review
- Finding: `.env.local` permissions (Lab)
- Decision: Approved
- Problem: `projects/mission-control-lab/.env.local` is mode 644 and world-readable on the host/container filesystem.
- Risk: low, because it holds Convex deployment metadata rather than API secrets, but permissions are looser than intended.
- Action taken: attempted `chmod 600 projects/mission-control-lab/.env.local` from the current session, including elevated exec path, but the file is root-owned and this session cannot mutate it.
- Verification: `stat -c '%a %U %G %n'` shows `644 root root`; direct chmod attempt returned `Operation not permitted`.
- Rollback: n/a
- Notes: host/root follow-up command if desired later: `chmod 600 /data/.openclaw/workspace/projects/mission-control-lab/.env.local`


## Morning Meeting Decision [12:58]
- Report: nightly-security-review
- Finding: `.env.local` permissions (Lab)
- Decision: Approved
- Problem: `projects/mission-control-lab/.env.local` was mode 644 and world-readable on the host/container filesystem.
- Risk: low, because it holds Convex deployment metadata rather than API secrets, but permissions were looser than intended.
- Action taken: Philippe ran `chmod 600` manually in Terminal; verified from this session.
- Verification: `stat -c '%a %U %G %n' /data/.openclaw/workspace/projects/mission-control-lab/.env.local` → `600 root root /data/.openclaw/workspace/projects/mission-control-lab/.env.local`
- Rollback: `chmod 644 /data/.openclaw/workspace/projects/mission-control-lab/.env.local`
- Notes: root ownership remains a separate operational caveat.

## Morning Meeting Decision [12:59]
- Report: self-improvement
- Finding: SI-2026-0503-001 / AUTONOMY.md live modes omission
- Decision: Approved
- Problem: `AUTONOMY.md` did not make it clear that `nightly-memory-extraction` is a separate cron-governed autonomous lane rather than a Workspace Lane live mode.
- Risk: low documentation drift; functional behavior unaffected.
- Action taken: added a scope-distinction sentence to `AUTONOMY.md` clarifying that system cron-owned reasoning jobs like nightly memory extraction are governed separately and are not enumerated as Workspace Lane autonomy modes.
- Verification: direct file read confirms the clarification is present in the Purpose section.
- Rollback: remove the added sentence from `AUTONOMY.md`.
- Notes: chose scope distinction rather than expanding the live-mode list.

## 2026-05-04
- Mission Control Lab Trading: added ETF-specific ticker panels and restarted/verified Lab. Commit `23a5818` makes fund-like ticker pages show Fund profile/Fund metrics, hides company-only financial highlights/cash-debt/balance-sheet sections for ETFs, and uses honest holdings/expense/AUM pending states when providers do not expose those fields. Validation: lint, tsc, trading smoke tests, build; live NUKL.XETRA HTML verified Fund metrics present and company-only sections absent.

- Mission Control Lab Trading: added client-side stale-while-revalidate watchlist metadata cache. Commit `766b703` stores last-known-good provider metadata in browser localStorage for 24h keyed by canonical symbol/requested symbol, renders cached provider names immediately, suppresses old EODHD placeholder names, then refreshes metadata in the background. Validation: lint, tsc, trading smoke tests, build, restarted Lab preview, verified compiled watchlist chunk contains cache key and metadata API returns canonical `NUKL.XETRA`/`IREN.US` names.

- Mission Control Lab Trading: added first ETF fund-facts enrichment adapter. Commit `a84c28e` fetches public justETF profile facts by ISIN, merges verified fund provider/benchmark/TER/AUM/replication/distribution/holdings/top-10/exposure facts into ETF profiles, adds a Fund facts resource link, and renders holdings/cost panels when those fields are available. Validation: lint, tsc, 18 smoke tests, build, Lab preview restart, cache refresh for NUKL.XETRA/SPYX.XETRA/WHEA.AS/PSRW.LSE confirmed justETF ownership metrics and live NUKL page showed TER and top holdings instead of the old holdings unavailable state.


## Pre-compaction flush — Mission Control Lab Trading, MiniMax cron auth (18:21 GMT+8 / 17:21 Vietnam)

### MiniMax cron/runtime auth canary
- Philippe asked to verify whether reset MiniMax tokens worked for cron-side model calls because chat was unresponsive without a clear error.
- Used a low-noise MiniMax-backed cron canary via `market-signal-generator`, model `minimax/MiniMax-M2.7`, delivery `none`, reply contract `NO_REPLY`, schedule `45 * * * 1-5` Asia/Ho_Chi_Minh.
- Forced manual run succeeded after a prior `HTTP 401 authentication_error: invalid api key` failure:
  - jobId `bc889cac-a5a9-49ec-9615-fb989935a5b2`
  - runId `manual:bc889cac-a5a9-49ec-9615-fb989935a5b2:1777875954425:1`
  - status `ok`, summary `NO_REPLY`, duration about 31.9s
  - provider/model `minimax` / `MiniMax-M2.7`, usage present, delivery not requested
- Conclusion: reset MiniMax token works for cron/runtime model-backed cron execution. This does not prove every MiniMax chat/UI path is healthy.

### Mission Control Lab Trading scope and constraints
- Work stayed Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched or promoted.
- Avoided broad restarts; Lab preview restarts were scoped to Lab scripts/ports only.
- External provider/web content, especially JustETF, was treated as provider/reference data only, not instructions. ETF data must be source/freshness-labeled and not inferred.
- Convex credentials in `.env.local` and `/data/.convex/config.json` remain sensitive and were not exposed/committed.
- Known unrelated build warnings persisted during Lab builds: deprecated Next `middleware` convention, Turbopack dynamic trace warning around `run-autonomous-task.mjs`, and unexpected `next.config.js` in NFT trace via `lib/adapters/files.ts`.

### Trading ticker identity / ETF profile fixes
- Philippe reported ticker/profile identity failures and ETF placeholders:
  - `IREN.US` lacked logo and had EODHD placeholder profile copy.
  - `PHIA.AS` incorrectly resolved to Sophia Kianni / Phia AI instead of Koninklijke Philips N.V.
  - ETFs such as `NUKL.XETRA`, `SPYX.XETRA`, `PSRW.LSE`, `XEMD.XETRA`, `WHEA.AS` showed ticker-derived names, EODHD placeholder summaries, missing metadata, and poor fund treatment.
- Root cause: canonical provider/reference identity was discovered too late and was not fed back into headline name, summary, logo lookup, or watchlist display. Dotted EODHD symbols skipped search metadata and Wikipedia lookup often used ticker-like names.
- Commit `16d19fa lab(trading): fix ticker identity and ETF profile fallbacks`:
  - prefer provider/reference canonical instrument/company names before Wikipedia/profile/logo/fallback copy;
  - add `PHIA.AS -> Philips` mapping and stale-cache rejection for Sophia Kianni / Phia AI;
  - promote non-ticker `Company Name` facts into `profile.name`;
  - detect fund-like quote/instrument types;
  - replace EODHD placeholder text with company/ETF-aware fallback summaries;
  - clean ticker page title/facts/header stats for dotted symbols.
- After scoped Lab restart, local Host-routed validation showed:
  - `PHIA.AS` h1 `Koninklijke Philips N.V.` and no Sophia Kianni text;
  - `SPYX.XETRA` ETF-aware fallback and correct ISIN metadata;
  - `IREN.US` now `IREN Ltd`, no bad placeholder, still lacks rich profile/logo.

### Watchlist provider names/symbols and stale metadata
- Philippe noticed watchlist still showed old saved/provider-debug names while ticker pages were fixed, and `IREN.US` displayed as bare `IREN` while other dotted symbols remained dotted.
- Root cause: watchlist table preferred saved Convex row names unless empty/ticker-like, and `displaySymbol()` stripped `.US` as a former shortcut.
- Commit `621371b lab(trading): prefer provider symbols on watchlists`:
  - added `isProviderPlaceholderDisplayName()` to suppress old saved provider-placeholder strings;
  - updated `resolvedWatchlistName()` to prefer provider metadata when saved names are empty, ticker-like, or placeholder-like;
  - added `resolvedWatchlistSymbol()` to prefer `metadata?.symbol`, so provider-backed rows show canonical symbols such as `IREN.US`;
  - updated row links/action labels and Watchlist News placeholders to use provider canonical symbols.
- Validation passed: lint, trading smoke tests, build, API checks for `NUKL.XETRA`, `IREN.US`, and `BESI.AS`.
- Philippe then raised stale-data flash while metadata fetch was pending.
- Commit `766b703 lab(trading): cache watchlist metadata client-side`:
  - added best-effort localStorage cache `mission-control-lab:watchlist-metadata:v1`, TTL 24h;
  - seeds metadata from last-known-good immediately, fetches fresh metadata in background, writes successful responses, and keeps cached data on fetch failure;
  - maps by requested and provider-returned symbols via `metadataMapFromItems()` and resolves metadata via normalized item/display/provider symbols;
  - improved name suppression with safer metadata names.
- Validation passed: lint, `npx tsc --noEmit`, trading smoke tests, build. Lab preview restarted with scoped Lab scripts.

### ETF page UX and JustETF fund facts
- Philippe agreed ETF profile quality should be the next priority before inline watchlist editing, provider-backed Watchlist News, ticker/cache hygiene, and pinned-watchlist Overview work.
- yfinance/quoteSummary proved unreliable for EU ETF fundamentals (`NUKL.XETRA`, `SPYX.XETRA`, `PSRW.LSE`, `WHEA.AS` mostly returned no facts/fundamentals or 404). Decision: use provider/reference identity plus ISIN-based fund-data adapters, and show honest unavailable states when data is absent.
- Commit `23a5818 lab(trading): add ETF-specific ticker panels`:
  - fund-oriented section nav: Overview, Price, Fund profile, Fund metrics, News, Reports, Dividends, Technicals, Glossary;
  - company/equity sections hidden for fund pages, including Financial Highlights, Cash & Debt, Balance Sheet, EPS/company-only financial panels;
  - added fund profile/metric rendering and honest unavailable panels for holdings/cost/structure when provider data is missing.
- Validation passed: lint, trading smoke tests 17/17, build. Host-routed `NUKL.XETRA` check showed fund-specific panels and no company financial sections.

### JustETF ISIN-based ETF enrichment
- Corrected ISIN/reference context:
  - `NUKL.XETRA`: VanEck Uranium and Nuclear Technologies UCITS ETF, ISIN `IE000M7V94E1`.
  - `SPYX.XETRA`: State Street SPDR MSCI Emerging Markets Small Cap UCITS ETF, ISIN `IE00B48X4842`.
  - `WHEA.AS`: SPDR MSCI World Health Care UCITS ETF, ISIN `IE00BYTRRB94`.
  - `PSRW.LSE`: Invesco FTSE RAFI All World 3000 UCITS ETF, ISIN `IE00B23LNQ02`.
- Commit `a84c28e lab(trading): enrich ETF profiles with fund facts`:
  - added `justetf` to `TickerSourceName`;
  - created `lib/trading/sources/justetf-fund-facts.ts` to fetch `https://www.justetf.com/en/etf-profile.html?isin=<ISIN>` with browser-like UA, timeout, `no-store`, and parse stripped HTML;
  - parses provider, benchmark, TER/expense ratio, AUM/fund size, replication, distribution, domicile, holdings count, top 10 weight, holdings as-of date, top holdings, countries, sectors, and profile resource link;
  - `reference-enrichment.ts` fetches JustETF in parallel with FinanceDatabase/yfinance/filings, merges JustETF facts into profile facts, uses JustETF source for missing/generic fund profiles, replaces generic fund summaries where appropriate, adds Fund facts resources, and merges holdings/exposure/cost into supplemental ownership;
  - ticker page displays fund metrics and parsed holdings/exposure/cost when available, while retaining honest unavailable states when absent;
  - CSS added `.trading-fund-list-panel`; smoke tests increased to 18.
- Validation after fixes passed: `npm run lint`, `npx tsc --noEmit`, `python tests/trading-provider-smoke.test.py` 18/18, `npm run build`.
- Manual verification:
  - JustETF fetch for `IE00B48X4842` returned 200 HTML with ISIN, Total expense ratio, Holdings.
  - Adapter parse for `SPYX`: State Street, MSCI Emerging Markets Small Cap, TER 0.55%, AUM EUR 449m, Physical, Accumulating, holdings 1,885, top 10 4.67%, holdings as of 31/03/2026.
  - `NUKL`: VanEck, MarketVector Global Uranium and Nuclear Energy Infrastructure, TER 0.55%, AUM EUR 2,291m, holdings 25, top 10 65.56%, holdings include Cameco 15.46%, NexGen Energy 7.24%, Sprott Physical Uranium 6.69%, Oklo 5.94%, Jacobs Solutions 5.24%, Fuji Electric 5.14%.
  - `WHEA`: State Street, MSCI World Health Care, TER 0.30%, AUM EUR 458m, holdings 117, top 10 45.44%, holdings include Eli Lilly, Johnson & Johnson, AbbVie, AstraZeneca.
  - `PSRW`: Invesco, RAFI Fundamental Global, TER 0.39%, AUM EUR 194m, Distributing, holdings 1,171, top 10 13.03%, holdings as of 26/03/2026.
- Repo caveat: Lab `.gitignore` can make new Lab files appear ignored from workspace root; `justetf-fund-facts.ts` had to be force-added. Commit includes the new file.

### ETF holdings/exposure UI split and duplicate removal
- Philippe asked whether JustETF data auto-loads for unseen ETFs and noted duplicate information between Fund Analytics and bottom Holdings for `NUKL.XETRA`. Also requested differentiated sub-sections because holdings, countries, and sectors were shown in one undifferentiated list/style.
- Answer: yes, unseen ETFs auto-load through the normal `getTickerProfile()` path if the profile resolves to an ISIN and fund-like instrument. No manual prewarm is required beyond opening the ticker page/API path; cache TTL/stale cache can affect refresh timing.
- Commit `d5a4039 lab(trading): split ETF holdings analytics`:
  - added JustETF metric category notes (`Fund holding`, `Country exposure`, `Sector exposure`) in the parser;
  - split ETF ownership metrics into `summary`, `holdings`, `countries`, `sectors`, and `cost` groups;
  - added `FundAnalyticsPanel()` and grouped UI cards inside one canonical `Fund analytics` / `Holdings and exposure` section;
  - kept `Fund cost & structure` as the separate cost/structure section;
  - removed bottom `#ownership` Holdings card for fund pages to avoid duplicate content;
  - updated fund nav to include Analytics, Cost, Reports, Dividends, Technicals, Glossary;
  - added CSS for grouped fund analytics sections and updated smoke tests to 19.
- Validation passed:
  - `npm run lint`
  - `npx tsc --noEmit`
  - `python3 tests/trading-provider-smoke.test.py` 19/19
  - `npm run build`
- Runtime verification after scoped Lab preview restart:
  - Host-routed `NUKL.XETRA` page showed `Holdings summary`, `Top holdings`, `Country exposure`, `Sector exposure`, `Cameco`, `Canada`, and `Industrials`;
  - no duplicate `id="ownership"` and no `Holdings feed pending` on the fund page.

### Current open Trading follow-ups
- ETF issuer/logo strategy remains unresolved; ETF logos were not added.
- IREN richer summary/logo enrichment remains pending without faking data.
- Real provider-backed Watchlist News remains pending.
- Watchlist inline editing for priority/alert/watch note remains pending.
- Ticker/profile cache hygiene and refresh/debug route remains pending.
- Trading Overview consuming pinned Convex watchlists remains pending.
- Possible duplicate logical watchlist entries, including duplicate `IREN.US`, remain unresolved.
- Public/browser Lab may require refresh/cache clearing if stale client code is visible.

### Heartbeat/system monitoring during work
- Heartbeat policy remained quiet monitoring only; return `HEARTBEAT_OK` if no attention needed.
- Trading bot endpoints checked OK:
  - `http://127.0.0.1:8000/health` returned OK/paper_only.
  - `http://127.0.0.1:8000/health/auth` returned auth valid/paper_only.
- Queue triage at one point showed 14 total tasks, 13 completed, 1 blocked, 0 pending/spawned, no stale spawned warnings, and no runnable items.

### Git/workspace caveats
- Relevant committed Lab Trading milestones today: `16d19fa`, `621371b`, `23a5818`, `766b703`, `a84c28e`, `d5a4039`.
- Generated ticker-profile cache files and other unrelated workspace-root changes were observed during validation. Do not commit generated cache/memory/unrelated files unless explicitly intended.
- `memory/2026-05-04.md` itself was modified/generated during work and should not be included in Lab feature commits unless explicitly intended.

## Mission Control Lab Trading ETF ticker wrap-up [18:10 Vietnam / 19:10 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not promoted or modified. Sensitive Convex env/config files remained untouched.
- Final ETF ticker-page polish completed and committed after Philippe's screenshot-guided feedback.
- Commit `23e6f51 lab(trading): refine ETF ticker layout`:
  - removed `Recent news` from ETF/fund ticker pages while leaving regular stock ticker news intact;
  - moved `Fund cost & structure` above analytics and filled it from JustETF facts when available, including TER/expense ratio, AUM/fund size, replication, and distribution policy;
  - made `Fund analytics` full-width;
  - removed repeated per-subsection `Sources: JUSTETF` noise and kept one section-level `Source: JUSTETF`;
  - added compact horizontal exposure bars for country/sector allocation;
  - split long ETF fund-profile summaries into shorter paragraphs.
- Commit `ea2e13f lab(trading): seal ETF profile presentation`:
  - lightened typography in `Fund cost & structure` and `Fund analytics` to better match Mission Control's restrained style;
  - kept `Fund Profile` identity-only for ETFs: Instrument Type, Exchange, Country, ISIN;
  - moved provider, benchmark, domicile, AUM, TER, replication, distribution, holdings count/top-10 weight, and holdings date into `Fund Metrics` / dedicated ETF sections;
  - added smoke coverage for the identity-only profile rule and lighter typography markers.
- Commit `1a925b3 lab(trading): hide stock-only ETF sections`:
  - removed `Finance glossary`, `Estimates`, and `EPS estimates` from ETF/fund ticker pages only;
  - removed Glossary from ETF section nav;
  - kept stock ticker behavior unchanged;
  - added smoke coverage confirming ETF pages omit stock-only glossary/estimate sections.
- Validation for the final ETF sequence:
  - `npm run lint` passed;
  - `npx tsc --noEmit` passed;
  - `python3 tests/trading-provider-smoke.test.py -v` passed, 22/22 after the final ETF-only section removal;
  - `npm run build` passed with only known unrelated warnings: deprecated Next `middleware` convention, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning;
  - scoped Lab preview restart/render check for `NUKL.XETRA` confirmed ETF page no longer renders Finance glossary, `id="finance-glossary"`, Estimates, `id="estimates"`, or EPS estimates, while `Fund cost & structure`, `Fund analytics`, Dividends, and Technicals remain.
- Operational note: one render check initially saw stale Lab preview output after the build; a scoped Lab preview restart fixed it. Treat fresh Lab restart/cache state as required evidence for UI claims when compiled output matters.
- Current ETF ticker page state is sealed for now. Remaining Trading follow-ups for a future session: ETF issuer/logo strategy, richer IREN summary/logo without faking data, real provider-backed Watchlist News, watchlist inline editing for priority/alert/watch note, ticker/profile cache hygiene/debug refresh route, Trading Overview consuming pinned Convex watchlists, possible duplicate logical watchlist entries such as duplicate `IREN.US`.

## Mission Control Lab Trading Watchlist News [18:42 Vietnam / 19:42 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not promoted or modified.
- Implemented real provider-backed Watchlist News for the active watchlist by extending the existing `/api/trading/watchlist-metadata` route instead of adding a second endpoint or Convex schema change.
- Data flow: the route already resolves each active symbol through `getTickerProfile()`; it now also collects usable linked `recentNews` items from each profile, filters provider-pending/unavailable placeholders, caps at 3 headlines per symbol and 18 total, and deduplicates by URL/title.
- UI: `WatchlistNews` now renders source/ticker/time-labeled linked headline rows when provider news exists, and keeps an honest empty state when no provider-linked headlines are available.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 22/22; `npm run build`; scoped Lab restart; `scripts/lab-health.sh`; `scripts/lab-lane-smoke.sh`; direct API check for `NVDA,IREN.US,NUKL.XETRA` returned 3 metadata rows and 6 linked headlines.
- Visual caveat: headless Chromium screenshot of `/trading/watchlist` stayed in Convex loading state, so rendered headline rows were verified through the live API and build/runtime gates rather than the hydrated page screenshot. Layout shell showed no obvious breakage.
- Known unrelated build warnings persisted: deprecated Next `middleware` convention, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning via `lib/adapters/files.ts`.
- Next recommended slice: explain and scope inline editing for Priority / Alert / Watch note before implementation, as requested by Philippe.

## Mission Control Lab Trading Watchlist News cache [18:59 Vietnam / 19:59 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`.
- Added client-side stale-while-revalidate cache for Watchlist News, separate from the existing metadata cache: `mission-control-lab:watchlist-news:v1`, TTL 30 minutes, keyed by normalized sorted active-list symbols.
- Behavior: when switching back to a recently loaded watchlist, cached headlines render immediately; `/api/trading/watchlist-metadata` still refreshes in the background; on refresh failure, cached headlines remain instead of reverting to an empty/loading state.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 22/22; `npm run build`; source assertion for news cache wiring; Lab health; Lab lane smoke; direct API check still returned 3 metadata rows and 6 linked headlines for `NVDA,IREN.US,NUKL.XETRA`.
- Operational note: first scoped Lab restart attempt hit transient `EADDRINUSE` on Lab-only ports; no broad process kill was used. A follow-up start produced healthy Lab PIDs and passed `lab-health`/`lab-lane-smoke`.
- Known unrelated build warnings persisted: deprecated Next `middleware` convention, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning.

## Mission Control Lab Trading inline watchlist edits [19:14 Vietnam / 20:14 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`.
- Implemented inline editing for Watchlist row Priority and Alert as immediate row-level dropdowns using existing `watchlistApi.update`.
- Implemented Watch note editing in the row details popover with textarea plus Save/Cancel. Existing notes show a subtle dot/state on the `…` row button; delete remains a separate row action.
- Added row-level saving/error handling for updates without implying alerts trigger notifications yet.
- Hardened `convex/watchlist.ts` update mutation so it only patches fields explicitly provided. This prevents priority/alert-only edits from clearing `thesis`, provider metadata, tags, or other optional fields.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 22/22; `npm run build`; Lab health; Lab lane smoke; source assertions for inline edit wiring and patch safety.
- Known unrelated build warnings persisted: deprecated Next `middleware` convention, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning.

## Mission Control Lab Watchlist News layout polish [19:57 Vietnam / 20:57 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`.
- Updated Watchlist News from a long one-column ticker-grouped feed into a denser desk-style module: max 16 rendered/provider-returned items, two-column desktop grid, one-column mobile fallback, tighter spacing, two-line summaries, and regular-weight serif headlines for a more newspaper-like feel.
- Added lightweight Watchlist News controls: ticker filter (`All tickers` plus symbols present in the feed) and sort selector (`Newest` default, `Ticker` optional). Client keeps max 16 visible after filtering/sorting.
- Added `publishedAt` to provider news items from Yahoo/EODHD and carried it through the watchlist metadata API so API sorting can be newest-first globally while preserving max 3 items per ticker.
- Runtime validation: live Lab API for `NVDA,IREN.US,NUKL.XETRA,AVGO.US,BESI.AS,ASML.AS,SNDK.US` returned 7 metadata rows and 16 linked news items after Lab restart; top output was newest-first by relative time. Served assets contained the new controls/CSS strings.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 22/22; `npm run build`; Lab health; Lab lane smoke; live API cap check.
- Visual caveat: headless Chromium screenshot again stayed in Convex/loading state, so actual hydrated two-column article rendering was verified through served bundle/API/source rather than screenshot pixels.
- Known unrelated build warnings persisted: deprecated Next `middleware` convention, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning.

## Mission Control Lab watchlist move + logo first pass [22:32 Vietnam / 23:32 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not promoted.
- Started Philippe's ordered follow-up list with item 1 and item 5, plus the urgent CRWV logo correctness issue.
- Replaced row `…` watch-note editing with a move menu at the same location. The row action now shows a compact move icon, lists other watchlists, and moves the item to the selected list while preserving symbol/name/exchange/currency/tags/thesis/priority/alert where applicable. If the target already has the symbol, it merges current settings into the target duplicate and removes the original source row.
- Added Convex mutation `watchlist.move` and generated/deployed Convex bindings with `npx convex codegen`.
- Kept add-form thesis/watch-note support in place for now; only the row `…` note editor was removed because Philippe said he would not use that interaction.
- Fixed watchlist/ticker logo centering by making logo tiles true centered flex boxes and forcing logo images to `width: 100%; height: 100%; object-fit: contain; object-position: center;`. This addresses rectangular wordmarks such as Sandisk/Broadcom sitting high in the thumbnail.
- Diagnosed CRWV.US Google logo: cached profile had Wikipedia/Wikidata entity `Q95` and `Google_2015_logo.svg` for CoreWeave because generic Wikipedia lookup on EODHD's long common-stock name selected a Google-related search result. Added explicit `CRWV` / `CRWV.US -> CoreWeave` title mapping and a defensive Google-logo block for CRWV. Removed stale `data/trading/ticker-profiles/CRWV_US.json`.
- Runtime validation after Lab restart: `CRWV.US` watchlist metadata returned `logoUrl: null` instead of Google; AVGO/SNDK still returned their Wikimedia wordmark logos. This is a correctness fallback until a real CoreWeave logo source is added later.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab health; Lab lane smoke; live CRWV metadata check; served bundle contains `trading-watchlist-move-list` and `watchlist:move`.
- Known unrelated build warnings persisted: deprecated Next `middleware`, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning.
