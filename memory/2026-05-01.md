
## Lab ticker estimates layout refinement [23:18 GMT+7]
- Philippe reported remaining ASML ticker issues: ASML.AS initially rendered EUR then client quote refresh flipped it to USD; Estimates ordering/hierarchy still felt off; EPS needed clearer labeling/separation; and the bottom sections looked misaligned.
- Implemented and committed `100cfd7 Refine Lab ticker estimates layout` in Lab only.
- Fixes:
  - `/api/trading/quote/[symbol]` now resolves EODHD search metadata for canonical symbols and uses the provider currency, fixing ASML.AS refresh from overwriting EUR quote display with USD.
  - Estimates target table now sorts by numeric target from low → high instead of mean/median/high/low ordering.
  - Removed the unexplained standalone `1500.00` above the range bar; median target now lives in the summary row and mean is labelled explicitly in the range header.
  - Recommendation bar is taller and contains recommendation count labels inside the bar where space allows.
  - EPS estimates moved into a separate `EPS estimates` section instead of being buried inside the Estimates block.
  - Bottom provider grid now makes Estimates full-width, with EPS / Dividends / Ownership / Technicals arranged below.
- Validation: `npx tsc --noEmit`, `npm run lint`, `npm run build`, Lab preview restart, `scripts/lab-health.sh`, live `/trading/ticker/ASML.AS` 200, and live `/api/trading/quote/ASML.AS` returned `price: EUR ...`. HTML checks confirmed no `USD 1,` on ASML page, `EUR 1,` present, `Median target`, `Price target range`, `EPS estimates`, `Strong Buy`, and `Sources: Yahoo / yfinance` present.
- QA note: full-page screenshot `tmp/mobile-check/asml-estimates-layout.png` was produced but the automated viewport did not reach the lower Estimates section; HTML structure confirms sections exist. If visual confidence is needed, take a targeted lower-page screenshot/crop next.

## Lab ticker lower-section final alignment [23:39 GMT+7]
- Philippe requested final lower-section tuning after screenshot review: change top Estimates third summary cell from Median Target to Mean, remove the floating mean label above the price-target bar, show only Low / Median / High under the bar, make analyst-bar text more minimal/mono, standardize EPS/Dividends/Ownership/Technicals typography, and force the four lower modules into one desktop row.
- Implemented and committed `3a25a81 Finalize Lab ticker lower section layout` in Lab only.
- Changes:
  - Estimates top summary cells now read Consensus / Analysts / Mean.
  - Price target range header no longer repeats the mean value.
  - Target table now renders only Low, Median, High, aligned left/center/right.
  - Analyst bar internal labels now use a smaller monospaced uppercase style instead of bold/italic-looking typography.
  - EPS Estimates, Dividends, Ownership, and Technicals now share the same compact label/value typography and row rhythm.
  - Desktop provider grid is now four columns under full-width Estimates, so EPS / Dividends / Ownership / Technicals fit in one row; it falls back to two columns below 1280px and one column on mobile.
- Validation: `npx tsc --noEmit`, `npm run lint`, `npm run build`, Lab preview restart, `scripts/lab-health.sh`, live `/trading/ticker/ASML.AS` 200. HTML checks confirmed `Mean` present, `Median target` absent, no extra mean value in the range header, `median` row present, `mean` row removed, `EPS estimates` and `technicals` present, no `USD 1,` on ASML page, and `EUR 1,` present.
- Screenshot artifact: `/data/.openclaw/workspace/tmp/mobile-check/asml-estimates-final-row.png` (uncommitted QA artifact).

## Lab Trading pause point after bottom-section redo [23:44 GMT+7]
- Philippe called the Lab Trading ticker bottom section good enough for today and asked to save the continuation foundation for tomorrow.
- Current Lab-only ticker-page state is committed through:
  - `100cfd7 Refine Lab ticker estimates layout`
  - `3a25a81 Finalize Lab ticker lower section layout`
  - memory/log commits through `353d6cc`
- Current validated state:
  - ASML.AS quote refresh no longer flips EUR to USD; `/api/trading/quote/ASML.AS` returns `price: EUR ...`.
  - Estimates now uses a full-width analyst section with Consensus / Analysts / Mean summary cells.
  - Price target range row shows Low / Median / High only, aligned left / center / right.
  - Analyst distribution bar uses compact mono-style labels.
  - EPS Estimates, Dividends, Ownership, and Technicals share compact typography and fit into one desktop row under Estimates, with responsive fallback below 1280px.
  - Validation passed: `npx tsc --noEmit`, `npm run lint`, `npm run build`, Lab preview restart, `scripts/lab-health.sh`, live ASML route smoke.
- Tomorrow / continuation tasks:
  1. Do a targeted visual QA screenshot of the lower ticker sections after scrolling to Estimates, not just full-page top capture. Confirm Estimates / EPS / Dividends / Ownership / Technicals actually look balanced in the rendered viewport.
  2. Investigate why the Price History chart area can appear blank in screenshots even though range stats/data are populated. Check `TickerPriceChart`, client hydration, Lightweight Charts sizing, and container height/visibility.
  3. Decide whether the top MarketTape should be wired to real/live index data or explicitly labelled/static. It still uses sample/static `marketTape` data.
  4. Consider simplifying noisy unavailable financial-statement areas for non-US tickers like ASML.AS: Financial Highlights, Cash & Debt, Balance Sheet, and Financials Overview still show provider-required placeholders because EODHD Extended does not include fundamentals.
  5. Later data-source work: global/non-US filings & reports, richer non-US fundamentals, and possibly better analyst/ownership attribution if yfinance coverage looks inconsistent.
  6. Keep all remaining work in `projects/mission-control-lab` only; do not promote or touch live Dashboard without Philippe explicitly approving.
- Notes for next pass:
  - Use ASML.AS as the first visual QA case, then smoke AAPL.US and VCB.VN to avoid overfitting the UI to a single European ticker.
  - Preserve the honest-data rule: no sample/fake numbers in real ticker decision modules; show clear unavailable states when provider-backed data is missing.

## Workspace home-improvement: queue_state timestamp hardening [08:31 MYT]
- Changed `scripts/queue_state.py` to accept more timestamp formats (`YYYY-MM-DD HH:MM:SS`, ISO `Z`, and ISO offsets) and to fail clearly if `memory/executor-subagent-queue.json` is not a JSON list.
- Why: this is a low-risk helper-script hardening pass that reduces false stale-task detection and confusing queue-load failures as workspace tooling evolves.
- Expected benefit: queue diagnostics and stale-heal logic stay reliable across more log/export timestamp shapes, with clearer operator errors on malformed queue data.
- Rollback: revert the `scripts/queue_state.py` parsing/validation block to the previous narrower format list.

## Morning Meeting Decision [12:31 GMT+7]
- Report: nightly-security-review
- Finding: 2026-05-01 nightly security review clean
- Decision: Accepted risk
- Problem: none requiring action; only standing accepted-risk informational items remain
- Risk: low and unchanged; localhost-bound webhook auth posture and `Math.random()` sudo run IDs remain accepted-risk/info only
- Action taken: none
- Verification: reviewed `memory/security/2026-05-01-nightly-security-review.md` and confirmed verdict CLEAN with 0 critical / 0 high / 0 medium
- Rollback: n/a
- Notes: suppress repeat discussion unless state changes

## Morning Meeting Decision [12:37 GMT+7]
- Report: platform-health-council
- Finding: PHC-2026-0501-001 / workspace-home-improvement delivery contract mismatch
- Decision: Approved
- Problem: the report flagged `workspace-home-improvement` as having a noisy delivery-contract mismatch because the job was silent by config but prior runs still left an internal unresolved `last` routing note in run metadata.
- Risk: low; the job itself was completing, but the contract was not explicit and left avoidable ambiguity in cron evidence.
- Action taken: verified run history, confirmed the job was already effectively silent from a user-visible perspective, then tightened the cron prompt so it explicitly states it is an intentionally silent maintenance job and must not attempt any announce/route behavior, replying with `NO_REPLY` only.
- Verification: `cron runs` for job `41f4af56-1187-42de-8396-c3f75d19be71` showed recent runs finishing `status=ok` with `summary=NO_REPLY` and `deliveryStatus=not-requested`; cron job definition now shows the explicit silent-job instruction in payload text with `delivery.mode=none`.
- Rollback: remove the added explicit-silence line from the cron payload if a future decision is made to announce these results somewhere.
- Notes: PHC overstated this slightly; the real issue was noisy contract ambiguity, not a broken home-improvement execution path.

## Workspace home-improvement: queue_state locking hardening [13:43 MYT]
- What changed: hardened `scripts/queue_state.py` so queue reads/writes use the queue lock, stale-heal writes are atomic with a temp-file replace, and timestamp parsing trims whitespace before parsing; added `tests/test_queue_state.py` coverage for offset timestamps, stale-heal behavior, and invalid non-list queue JSON.
- Why: `queue_state.py` is an operator helper that can run near other queue tooling, so it should not risk partial writes or unlocked read-modify-write races.
- Expected benefit: queue diagnostics and stale-heal runs stay safer under concurrent use, while the new tests reduce regression risk for the timestamp/parser edge cases already supported in production.
- Rollback: revert `scripts/queue_state.py` and remove `tests/test_queue_state.py` if this hardening causes any unexpected workflow issue.

## Morning Meeting Decision [12:43 GMT+7]
- Report: platform-health-council
- Finding: PHC-2026-0501-002 / stale `lossless-claw` config-warning entries
- Decision: Deferred
- Problem: `openclaw.json` still contains stale `lossless-claw` plugin config references that create recurring warning noise even though the extension still functions.
- Risk: low; this is warning noise, not a live functional failure.
- Action taken: none
- Verification: reviewed `memory/health-council/2026-05-01.md` and confirmed the finding is low-risk config-noise only.
- Rollback: n/a
- Notes: defer until a future OpenClaw update or a deliberate config-cleanup window.

## Morning Meeting Decision [12:58 GMT+7]
- Report: self-improvement
- Finding: 2026-05-01 self-improvement review clean
- Decision: Accepted risk
- Problem: none; no new actionable drift, conflict, or documentation issue was found.
- Risk: low to none; deferred and accepted-risk items remain unchanged and correctly suppressed.
- Action taken: none
- Verification: reviewed `memory/self-improvement/2026-05-01.md` and confirmed verdict CLEAN with 0 new findings requiring action.
- Rollback: n/a
- Notes: close self-improvement for today unless state changes.

## Morning Meeting closeout [13:00 GMT+7]
- Morning Meeting completed using the usual one-item-at-a-time protocol.
- Nightly Security Review: accepted risk / no action. Verdict remained CLEAN with only standing info-level accepted-risk items.
- Platform Health Council:
  - PHC-2026-0501-001 approved and tightened: `workspace-home-improvement` cron contract was clarified as intentionally silent (`delivery.mode=none`, explicit `NO_REPLY`-only instruction) after verifying the run path itself was healthy.
  - PHC-2026-0501-002 deferred: stale `lossless-claw` config-warning entries are low-risk noise and can wait for a future OpenClaw update or deliberate config-cleanup window.
- Self-Improvement Review: accepted risk / no action. Report was CLEAN with 0 new actionable findings.
- Dreaming update: light/rem/deep all ran; recall artifacts were repaired and recent Lab work was staged, but 0 items were promoted into `MEMORY.md`.
- Home-improvement update to report in meeting: morning pass delivered `scripts/queue_state.py` timestamp-format hardening so queue diagnostics accept more timestamp shapes and fail clearly on malformed non-list queue JSON.
- Current Morning Meeting outcome: report stack handled, dreaming handled, home-improvement handled, and daily memory is now up to date for a fresh follow-up session.
- Recommended fresh-session carry-forward:
  1. Mission Control Lab: targeted visual QA screenshot of ASML lower ticker sections.
  2. Investigate blank-looking Price History chart despite populated data.
  3. Decide whether MarketTape should become real/live or be explicitly labeled static.
  4. Consider simplifying placeholder-heavy non-US financial sections.
- Preserve lane rule: keep Trading / BOILER ROOM work in `projects/mission-control-lab` only unless Philippe explicitly approves touching the live Dashboard.

## Lab Trading MarketTape truth label [13:16 GMT+7]
- Philippe clarified the Price History chart issue was only a Marvin-side render/screenshot limitation because the chart works for him, so it is not being treated as a product bug.
- Investigated the top Trading MarketTape and confirmed it still uses static sample values from `components/pages/trading/trading-sample-data.ts` on both `/trading` and ticker pages.
- Applied the smallest Lab-only truthfulness fix: `MarketTape` now renders a visible status label, defaulting to `Static sample tape · live market tape not wired yet`, above the tape.
- Files changed: `projects/mission-control-lab/components/pages/trading/shared.tsx` and `projects/mission-control-lab/components/pages/trading/trading.module.css`.
- Validation: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed in `projects/mission-control-lab`; build output confirmed the `/trading` prerender includes the static-sample label. Build still reports the two known unrelated Turbopack warnings around autonomous task runner tracing / next.config NFT tracing.
- Next possible slice: replace the static tape with a real provider-backed tape after choosing source strategy. EODHD-backed ETF proxies such as `SPY.US`, `QQQ.US`, `DIA.US`, `VIXY.US`, `GLD.US`, and `USO.US` returned valid realtime/delayed quotes in a smoke test, but true index/rate/commodity mapping should be designed deliberately rather than hidden behind sample labels.

## Lab Trading EODHD ETF-proxy MarketTape [13:39 GMT+7]
- Philippe approved replacing the static/sample MarketTape with a real EODHD-backed ETF-proxy tape so the task can come off the Trading continuation list.
- Implemented Lab-only `lib/trading/market-tape.ts` using EODHD realtime/delayed quotes for explicit ETF proxies:
  - `SPY.US` as `S&P proxy`
  - `QQQ.US` as `Nasdaq proxy`
  - `DIA.US` as `Dow proxy`
  - `VIXY.US` as `VIX proxy`
  - `GLD.US` as `Gold proxy`
  - `USO.US` as `Oil proxy`
- Wired `/trading` and `/trading/ticker/[symbol]` to fetch the server-side tape instead of importing hardcoded `marketTape` sample data.
- The tape status now says `EODHD ETF-proxy tape · delayed quotes · updated ...` when data is available, and shows an unavailable/API-key/provider-empty status rather than falling back to fake numbers.
- Validation: `npx tsc --noEmit`, `npm run lint`, `npm run build`, Lab preview restart, and `scripts/lab-health.sh` all passed. Live HTML checks on `http://127.0.0.1:3015/trading` and `/trading/ticker/ASML.AS` confirmed `EODHD ETF-proxy tape`, proxy labels, live quote values, and no `Static sample tape` text.
- Known caveat: this is intentionally ETF-proxy market context, not raw index/rate/commodity data. Future data-source work can replace proxies with true index/rates/commodity feeds if needed.

## Lab Trading MarketTape corrected to actual indices [13:53 GMT+7]
- Philippe correctly challenged the ETF-proxy tape because `QQQ.US` showed an ETF price around `667.74` while the Nasdaq index should show around `24,892.31` points.
- Verified EODHD actual index symbols work despite search not surfacing them cleanly:
  - `IXIC.INDX` returned Nasdaq Composite around `24,892.31`.
  - `GSPC.INDX` returned S&P 500 around `7,209.01`.
  - `DJI.INDX` returned Dow around `49,652.14`.
  - `VIX.INDX` returned VIX around `16.89`.
- Corrected `lib/trading/market-tape.ts` to use actual EODHD index symbols for S&P 500, Nasdaq, Dow, and VIX; kept `XAUUSD.FOREX` for Gold spot using `previousClose` when current close is unavailable; kept only Oil as an explicit `USO.US` proxy because tested WTI/oil symbols returned unavailable/NA under the current endpoint/plan.
- Status wording changed from `EODHD ETF-proxy tape` to `EODHD market tape · delayed quotes · Oil uses ETF proxy · updated ...`.
- Validation: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed. Lab preview restart initially hit the known EADDRINUSE/stale-port race, but a follow-up `scripts/lab-health.sh` passed with the new runtime healthy. Live HTML checks on `/trading` and `/trading/ticker/ASML.AS` confirmed `EODHD market tape`, actual index labels/values including `Nasdaq 24,892.31`, `Gold spot`, `Oil proxy`, and no remaining `ETF-proxy tape` / `Nasdaq proxy` text.
- Lesson: EODHD search can miss actual index symbols; verify direct known symbols before assuming only ETF proxies are available.

## Lab Trading MarketTape meta polish [14:09 GMT+7]
- Philippe requested the long tape status (`EODHD MARKET TAPE · DELAYED QUOTES · OIL USES ETF PROXY · UPDATED ...`) be simplified and moved to the right side of the market-data ribbon, using a lighter font similar to the ticker quote freshness text.
- Changed `MarketTape` so the status sits inline as the final ribbon cell instead of a separate top row.
- Changed tape status text to simple `Updated: 01 May, 04:16` style; removed duplicate `EODHD market tape`, `delayed quotes`, and `Oil uses ETF proxy` wording because `Oil proxy` is already visible as the item label.
- Styled `.trading-market-tape-status` with the same quiet small-font feel as ticker refresh metadata: lighter color, 10.5px, normal weight, tabular numerals, right-aligned inside the ribbon.
- Validation: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed. Lab preview restart briefly reported a PID-file race, then `scripts/lab-health.sh` passed. Live `/trading` HTML confirmed `Updated: 01 May`, no `EODHD market tape` / `Oil uses ETF proxy`, and `Oil proxy` remains visible.

## Lab ticker bottom-section typography cleanup [14:13 GMT+7]
- Philippe asked to apply the cleaner lighter font treatment used for the market-tape `Updated: ...` cell to the mono-style elements in the bottom ticker modules shown in the screenshot.
- Updated lower ticker CSS in `components/pages/trading/trading.module.css`:
  - Estimates labels, price-target labels, EPS labels, and range header metadata now use lighter sans metadata styling instead of heavy uppercase/mono styling.
  - Estimate target/EPS values now use inherited sans with normal weight and tabular numerals instead of `ui-monospace` heavy values.
  - Recommendation bar labels now use inherited sans, lighter weight, and non-uppercase treatment.
  - EPS / Dividends / Ownership / Technicals lower-module `dt`/`dd` typography now uses the same quiet small sans metadata style and normal-weight values.
- Answered Philippe's market-tape update question: it refreshes on server render/page load/navigation, not with a client-side 60s polling loop like the ticker title quote block.
- Validation: `npx tsc --noEmit`, `npm run lint`, `npm run build`, Lab preview restart, and `scripts/lab-health.sh` passed. Live `/trading/ticker/ASML.AS` HTML confirmed the lower estimate sections render and no inline `font-family:ui-monospace` marker appears in page HTML.

## Lab ticker minor polish: market tape polling, quote layout, estimates markers [14:47 GMT+7]
- Philippe requested four small polish changes before larger Trading work.
- Market tape: added `/api/trading/market-tape` and a client `MarketTapeClient` wrapper. The tape now checks every 60 seconds while the page is visible, but only when the client-side New York market-hours gate says US markets are open. Server response also includes `isMarketOpen` for initial state/diagnostics.
- Ticker title bar: swapped the manual `Refresh` button above the quote freshness text and centered the quote-refresh stack so the button sits more centrally in the price block.
- Estimates: changed the price-target range rail markers from thicker pills to slim vertical lines.
- Estimates: reduced rail markers to Low / Mean / High only, at their value-based positions, and changed the table under the rail from Low / Median / High to Low / Mean / High.
- Validation: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed. Lab preview restart again hit the known stale-port/PID race; a port-scoped Lab cleanup/restart restored health. `scripts/lab-health.sh` passed. Live checks confirmed `/api/trading/market-tape` returns status + `isMarketOpen`, ticker page includes `data-market-open`, `Refresh current quote`, `Mean` but not `Median`, and the range rail has Low/Mean/High markers.
- Note: current `isMarketOpen` is a simple weekday 09:30-16:00 America/New_York gate and does not yet account for US exchange holidays or half-days.

## Lab Trading ticker/page design phase good enough for now [15:00 GMT+7]
- Philippe signed off the current Lab Trading page/ticker design phase as `good enough for now` before moving to larger Trading work.
- Current Lab design checkpoint is committed through `4effc1e Polish Lab ticker refresh and estimates`.
- Good-enough current state:
  - MarketTape uses real EODHD-backed index data for S&P 500, Nasdaq, Dow, VIX, Gold spot, and an explicitly labelled Oil proxy.
  - MarketTape update metadata is quiet and inline, and the tape now has a 60s visible-page polling path gated by simple US regular market hours.
  - Ticker title quote block has the Refresh button centered above the Updated / Refreshed / Delayed quote metadata.
  - ASML.AS quote refresh stays in EUR instead of flipping to USD.
  - Estimates / EPS / Dividends / Ownership / Technicals have the lighter typography pass and balanced bottom layout.
  - Estimates price-target range now uses slim Low / Mean / High markers and Low / Mean / High values under the rail.
- Validation already completed for the final polish: `npx tsc --noEmit`, `npm run lint`, `npm run build`, Lab health, and live `/trading/ticker/ASML.AS` checks.
- Remaining known caveats intentionally deferred:
  - MarketTape market-hours logic is simple weekday 09:30-16:00 America/New_York and does not yet include exchange holidays or half-days.
  - Oil remains `Oil proxy` via `USO.US` because direct oil/futures symbols tested unavailable/NA on the current EODHD path.
  - Non-US fundamentals/financial-statement sections can still be simplified later where provider-backed fundamentals are unavailable.
  - Richer global filings/reports and data-source attribution can wait for a later data/source phase.
- Learning check: no new correction/error entry needed. Existing correction `CORR-20260420-1850` already covers Philippe saying a result is good enough: capture the stop point and stop cleanly instead of continuing to optimize.
## Lab Trading yfinance fundamentals follow-up
- Philippe reported ASRNL.AS still showed no visible data after `d7a3580`, and noted the app should prefer the Python `yfinance` bridge over scraped Yahoo Finance paths for fundamentals to avoid scrape blockers.
- Investigation found the first implementation wired the pipeline but mapped statement rows using pretty labels such as `Total Revenue`; `yfinance.get_*` statement calls return compact labels such as `TotalRevenue`, so the fundamentals block had info/ratios but empty statement-derived sections.
- Fixed the bridge to normalize statement labels, add insurance/EBIT/total-debt fallbacks, choose latest fiscal dates correctly, and format yfinance financial values with the reported currency instead of `$`.
- Committed fix as `9fdec04 fix(trading-lab): map yfinance compact statement labels`.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, `npm run build`, Lab restart/health.
- Live ASRNL.AS check after cache clear now shows yfinance-derived sections: Revenue EUR 13.1B, Net Income EUR 0.5B, Operating Income EUR 6.4B, Free Cash Flow -EUR 0.6B, Cash EUR 2.7B, Debt EUR 8.9B, Balance Sheet assets EUR 142B/liabilities EUR 132B/equity EUR 10.1B, and Financial Overview bars from yfinance statements.
- Caveat: source labels still display as “Yahoo / yfinance” because the existing source enum has `yahoo`; future cleanup could split a first-class `yfinance` source name if useful.
- Added deferred backlog item for global official filings adapters, gated behind yfinance verification; priority captured as Europe ESEF/filings.xbrl.org, Korea OpenDART, Japan EDINET, Taiwan MOPS/TWSE, Brazil CVM, then South Africa only after public CIPC/JSE iXBRL retrieval is confirmed.


## Pre-compaction carry-forward: Lab Trading yfinance/global filings [17:38 GMT+8]
- Current validated Lab Trading fundamentals state: ASRNL.AS now shows yfinance-derived financial sections after commit `9fdec04 fix(trading-lab): map yfinance compact statement labels`; Lab restart/health passed and live cache/page validation showed EUR-denominated highlights, cash/debt, balance sheet KPIs, and financial overview bars.
- Root cause fixed: yfinance statement calls returned compact labels like `TotalRevenue`; the bridge now normalizes row labels, includes insurance/EBIT/debt fallbacks, selects latest fiscal periods correctly, and formats values using the ticker currency instead of hardcoded `$`.
- Source-label caveat remains: UI/contract still uses source enum `yahoo`, with display label `Yahoo / yfinance`; future cleanup can add first-class `yfinance` source metadata if useful.
- Top-level `sourceMap.financials` may still show EODHD missing even when individual sections are yfinance-backed; consider a later source-map cleanup so provider provenance is less confusing.
- Deferred task added: `trading-implement-global-official-filings-adapters`, gated behind yfinance verification. Priority order captured as Europe ESEF/filings.xbrl.org, Korea OpenDART, Japan EDINET, Taiwan MOPS/TWSE, Brazil CVM; South Africa CIPC/JSE remains candidate-only until public iXBRL retrieval is proven.
- `AUTONOMOUS.md` mirror changes were committed in `fdf4991 Add global filings adapter backlog task`; note that commit included broader task-board mirror sync changes, not only the new global-filings item.

## Lab Trading financial visual sections polish [17:58 GMT+8]
- Philippe reported that the new yfinance fundamentals data was present but not filling the intended visual modules correctly.
- Screenshots showed two main UI regressions: Financial Highlights had become number-first cards with noisy empty states instead of chart-first KPI tiles, and Balance Sheet had drifted away from the clean Assets / Liabilities / Equity composition by adding extra KPI cells while keeping the old three-series chart.
- Fixed in Lab only, committed as `832dcb9 Polish Lab ticker financial visual sections`.
- Financial Highlights now fills more slots from available yfinance data: Revenue, Net Income, Gross Margin, Operating Margin, EPS, Free Cash Flow, ROE, Debt / Equity, and Current Ratio where available.
- The yfinance bridge now emits recent trend arrays for annual statements and derived ratios so KPI cells can render compact sparklines/mini area charts instead of “Data unavailable” boxes.
- Empty sparkline states were visually quieted and no longer print loud `DATA UNAVAILABLE` text inside populated cards.
- Balance Sheet was simplified back to the cleaner three-KPI model: Assets, Liabilities, Equity. The messy `2025-12-31 FY · yfinance annual statements` title was replaced by `Assets, liabilities, equity`; source provenance remains in the footer.
- Removed the redundant cash/debt mini-chart from the Balance Sheet visual area; cash/debt remains in its own section.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, `npm run build`, Lab restart/health after known stale-port race settled, and live ASRNL.AS HTML/cache checks.
- Live ASRNL.AS checks confirmed: no `Data unavailable` text in the financial highlight area, 16 `trading-small-sparkline` SVG markers rendered, the expanded highlight set appears, and Balance Sheet no longer shows cash/debt captions in that section.

## Lab Trading final financial-section trim before filings [18:38 GMT+8]
- Philippe approved the improved yfinance-backed financial visuals and requested final small trims before moving to non-US filings/reports.
- Implemented in Lab only and committed as `2ad3178 Polish Lab ticker financial section trim`.
- Company Profile section no longer shows Sector, Industry, or Currency; Sector/Industry also removed from the top header stat strip to avoid duplicate page metadata.
- Financial Highlights section now shows a compact section-level `Source: yfinance` instead of per-block source clutter / stale `SOURCE: EODHD` wording.
- Financial Highlight sparklines now carry subtle first/last year labels via `trendYears`, e.g. 2022–2025, so users can understand the time window.
- Unavailable Financial Highlight blocks now only show the title and `Unavailable`; removed provider-required and long explanatory text from cards such as ROIC.
- Desktop Financial Highlights grid changed to four columns, producing a 4-4-2 layout for the current 10 slots; tablet falls back to two columns and mobile remains compact.
- Key Ratios (TTM) now filters out ratios already visualized in Financial Highlights, including Return on Equity → ROE mapping, reducing duplicate rows.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, `npm run build`, Lab health, and live ASRNL.AS HTML checks. Known unrelated build warnings remain the middleware/proxy deprecation, dynamic task runner trace warning, and next.config NFT trace warning.
- Live ASRNL.AS checks confirmed: `Source: yfinance` present, year labels rendered, ROIC has no provider-required/awaiting-provider text, profile/header no longer render Sector/Industry/Currency rows, and duplicate Return on Equity is absent from Key Ratios while Current Ratio / Debt-Equity remain in Financial Highlights.

## Lab Trading financial highlight cleanup correction [18:57 GMT+8]
- Philippe clarified that Sector/Industry should remain in the ticker top header stat strip, but stay removed from the Company Profile section. Restored Sector/Industry in the header while keeping Company Profile filtered.
- Removed remaining per-card Financial Highlights detail text such as `2025-12-31 FY from yfinance annual statements.` and `TTM ratio from yfinance fundamentals.`; the section-level `Source: yfinance` remains sufficient.
- Adjusted sparkline wrapper sizing so the year labels align to the same width as the mini chart instead of sitting at the full card edges while the chart is centered/narrower.
- Fixed yfinance currency formatting for negative values from `-EUR 0.6B` to `EUR -0.6B`.
- Committed as `bdb072b Refine Lab financial highlight cleanup`.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, `npm run build`, Lab restart/health, and live ASRNL.AS HTML checks. Live checks confirmed header Sector/Industry present, Company Profile Sector/Industry/Currency absent, per-card metadata text absent, `EUR -0.6B` present, and `-EUR 0.6B` absent.
