# 2026-04-06 — Daily Memory

<!-- prior-day context: 2026-04-05 — Mission Control Agents Phases 1-7 (identity, activation, workspace wiring), ClawHub skill resolution, sidebar redesign plan, deferred ideas audit -->

## Nightly Memory Extraction (01:00 GMT+8 / 2026-04-06)

### Market Signal Generation (cron, 00:47 and 01:47 GMT+8)

**Run 1 (00:47 GMT+8):**
- signal_generator.py: 19 signals, all HIGH priority
- Shadow A/B: baseline=19, enriched=22, overlap=19, enriched_only=3
- reasoning_engine.py: 11 STRONG BUY, 8 BUY, 0 HOLD
- Top theme: Private credit sector stress (p018, 86/100 STRONG BUY — Jan 2023 JPM-triggered regional bank contagion pattern)
- Secondary: US hiring recession (78/100), geopolitical arrest warrant (78/100)

**Run 2 (01:47 GMT+8):**
- signal_generator.py: 15 signals, 13 HIGH / 2 MEDIUM
- Shadow A/B: baseline=15, enriched=15, overlap=15, enriched_only=0
- reasoning_engine.py: 9 STRONG BUY, 4 BUY, 2 HOLD
- Top: "Unprecedented" Withdrawal Requests Now Hitting Private Credit (ZeroHedge, 87/100 STRONG BUY)

**Pattern observed:** Private credit stress continues as dominant recurring signal cluster across both Apr 5 runs. Historical anchor: Jan 2023 JPM miss → credit spread widening → regional bank contagion.

**Durable facts extracted (→ market-intel-facts.json):**
- mi-031: Private credit sector stress dominant theme, Apr 5, 86-87/100 STRONG BUY
- mi-032: Shadow A/B enrichment pipeline live; run 1 found 3 enriched-only signals, run 2 found 0
- mi-033: Multi-agent strategy artifact at projects/multi-agent-strategy/docs/multi-agent-strategy.md (carried from Apr 4 handoff)

### Skill Level Check (cron, 08:00 GMT+8 / 00:00 UTC)

5 skills assessed on Apr 5 (Sunday weekly):
- Unreal (beginner): 0.0% — challenge mode, no artifacts found
- Japanese (novice): 45.0% — test mode, passed
- Python (novice): 65.0% — test mode, passed
- Blender (beginner-intermediate): 0.0% — challenge mode, no artifacts found
- After Effects (beginner): 0.0% — challenge mode, no artifacts found

**Pattern:** Unreal, Blender, After Effects consistently score 0% in challenge mode (no prior artifacts exist). Japanese improving. Python at 65% novice level.

### Daily Task Generator (cron, 09:00 GMT+8 / 01:00 UTC)

- 4 tasks generated across Career, Trading, Other, Personal
- GitHub sync: kanban board committed and pushed
- Telegram notification sent to goal-tasks (`-1003704803669`)
- Token lookup note: initial attempt failed (`.telegram-bot-token` not in workspace root); resolved by reading token from `/data/.openclaw/openclaw.json`

### Mission Control Autonomous Work (Apr 5 afternoon/evening)

All captured in depth in 2026-04-05.md above. Key artifacts from Apr 5:
- `projects/mission-control/docs/sidebar-redesign-plan.md` — collapsible sidebar plan with Lucide icon proposal
- `projects/multi-agent-strategy/docs/multi-agent-strategy.md` — strategy research output
- `uploads/mission-control/{trading,sportsbet-advisor,language-learning}/` — ClawHub skill sources uploaded manually
- `agent-workspaces/{dev-team-lead,content-seo-team-lead,sportsbet-advisor,trading-advisor,language-tutor}/` — durable seat identities wired

**Durable facts extracted (→ philippe/items.json):**
- philippe-019: Durable agent identities locked (Sudo, Vantage, Johan, Milou, Japin)
- philippe-020: ClawHub sources resolved via manual upload after rate limiting
- philippe-021: Future team additions planned (Editor for Vantage, Integrator for Sudo)
- philippe-022: Sidebar redesign plan created

## Prior Day Review
<!-- prior-day context: 2026-04-05 -->

## workspace-home-improvement [09:55]
**What changed:** Deprecated `projects/_ops/daily-check.html` and `daily-check-readme.md` with a visible banner and HTML comment.
**Why:** The dashboard fetched from the obsolete `autonomous-kanban/board.json` endpoint which is no longer the authoritative task store. It was silently broken — Mission Control Tasks is the current canonical view.
**Expected benefit:** No more confusion from a broken dashboard that appears to show workspace status. Users directed to the real tool.
**Rollback:** `git revert` the commit; remove the HTML comment and banner from the two files.

## Morning Meeting Decision [11:53]
- Report: nightly-security-review
- Finding: SEC-001 / Codex OAuth token expires 2026-04-08
- Decision: Adjusted — accepted as outdated, removed from token manifest
- Problem: Token tracking was outdated; Philippe manually switches Codex accounts on a multi-day cycle
- Risk: None — CLI/runtime Codex auth is managed manually, not via this manifest entry
- Action taken: Removed `openai-codex:default` entry from `config/token-manifest.json`; git commit `9b1f624`
- Verification: `cat config/token-manifest.json` confirms entry removed; remaining tokens intact
- Rollback: `git revert 9b1f624`
- Notes: Security review suppression baseline for this finding should now suppress it permanently since the tracking entry no longer exists

## Morning Meeting Decision [12:02]
- Report: self-improvement
- Decisions:
  - LOW-001: Verify + close — all 3 active corrections resolved; git commit `50c0070`
    - CORR-20260401-1028: resolved, closed 2026-04-06
    - CORR-20260324-0005: resolved, closed 2026-04-06
    - CORR-20260323-2255: resolved, closed 2026-04-06
  - INFO-001: Fix TOOLS.md listing — `news-feeds`, `proactive-execution`, `deepresearchwork` added to clawhub skills section; git commit `50c0070`
  - INFO-002: Deferred — agent-team package investigation postponed

## Mission Control Skills page pass (13:09–13:15 GMT+8)

**Context:** Philippe asked for one tight Skills-page pass to fix lost manual tags/hidden items and improve long-description handling without touching proactive-execution team-lead wiring yet.

**What changed:**
- Added real workspace-backed Skills preferences API at `projects/mission-control/app/api/skills/preferences/route.ts`
- Skills UI state now persists to `projects/mission-control/data/skills-ui-state.json` instead of living only in browser localStorage
- Kept localStorage as a migration/fallback path so existing browser-only tags/hidden state can seed the new server-backed store when remote state is empty
- Added a `Read more` / `Close summary` flow to long-description skill cards in `components/pages/SkillsWorkspaceClient.tsx`
- Full summaries now open in a small floating pop card anchored over the selected skill card
- Pop card supports close button, outside click, Escape key, and scrolls when summary text is taller than the card body
- Added lightweight "saving preferences…" feedback in the Skills header while tags/hidden state is being written

**Verification:**
- `npm run build` passed in `projects/mission-control`
- `./scripts/preview-restart.sh` completed successfully
- `curl http://127.0.0.1:3005/api/skills/preferences` returned the expected default JSON payload
- Nested Mission Control repo commit: `b0e214b0` (`skills: persist preferences and add summary popovers`)

**Expected benefit:**
- Manual tags and hidden items now survive reloads/restarts as real workspace state
- Long skill summaries are readable without blowing up card layout or forcing a full modal

## Mission Control Skills follow-up fix (13:22–13:26 GMT+8)

**Context:** Philippe reported the new `Read more` control only worked after manually expanding the skill card first.

**Root cause:** The trigger lived inside the collapsed `<summary>` region of a `<details>` card, so the interaction was unreliable while the card was still closed.

**Fix applied:**
- Moved the `Read more` / `Close summary` trigger out of the `<summary>` block and into the card body area directly below the collapsed preview text
- Kept the same floating summary pop card behavior, but made the trigger work whether the details card is closed or expanded

**Verification:**
- `npm run build` passed in `projects/mission-control`
- `./scripts/preview-restart.sh` completed successfully
- Nested Mission Control repo commit: `4f1c6870` (`skills: make read-more work on collapsed cards`)

## Mission Control savepoint + cleanup sweep (13:29–13:36 GMT+8)

**What changed:**
- Created comprehensive handoff savepoint: `projects/_ops/mc-savepoint-2026-04-06-afternoon.md`
- Added 2 durable Skills-page lessons to `.learnings/errors.md`:
  - localStorage-only Skills preferences were fake persistence
  - `Read more` control inside collapsed `<summary>` was unreliable
- Updated `MEMORY.md` with Apr 6 Mission Control Skills posture so future sessions know:
  - preferences are now server/file-backed via `/api/skills/preferences`
  - canonical storage is `projects/mission-control/data/skills-ui-state.json`
  - localStorage is migration/cache only
  - long descriptions use contextual pop cards
  - important controls should not live inside collapsed `<details>/<summary>` regions

**Why:** Philippe wanted a strong handoff for a future session/new agent and a clean end-of-session sweep so nothing from today gets lost or needs re-explaining.

**Expected benefit:** Future continuation should be able to resume from the real current state without re-diagnosing today’s fixes or losing small but important interaction lessons.

## Mission Control Agents / Sudo runtime work (14:00–15:20 GMT+8)

**Context:** Philippe resumed Mission Control and explicitly wanted to move past seat-flavored roleplay toward a truthful dev-team operating model: Marvin supervises, Sudo manages, FE/BE/QA execute with seat-specific models. He also asked to inspect the Nerve repo for reusable patterns first.

### Orientation + review
- Re-read Mission Control surface docs and runbooks, ending on savepoints:
  - `projects/_ops/mc-savepoint-2026-04-05-night.md`
  - `projects/_ops/mc-savepoint-2026-04-06-afternoon.md`
- Confirmed current active frontier: Agents page polish first, then Sudo/dev-team realism.

### Marvin oversight polish
- Reduced the visual weight/height of Marvin’s right-side oversight panel in `projects/mission-control/components/agents/AgentSeatCard.tsx`
- Goal: stop the panel reading like a full-height mini dashboard
- Verification: `npm run build` passed, preview restarted cleanly
- Nested Mission Control repo commit: `bfc2a778` (`agents: compact marvin oversight panel`)

### Nerve repo findings for dev-team routing
- Inspected `/tmp/openclaw-nerve`
- Key reusable concepts found:
  - per-run `model` and `thinking` settings are real
  - task/run execution is tracked as child sessions
  - agent-specific workspace separation is real
- Key missing concept: Nerve does **not** appear to provide the desired Marvin-supervises / Sudo-manages-team hierarchy by itself
- Durable product conclusion: borrow Nerve’s run/model/session-tracking patterns, but build the Sudo managerial layer ourselves

### Codex model-guidance foundation
- Philippe shared the OpenAI Codex Prompting Guide and wanted a local model-guidance doc similar to `gpt-5.4.md`
- Added `model-guidance/codex.md`, adapted from the Codex Prompting Guide / starter prompt for OpenClaw + workspace realities
- Updated `AGENTS.md` so `openai-codex/gpt-5.3-codex` (`codex`) explicitly reads that guidance before coding-heavy delegated work
- Workspace commit: `d7e5586` (`docs: add codex model guidance`)

### Mission Control Phase 1: truthful seat runtime defaults
- Built Phase 1 in Mission Control so seat activation carries real runtime metadata instead of just themed prompt text
- Added seat runtime metadata in `projects/mission-control/lib/agents/definitions.ts` and `lib/agents/chat-activation.ts`:
  - runtime mode (`Direct control`, `Lead route`, `Seat mode`)
  - default model
  - default thinking
  - supervisor / internal lane context
- Sudo now reads truthfully as a `lead route`
- Chat banner now shows runtime truth and gained an `Apply seat defaults` action
- Runbook updated to capture the new posture and note that `model-guidance/codex.md` is part of the dev-team routing foundation
- Verification: `npm run build` passed, preview restarted cleanly
- Commits:
  - nested Mission Control repo: `406e49a5` (`agents: add phase1 seat runtime defaults`)
  - workspace repo: `7c28cd4` (`docs: update mission control agents phase1 runtime brief`)

### Product decision after Phase 1
- Philippe rejected stopping at Phase 1 because it still felt like Marvin “putting on a Sudo-mask”
- Decision: skip faux testing and move straight to Phase 2, where Sudo delegates to FE/BE/QA through real child runs

### Mission Control Phase 2: real Sudo delegated lane runs
- Delegated heavy implementation to Codex inside `projects/mission-control`
- Built smallest honest Phase 2 slice:
  - API route: `projects/mission-control/app/api/agents/sudo-delegations/route.ts`
  - local run store: `projects/mission-control/data/sudo-delegations.json`
  - detached runner: `projects/mission-control/scripts/run-sudo-delegation.mjs`
  - lane definitions / defaults in `projects/mission-control/lib/agents/sudo-delegation.ts`
  - Sudo chat surface updated to expose delegation controls and truthful recent-run state
- Explicit lane defaults:
  - Frontend Developer → `codex`
  - Backend Developer → `codex`
  - QA Engineer → `minimax2.7`
- Truth posture of Phase 2:
  - Sudo still does **not** have its own backend runtime
  - FE/BE/QA delegated child runs are real and tracked under the main runtime boundary
  - UI now aims to show lane, requested model, run status, child session key, run id, and concise result/error text
- Docs added/updated:
  - `projects/mission-control/docs/sudo-delegation-phase2.md`
  - `projects/mission-control/README.md`
  - Sudo activation copy in `lib/agents/definitions.ts` and `lib/agents/chat-activation.ts`
- Verification reported by Codex: `npm run build` passed
- Nested Mission Control repo commit: `ca86b95a` (`Add Sudo delegated lane runs`)
- Codex completion notification failed because local OpenClaw gateway was unavailable (`gateway closed (1006 abnormal closure (no close frame))`), but this did not appear to invalidate the Mission Control build itself

### Immediate post-Phase 2 regression + likely fix
- After the Phase 2 merge, Philippe reported a client-side exception when navigating away from a page and back
- Likely cause identified: preview server had not been cleanly restarted after the in-place Phase 2 rebuild, causing route-switch mismatch/client exception behavior
- Action taken: clean `./scripts/preview-restart.sh` rerun in `projects/mission-control`
- Verification: rebuild passed again and preview restarted cleanly
- Follow-up requested from Philippe: hard refresh + re-test navigation; if crash persists, treat as real client regression in the new chat-side Phase 2 layer

### Durable product/runtime truths clarified today
- `Activate in Chat` for Sudo should not be described as a separate Sudo runtime unless that backend runtime actually exists
- Phase 1 alone is not enough for a meaningful dev-team test if the actual goal is “Sudo manages FE/BE/QA”
- The first meaningful dev-team test requires real delegation plumbing, not just seat-flavored prompt framing
- Codex guidance is now part of the intended dev-team execution foundation, not a side note

## Mission Control Sudo runtime/testing progression (15:30–17:48 GMT+8)

**Context:** Continued live Mission Control build/testing loop after Phase 2/3 work. Philippe wanted Sudo to feel native in Chat, then wanted the full Phase 3 stack completed before serious workflow testing, then immediately surfaced the first real runtime bug when Sudo incorrectly echoed Marvin-side content.

### Chat seat-selector cleanup and native Sudo seat UX
- Replaced old placeholder Chat agent menu with a real seat selector (`Marvin`, `Sudo`, `Vantage`) in the top control area
- Removed starter-prompt auto-injection as the primary seat activation path
- Reduced/retired the large overlapping seat-activation banner in favor of a compact seat-aware treatment in the top controls
- Kept Sudo-specific orchestration/delegation UI only when the Sudo seat is active
- Codex implementation commit in nested Mission Control repo: `3ea91be0` (`Integrate chat seat selector`)
- Preview restarted and verified live after the pass

### Visual cleanup regressions from seat integration
- Philippe reported leftover/ghost UI artifacts after seat switches and especially when activating Sudo
- First fix:
  - removed the leftover seat-panel shell
  - folded seat state into the top control row
  - kept `Apply seat defaults` there
  - nested Mission Control repo commit: `4bc857e4` (`chat: remove leftover seat panel shell`)
- Second fix after screenshot review:
  - problem was not true z-index overlap but the Sudo delegation panel reading like a translucent layer behind the transcript
  - made Sudo panel/card surfaces opaque and separated it more clearly from transcript area
  - nested Mission Control repo commit: `f5c7c0a3` (`chat: separate sudo panel from transcript`)
- Result: Philippe confirmed Marvin ↔ Sudo switching looked materially cleaner after the second pass

### Dev-team autonomy gap clarified
- Philippe correctly pointed out that the current manual FE/BE/QA buttons were not the same as a “fully autonomous dev team with Sudo independently thinking and delegating on its own”
- Decision: build the full Phase 3 stack before treating workflow tests as meaningful

### Mission Control Phase 3 stack

#### Phase 3A
- Goal: first real autonomous Sudo slice
- Built parent Sudo orchestration records and structured decision modes:
  - `direct_answer`
  - `ask_question`
  - `propose_alternative`
  - `delegate`
- Preserved explicit question/alternative path so Sudo can stop and ask Philippe instead of blindly executing
- Nested Mission Control repo commit: `799e3433` (`Implement Phase 3 Sudo orchestration`)
- Preview restarted after build

#### Phase 3B
- Goal: improve sequencing, execution-plan clarity, and final synthesis
- Added better bounded lane sequencing and stronger synthesis fields/output
- Brought clearer execution-plan structure into the Sudo panel (lane order, rationale, expected outputs, validation intent, completion criteria)
- Preview restarted immediately after completion per Philippe’s instruction pattern
- Exact nested repo commit hash not captured in chat, but the Phase 3B implementation/build landed cleanly and preview was restarted successfully afterward

#### Phase 3C
- Goal: finish the stack with Marvin oversight / escalation / approval boundaries
- Added explicit oversight fields and operator-visible supervision boundaries:
  - blocker
  - conflict
  - approval-needed
  - elevated uncertainty/risk
- Sudo now stops auto-continuation when continuation is unsafe or needs approval/review
- Sudo panel can distinguish:
  - informative oversight
  - approval required
  - Marvin review required
- Nested Mission Control repo commit: `8ace132c` (`Implement Mission Control Phase 3C oversight`)
- Preview restarted and the full Phase 3 stack was declared live enough for real workflow testing

### First real workflow test exposed a core routing bug
- Philippe tested the Sudo seat with a real request:
  - `Sudo, improve the left sidebar, following this implementation plan: 'projects/mission-control/docs/sidebar-redesign-plan.md'.`
- Actual bad behavior:
  - Sudo did **not** take over the request
  - UI showed a partial replay/reuse of an earlier Marvin-side reply about Phase 3C
  - this made it look like Sudo had effectively copied part of an old Marvin response instead of starting a fresh orchestration run
- Important product/runtime conclusion:
  - this was not just “bad orchestration output”
  - the main composer send path was still **seat-blind** and continued routing Sudo-seat submits into the normal Marvin live runtime path

### Root-cause fix for Sudo seat submits
- Investigated `components/chat/MissionControlChatSurface.tsx`, runtime bridge flow, and Sudo panel/orchestration hooks
- Confirmed root cause:
  - selecting the Sudo seat changed seat UI/context, but pressing Enter/send still used the generic `live.sendPrompt(...)` path
  - Sudo orchestration only ran when explicitly triggered inside the Sudo panel, not on the normal composer submit path
- Fix implemented:
  - when active seat is `dev-team` (`Sudo`), composer submit no longer sends directly to Marvin chat
  - instead it hands the composer brief into the Sudo orchestration path first
  - added orchestration trigger wiring so the Sudo panel reacts to normal composer submits
  - adjusted send button behavior to be seat-aware for Sudo submits
- Verification:
  - build passed
  - preview restarted cleanly
- Nested Mission Control repo commit: `2a0c4231` (`chat: route sudo seat submits to orchestration`)

### Durable lessons / truths reinforced today
- A seat selector alone is not enough; composer submit paths must also become seat-aware or the runtime will silently fall back to Marvin behavior
- For Sudo, the distinction between “seat-themed UI” and “actual routing/orchestration” is easy to fake accidentally; real tests need to prove submit-path interception, parent orchestration creation, and truthful child-run state
- Screenshot-based UI debugging was especially effective for Mission Control chat-seat regressions today; the visual issue was often a container/surface interaction problem rather than the higher-level feature itself

## Mission Control Dev Team / Sudo breakthrough + cleanup (15:30–22:10 GMT+8)

**Context:** After Phase 2/3 Sudo work became real enough to test, Philippe pushed hard on two things: (1) stop accepting fake seat-theater and fix the actual runtime/orchestration path, and (2) once the workflow worked, clean up the Chat UI so it felt like an operating surface instead of a debug console.

### Seat selector / native Sudo chat posture
- Replaced the old placeholder Chat agent menu with a real seat selector for `Marvin`, `Sudo`, and `Vantage`
- Retired starter-prompt auto-injection as the primary activation path
- Compact seat-aware top control treatment replaced the bigger activation banner
- Nested Mission Control repo commit: `3ea91be0` (`Integrate chat seat selector`)

### Immediate visual regressions and fixes
- Seat integration caused ghost/leftover UI artifacts and then a second issue where the Sudo panel visually bled into the transcript
- Fixes applied:
  - `4bc857e4` — `chat: remove leftover seat panel shell`
  - `f5c7c0a3` — `chat: separate sudo panel from transcript`
- Durable UI lesson: in Mission Control Chat, FLOATING/translucent surfaces can make containment/hierarchy issues feel like z-index bugs; screenshots were especially effective for solving these fast

### Phase 3 stack completed
- **Phase 3A** (`799e3433`) — Sudo autonomous decision layer added with bounded modes:
  - `direct_answer`
  - `ask_question`
  - `propose_alternative`
  - `delegate`
- **Phase 3B** (`139dd600`) — improved sequencing, richer execution plan, stronger final synthesis
- **Phase 3C** (`8ace132c`) — Marvin oversight / escalation / approval boundaries
- By the end of 3C, the product truth became:
  - Sudo leads normal Dev Team execution
  - Marvin supervises/escalates/reviews when appropriate
  - FE / BE / QA are explicit execution lanes

### First real workflow testing revealed plumbing bugs, not concept failure
#### Bug 1 — Sudo seat submit path was still seat-blind
- Symptom: a Sudo task request replayed/echoed old Marvin-side content instead of starting a fresh Sudo orchestration run
- Root cause: normal composer submit still used the generic live Marvin path even when Sudo seat was selected
- Fix: route Sudo-seat composer submits into Sudo orchestration first
- Commit: `2a0c4231` — `chat: route sudo seat submits to orchestration`
- Durable lesson: seat-aware UI without seat-aware submit routing is fake autonomy

#### Bug 2 — brittle model-override acknowledgement checks
- Symptom: repeated blocker `Sudo orchestration model override was not acknowledged by runtime. Requested codex5.4.`
- First patch widened alias matching and added retry/readback window:
  - `c7fa75c5` — `sudo: harden model override acknowledgement`
- Deeper investigation showed the more fundamental bug:
  - orchestration and delegated runners were still trying to switch models by sending `/model ...` chat commands into synthetic sessions
  - this is the wrong primitive for synthetic/autonomous runs
- Real fix: refactored Sudo orchestration and delegated lane runs so model choice is prepared as part of session setup rather than chatted in afterwards
- Commit: `3672cad4` — `Refactor sudo model selection to session setup`
- Durable lesson: for synthetic/autonomous Mission Control runs, model selection should be part of session/run setup, not a conversational afterthought

#### Bug 3 — decision parser grabbed the wrong JSON object
- Symptom: after getting past model setup, Sudo still failed with `did not return a supported decision mode`
- Root cause: the runner extracted the first `{...}` object it found, which could be the outer CLI wrapper rather than Sudo’s actual decision payload
- Fix: rank multiple JSON candidates and prefer the one that actually looks like a Sudo decision (`mode`, `lanePlan`, `lanePlanSteps`, `oversight`)
- Commit: `3b9a76c3` — `sudo: prefer decision json payload over wrapper`
- Durable lesson: for wrapped model output, “first JSON object wins” is too naive; schema-aware candidate scoring is safer

### First successful real Sudo implementation run
- Philippe asked Sudo to improve the left sidebar following `projects/mission-control/docs/sidebar-redesign-plan.md`
- This became the first successful end-to-end Sudo build
- Result:
  - real Sudo orchestration run
  - frontend-only lane plan
  - successful delegated FE run
  - real code landed and passed visual/use inspection
- Nested Mission Control repo commit produced by the run:
  - `abbfb1b7` — `mission-control: implement collapsible lucide sidebar redesign`
- Philippe’s verdict after preview restart: he liked it, functionality looked fine, no sidebar complaints
- Durable product conclusion: this was the first true proof that the Dev Team / Sudo workflow can take a scoped implementation plan, route it, execute it, and land something good enough to keep

### Sudo Chat cleanup pass after the first successful run
- Philippe and I aligned on a restraint-focused cleanup, not a redesign:
  1. compress Sudo header
  2. demote manual lane controls
  3. show only one active/current run by default
  4. hide/demote internal IDs
  5. collapse execution-plan detail after completion
  6. demote historical failed runs
- Philippe additionally wanted:
  - composer cleared after Sudo submit
  - dismiss/X controls for old Sudo decisions
  - a lightweight handoff button to review with Marvin after Sudo completes
- Cleanup pass implemented by Codex
- Nested Mission Control repo commit: `eeebb017` — `Polish Sudo chat flow`
- Philippe’s verdict after restart: “clean enough for now, much better than the previous version”

### Agent-team status clarified
- Late in the evening, Philippe asked whether `projects/_ops/agent-team/` is still worth keeping now that the Dev Team is real
- Verified folder contents: mostly prompts, contracts, docs, helper shell script; reads as governance/reference/history, not a live runnable software package
- Product conclusion:
  - old Hybrid Agent-Team v1 is largely superseded by the Dev Team / Sudo path
  - useful pieces may still be worth porting
  - full concept should likely be retired/archived to prevent narrative drift and Morning Meeting noise
- Philippe explicitly asked to set up a retirement pass and thanked Builder/Reviewer for their service
- Recommended next-doc step: update TOOLS/core docs so `projects/_ops/agent-team/` stops being framed as a live package
