# 2026-03-30

## Nightly Memory Extraction (02:00 ICT)

Extracted durable facts from March 29 work session:

### Mission Control
- **Transcript rehydration resolved**: Three pieces together fixed the "No live transcript yet..." issue — (1) route lookup against real sessions.json shape (top-level sessionKey map), (2) client hydration merge instead of hard transcript replacement, (3) automatic hydrate trigger when activeSessionKey becomes known
- **Composer + Chat workspace layout completed**: Fixed top rail + fixed composer + scrolling transcript between; AppShell made Chat-route-aware to hide bottom system strip on /general/chat and /chat only
- **OAuth refresh flow discovery**: Two separate auth stores exist — Codex CLI OAuth (~/.codex/auth.json) vs OpenClaw Runtime OAuth (auth-profiles.json); refreshing one does not update the other

### Durable facts saved to
- `/data/.openclaw/workspace/life/projects/mission-control.md` — updated with March 29 facts (transcript rehydration, composer layout, OAuth discovery)
- `/data/.openclaw/workspace/life/projects/marvin-config.md` — updated with OAuth refresh flow discovery

---

## Notes

- Extraction run completed at 02:00 ICT (March 30)
- March 29 session was intensive Mission Control Chat completion work
- No new project launches or relationship changes to record
## Workspace Home-Improvement [09:35 GMT+8]
**Target:** projects/_ops/PROACTIVE-RUNBOOK.md
**What changed:** Added a status header clarifying that `AUTONOMY.md` is the canonical proactive-execution policy and `projects/_ops/PROACTIVE-RUNBOOK.md` is a supporting runbook/reference.
**Why:** The file is not the primary policy source anymore, but it still contains useful supporting context. Marking it as fully deprecated overstated the situation and risked avoidable policy confusion.
**Expected benefit:** Clearer source-of-truth hierarchy without implying the runbook is obsolete or unsafe to reference.
**Rollback:** Edit or remove the header block in `projects/_ops/PROACTIVE-RUNBOOK.md`.

## Morning Meeting Decision [10:39 GMT+8]
- Report: self-improvement
- Finding: token-expiry review / manifest drift
- Decision: Adjusted
- Problem: `config/token-manifest.json` created a false-positive expired-token finding for `openai-codex:default` because the manifest had stale dates even though live runtime auth was refreshed and healthy.
- Risk: misleading overnight auth warnings and avoidable operator confusion
- Action taken: updated `config/token-manifest.json` to match the live Codex runtime auth state; recorded accepted-risk posture for lower-priority tokens Philippe does not want to rotate now.
- Verification: live runtime auth store `/data/.openclaw/agents/main/agent/auth-profiles.json` showed `openai-codex:default` last used at 2026-03-30 10:34 GMT+8 with live expiry 2026-04-08 13:17 GMT+8; manifest updated to align.
- Rollback: revert `config/token-manifest.json` if needed.

## Morning Meeting Decision [10:40 GMT+8]
- Report: platform-health-council
- Finding: cron timeout alignment / stale wrapper metadata
- Decision: Adjusted
- Problem: overnight review inferred possible timeout mismatch for `signal-accuracy-review` and `daily-task-generator` from old run metadata.
- Risk: misleading cron-health noise and unnecessary operator concern, not a live runtime failure.
- Action taken: verified live cron job definitions directly.
- Verification: `cron list` showed `signal-accuracy-review` already at `timeoutSeconds: 900` and `daily-task-generator` already at `timeoutSeconds: 300`; `daily-task-generator` also ran successfully today.
- Rollback: n/a

## Morning Meeting Decision [10:44 GMT+8]
- Report: self-improvement
- Finding: `skill-level-check` cron documentation mismatch in `TOOLS.md`
- Decision: Approved
- Problem: `TOOLS.md` listed `skill-level-check` under the disabled legacy wrapper section even though the live cron job is enabled and active.
- Risk: low, but confusing for future audits and operator review.
- Action taken: moved `skill-level-check` into the active cron table and removed it from the disabled-legacy list.
- Verification: `cron list` shows `skill-level-check` enabled with Sunday 07:00 ICT schedule; `TOOLS.md` now matches that live state.
- Rollback: revert the `TOOLS.md` edit.

## Autonomous Queue Execution [11:18 GMT+8]
**Task:** [Personal] 🎁 Surprise MVP: build a tiny learning helper for Japanese, Python, or business-analysis practice, such as a prompt generator, drill picker, or review tracker
**What changed:** Added `projects/learning-tools/python-daily-loop.html`, a tiny no-setup Python daily practice tool with rotating prompts, auto-graded quiz, coding rep task, and local progress tracking.
**Why:** Gives Philippe a repeatable low-friction daily exercise loop he can use immediately without setup.
**Expected benefit:** Better consistency for Python skill practice through a small browser-based routine instead of ad-hoc study decisions.
**Verification:** Builder created the artifact and Reviewer passed after checking the real file, the daily-loop structure, date rotation, and absence of placeholder content.
**Rollback:** Remove `projects/learning-tools/python-daily-loop.html` and revert queue/task-log entries if needed.


## Mission Control Chat Fix Pass [13:34 GMT+8]
- Re-anchored on Mission Control context before new work: read Mar 29–30 daily memory, preview/runtime runbooks, live runtime-bridge code, local Nerve repo patterns, and the updated `projects/_ops/mission-control-comprehensive-savepoint-2026-03-29-afternoon.md` as the main wiring truth.
- Confirmed `Stop` had been a placeholder, then wired it through the real Mission Control bridge using `chat.abort` and updated bridge capability metadata accordingly.
- Tightened Chat-page layout: reduced top-route padding, reduced sticky top inset, flattened second-row controls, and widened those pills/cells to save vertical space.
- Fixed preview deployment process issue: a plain `npm run build` was not enough because the old preview server kept serving stale behavior; `./scripts/preview-restart.sh` is required after Chat-page changes for the public preview to reflect new code.
- Fixed multiple Chat regressions in one pass:
  - refresh no longer rehydrates over a populated live transcript and rewind visible history to older persisted messages
  - effort label now prefers real thinking-level readback from the main/direct session or another visible session before falling back
  - markdown headings now parse through level 6 again, so lines like `#### 2` render as headings instead of raw hashtags
  - bottom-follow now uses a hybrid model: auto-follow only when already near bottom, otherwise show `Jump to latest ↓`
  - `Recent Sessions` count changed from a pill badge to a plain bold number to reduce cell height
- Mission Control preview rebuilt/restarted successfully after the latest Chat fixes.

## Mission Control Chat Cleanup + Bridge Diagnosis [14:50 GMT+8]
- Diagnosed the suspected Telegram-channel bridge failure more precisely: real gateway/bridge repros showed `chat.send` to `agent:main:telegram:direct:8471960624` succeeds cleanly through the full browser-style Mission Control path (preview proxy → WS sidecar → gateway), so the root issue was not “Telegram send kills the bridge”.
- Narrowed the Stop-button bug to UI lifecycle handling rather than transport failure: `chat.abort` on a live streaming run leaves the socket healthy, but Mission Control had been treating `chat.state === "aborted"` as a fatal error and rendering sticky red bridge/composer warnings until the next healthy message cycle cleared them.
- Fixed abort-state handling in `projects/mission-control/hooks/useRuntimeBridge.ts`: abort is now treated as a neutral stopped state, clearing sticky error state instead of creating fake `BRIDGE NOTE / ERROR / Chat run aborted.` messages.
- Cleaned the Chat-page body by removing pre-bridge scaffold content from the visible transcript area: fake operator prompt, operator summary, boundary handoff note, and demo diff/file/chart rails were removed from layout so the page now resolves to top control section → Live bridge session → Composer.
- Preserved the broader artifact concept intentionally: diff/file/chart rendering was removed from the current page scaffold, not rejected as a future feature direction.
- Visual polish updates during the same pass:
  - removed the redundant ivory transcript well so the chat sits directly on the page background
  - tightened transcript/composer spacing
  - changed Composer placeholder copy from `Send one real prompt to ...` to `Message to ...`
  - softened assistant bubble contrast twice, with the latest fill at `rgba(250, 246, 240, 0.94)`
- Important runtime/UI note: Mission Control effort readback is still using an inconsistent source-of-truth path. Live `/status` for the active session reports `Think: low`, but the session-summary JSON path Mission Control currently reads does not consistently expose `thinkingLevel`, causing the top strip to fall back to `Last requested: ...`. This remains an explicit follow-up item.
- Operational reminder confirmed again: after Mission Control UI edits, the public preview needs full rebuild + `scripts/preview-restart.sh`; lint/build alone is not enough to reflect changes in the live preview.

## Mission Control Chat Completion Pass [17:10 GMT+8]
- Created canonical handoff doc: `projects/_ops/mission-control-comprehensive-savepoint-2026-03-30-evening.md` to capture the full Mar 30 Chat-page session, including conceptual shifts, solved bugs, runtime discoveries, preview/deploy lessons, tool-lane implementation, upload flow, and the remaining reopen items.
- Major Chat outcomes beyond the earlier 14:50 note:
  - verified the live bridge already exposes structured tool events for `read`, `exec`, `write`, and `edit`, so no bridge rewrite was required before building process visibility
  - implemented a real Tools lane in the transcript with grouped tool cards, expandable rows, edit before/after rendering, write previews, and read/exec detail panels
  - fixed multiple tool-lane state bugs: disappearance after completion, stale `start` rows keeping groups alive, groups appearing already collapsed on first paint, and overly eager auto-close behavior
  - final tool-group behavior now keeps the newest Tools group open while older ones can collapse as newer groups appear
  - added browser-side session transition instrumentation for the recurring `WS Open` / `Session Connected` wobble; next evidence should be gathered from browser console logs tagged `[mission-control-runtime] session state`
  - added assistant bubble copy control, then fixed it with a clipboard fallback using hidden textarea + `execCommand('copy')`
  - converted Composer actions to icon controls (paperclip, mic placeholder, new-session, send)
  - added a real Composer upload path with paperclip picker, drag-and-drop, attachment chips, and workspace upload route `projects/mission-control/app/api/files/upload/route.ts`
- New upload behavior: files are written into `uploads/mission-control/` and the sent prompt appends the uploaded workspace-relative paths under `Attached files uploaded to workspace:` so Marvin can use them immediately.
- Philippe validated the upload path with a screenshot upload and confirmed it worked.
- End-of-session product posture: Chat is now effectively **done for now**; remaining future follow-ups are polish-level, not structural blockers.
- Highest-value reopen items remain:
  1. fix effort/thinking source-of-truth readback so the top strip shows confirmed live thinking level instead of falling back to `Last requested: ...`
  2. smarter multi-tool burst grouping so nearby sequential tool actions can roll into one Tools bubble instead of many `Used 1 tool` groups
  3. optional future polish on Composer action hierarchy, attachment chips, copy-button subtlety, and top-strip restraint

---

## Nightly Memory Extraction (02:00 ICT, March 31)

### Durable facts extracted from March 30

**Mission Control Chat completion:**
- Tools lane implemented with grouped tool cards for read/exec/write/edit events, expandable rows, before/after rendering for edits, write previews, read/exec detail panels
- Tool-lane state bugs fixed: group disappearance after completion, stale start rows, groups appearing collapsed on first paint, overly eager auto-close
- Chat regressions fixed: refresh behavior, effort label readback, markdown heading parsing (h1-h6), hybrid bottom-follow model, Recent Sessions count styling
- Composer upload path: paperclip picker, drag-and-drop, attachment chips, workspace upload route, files saved to `uploads/mission-control/` with paths appended to prompt
- Assistant bubble copy control with clipboard fallback
- Composer actions converted to icon controls (paperclip, mic, new-session, send)
- Chat declared "done for now" — remaining items are polish-level

**Durable facts saved to:**
- `/data/.openclaw/workspace/life/projects/mission-control.md` — updated with March 30 Chat completion facts (Tools lane, bugfixes, upload path, completion posture, reopen items, handoff doc)

### Extraction run completed
- No new project launches, relationship changes, or people/company facts to record
- March 30 was intensive Mission Control Chat completion work
