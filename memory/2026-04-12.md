# 2026-04-12

## Nightly Memory Extraction (02:00 KL / 18:00 UTC)
**Source:** agent:main:main cron session
**Session scope:** Apr 12 post-rollback recovery + safe Chat refactor lane

### Durable facts extracted
**Mission Control:**
- VPS rollback event: Philippe initiated a full VPS rollback on Apr 12 because recent Mission Control changes broke Home, Tasks, and Agents. Rollback state is now the canonical safe baseline.
- Git baseline reset: curated commit `4ddd4ed` force-pushed to `origin/master` to align GitHub with the rolled-back workspace.
- Chat extraction refactors landed (3 slices, all verified and committed):
  - `chat-rich-text.tsx` — rich text + workspace file-link helpers (commit `4ac9d7a6`)
  - `chat-ui-helpers.ts` — shared UI style helpers (commit `8230f1e4`)
  - `chat-tool-groups.tsx` — tool-group rendering (commit `ec0065f0`)
- Tasks truth-flow documented in `projects/_ops/tasks-truth-flow-map-2026-04-12.md`
- Comprehensive Mission Control savepoint written to `projects/_ops/mc-savepoint-2026-04-12-late-night.md`
- Apr 11 durable facts confirmed still current: Johan + Milou specialist seats, Vantage lead seat, Link job-advisor, custom news digest with FD/NRC/IEX

**Operational posture established:**
- Post-rollback: avoid bundled multi-page changes, one narrow change at a time
- Tasks treated as state-coordination problem, not simple UI refactor
- Chat refactor lane: build-verify-commit cycle before next slice

### Entity updates
- No new entity mutations: life/entity directories are empty; no entity files to update
- accessCount bumps deferred until entity structure is populated

### No Morning Meeting held today.

---

## Post-rollback Mission Control baseline (00:13-00:49 KL)
- Philippe reported a VPS rollback because recent Mission Control changes had messed up Home, Tasks, and Agents; new instruction is to treat the rollback state as the fresh safe baseline and be very careful with future changes.
- Rebuilt/restarted Mission Control preview from the rolled-back workspace and verified key routes responded again (`/general/home`, `/general/tasks`, `/general/agents`), after which Philippe confirmed the current workspace status was clean and working as intended.
- Important product-safety posture after rollback: avoid bundled multi-page changes, prefer one narrow change at a time, and treat Tasks as state-sensitive rather than a simple UI page.

## Git baseline reset to rolled-back workspace (00:25-00:26 KL)
- Created curated local baseline commit `4ddd4ed` (`Capture rolled-back workspace baseline`) from the current rolled-back workspace state.
- Force-pushed the curated baseline to `origin/master` after refreshing remote state, so GitHub now matches the clean rolled-back workspace baseline.
- Deliberately excluded obvious runtime/generated noise from the baseline commit (preview pid/log churn, trading/runtime churn, avatar deletions/additions).

## Tasks truth-flow findings captured (00:30-00:36 KL)
- Wrote `projects/_ops/mission-control-post-rollback-baseline-2026-04-12.md` and `projects/_ops/tasks-truth-flow-map-2026-04-12.md`, committed as `91bc090`.
- Durable Tasks architecture reminder: current-state authority is `projects/mission-control/data/autonomous-tasks.json`; `AUTONOMOUS.md` is still an active legacy mirror/import/reconciliation surface; `memory/tasks-log.md` is append-only history, not a live board lane.
- UI semantics to remember: `Pull from md` imports missing active legacy tasks from `AUTONOMOUS.md` into the structured store; `Apply board truth` reconciles `AUTONOMOUS.md` sections from the structured store.
- Practical rule: after rollback, treat Tasks as a state-coordination problem first and avoid mixing UI refactor with sync-logic changes in one pass.

## Safe Chat-only refactor lane started (00:37-00:57 KL)
- Following the rollback guidance, shifted active implementation work away from Home/Tasks/Agents and into Chat-only structural refactors.
- Completed first safe Chat slice: extracted rich-text + workspace file-link helpers from `MissionControlChatSurface.tsx` into `projects/mission-control/components/chat/chat-rich-text.tsx`; verified with clean build and preview restart; committed as `4ac9d7a6` (`Extract chat rich text helpers`).
- Completed second safe Chat slice: extracted shared UI style helpers into `projects/mission-control/components/chat/chat-ui-helpers.ts`; verified with clean build and preview restart; committed as `8230f1e4` (`Extract chat UI helpers`).
- Completed third safe Chat slice: extracted tool-group rendering into `projects/mission-control/components/chat/chat-tool-groups.tsx`; verified with clean build and preview restart; committed as `ec0065f0` (`Extract chat tool group rendering`).
- Two transient refactor failures were caught and fixed immediately by build verification: one unused-import lint failure after helper extraction, and one duplicate `formatEventTime` definition after tool-group extraction.
- Chat refactor rule that worked well: smallest possible extraction, behavior-preserving intent, build proof, preview restart, then commit before continuing.

## End-of-night handoff artifacts (00:59+ KL)
- Wrote comprehensive savepoint: `projects/_ops/mc-savepoint-2026-04-12-late-night.md`.
- Savepoint includes rollback context, git reset reasoning, Tasks truth-flow findings, all landed Chat slices, next recommended slice, and the exact operational posture for tomorrow.

## workspace-home-improvement (09:57)

**What:** Added per-connection lifecycle logging to `runtime-bridge-ws-sidecar.js` and `preview-origin-proxy.js` — open/close events now emit a short random client ID and current total client count.

**Why:** Both scripts had minimal logging at the connection level, making it hard to diagnose reconnect wobble, concurrent session interference, or sidecar/proxy handshake issues during runtime. Connection-level events were completely dark.

**Expected benefit:** When issues occur, log output now makes it possible to distinguish individual connections, track upstream vs browser closes, and count active clients at any point — without needing to add more instrumentation.

**Rollback:** `git revert bdefbfd`

## Morning Meeting follow-up (11:18)
- Approved and applied a minimal control-plane fix to `market-signal-generator`.
- Change: added `tz: Asia/Ho_Chi_Minh` to the cron schedule while keeping `45 * * * 1-5` and existing stagger.
- Why: the job was being evaluated in server/runtime timezone because `tz` was missing, which made a weekday-only job appear to drift into Sunday-night Ho Chi Minh time.
- Verification: next run now resolves to Monday 00:47 Asia/Ho_Chi_Minh (stagger preserved), which matches weekday semantics in Philippe-facing time.
- Risk/rollback: low risk; rollback is removing the added `tz` field from that one job.

## Morning Meeting follow-up (11:20)
- Ran one bounded docs reconciliation pass for the TOOLS.md vs MEMORY.md vs daily-notes overlap.
- Change: added a short `What goes where` section at the top of `TOOLS.md` defining the intended split:
  - `TOOLS.md` = live operational reality
  - `MEMORY.md` = durable decisions and standing rules
  - `memory/YYYY-MM-DD.md` = chronology, approvals, investigations, and one-off execution notes
- Why: AGENTS already had the right separation, but TOOLS/MEMORY still invited small duplication and overnight-review noise.
- Expected benefit: clearer promotion rules, less re-raising of already-handled items, and less drift about where operator truth should live.
- Rollback: revert commit created for this docs change.

## Morning Meeting follow-up (11:22)
- Updated `TOOLS.md` infrastructure note from `Linux 6.8.0-106-generic` to `Linux 6.8.0-107-generic`.
- Why: `TOOLS.md` is meant to reflect live runtime facts; `uname -r` verified the current kernel is `6.8.0-107-generic`.
- Expected benefit: keeps the runtime-facts file accurate and removes a small but real doc drift.
- Rollback: revert commit created for this one-line docs correction.

## Morning Meeting follow-up (11:23)
- Applied the approved batch of remaining tiny doc cleanups.
- Changes:
  - added recent Mission Control savepoint refs to `TOOLS.md`
  - added current Chat helper component inventory note to `TOOLS.md`
  - added `memory/self-improvement/` to `TOOLS.md` deeper references
  - added environment-safety note that installed OpenClaw runtime hotfixes under `/usr/local/lib/node_modules/openclaw/dist/` require host/container-namespace access
  - removed the stray trailing JSON artifact from `model-guidance/minimax-m2.7.md`
- Why: these were verified low-risk documentation gaps/noise points from the self-improvement review.
- Expected benefit: better operator recall, less overnight re-raising, and cleaner model-guidance files.
- Rollback: revert commit created for this cleanup batch.

## Memory / learnings sweep (11:25)
- Promoted the reusable Morning Meeting cron lesson into `MEMORY.md` as a durable rule: operator-facing or market-facing OpenClaw cron jobs should set explicit `tz` instead of relying on server timezone defaults.
- Logged the `market-signal-generator` triage miss in `.learnings/errors.md` so future cron-health investigations separate expression bugs, timezone drift, and true scheduler misfires.
- No new feature request or user-correction entry was needed from this Morning Meeting; the rest was already captured in daily memory and core docs.

## Safe Chat refactor lane follow-up (11:27)
- Continued the post-rollback Chat-only safe refactor lane from `projects/_ops/mc-savepoint-2026-04-12-late-night.md`.
- Change: extracted the message/event rendering cluster out of `projects/mission-control/components/chat/MissionControlChatSurface.tsx` into new module `projects/mission-control/components/chat/chat-message-blocks.tsx`.
- New module now owns: `assistantLabelForSeat`, `LiveMessageBlock`, `LiveEventBlock`, plus their tiny local helpers (`CopyIcon`, clipboard helper, `shortKey`).
- Why: this was the next recommended behavior-preserving structural slice after rich text, UI helpers, and tool-group rendering. It reduces Chat monolith size without touching runtime/session-control logic.
- Verification: `npm run build` passed after fixing three immediate extraction leftovers (`formatEventTime` import, `RuntimeBridgeLiveEvent` type import, `RuntimeBridgeChatMessage` type import).
- Preview verification: a fresh `preview-start.sh` hit an expected port-collision because preview services were already running, so route truth was verified against the live existing preview instead; `http://127.0.0.1:3005/general/agents` and `http://127.0.0.1:3005/general/chat` both returned 200.
- Rollback: revert the commit created for this extraction slice.

## Safe Chat refactor lane follow-up (11:35)
- Continued the Chat-only safe refactor lane with one bounded presentation-only cleanup pass.
- Change: extracted the transcript-adjacent UI cluster from `projects/mission-control/components/chat/MissionControlChatSurface.tsx` into new module `projects/mission-control/components/chat/chat-transcript-adjacent.tsx`.
- New module now owns: live bridge session header/pill block, recent bridge-events popover, transient system notices list, empty-transcript placeholder, jump-to-latest UI, and a tiny local `InfoIcon` helper.
- Why: this was the next safe cluster after message/event rendering, and Philippe approved combining it with a small top-level icon/helper cleanup in the same pass as long as the work stayed presentation-only.
- Boundaries preserved: state ownership, refs, transcript assembly, runtime bridge logic, session switching, send/abort behavior, and speech/attachment flows all stayed in the parent component.
- Verification: `npm run build` passed cleanly. Live route truth also checked against the existing preview: `http://127.0.0.1:3005/general/agents` and `http://127.0.0.1:3005/general/chat` both returned 200.
- Rollback: revert the commit created for this extraction slice.

## Safe Chat refactor lane follow-up (11:52)
- Continued the Chat-only safe refactor lane with composer extraction.
- Change: extracted the composer UI from `projects/mission-control/components/chat/MissionControlChatSurface.tsx` into new module `projects/mission-control/components/chat/chat-composer.tsx`.
- New module now owns: attachment chip rendering, drag/drop hint UI, composer textarea block, send/new-session/mic/attachment icon buttons, speech/composer status/error text, and the small composer icon set (`SendIcon`, `NewSessionIcon`, `MicIcon`, `PaperclipIcon`).
- Why: this was the next recommended safe structural slice from the late-night roadmap, and it removes another large presentational block without moving runtime/session-control ownership.
- Boundaries preserved: submit/send behavior, upload flow, speech hook state, new-session behavior, drag/drop handlers, and all parent state ownership stayed in `MissionControlChatSurface.tsx`.
- Verification: `npm run build` passed after fixing small extraction leftovers (unused local types/props, and using the real exported `SpeechToTextStatus` type instead of a narrowed local guess).
- Fresh preview verification: `bash scripts/preview-restart.sh` completed successfully; `http://127.0.0.1:3005/general/chat` and `http://127.0.0.1:3005/general/agents` both returned 200 after restart.
- Rollback: revert the commit created for this extraction slice.

## Safe Chat refactor lane follow-up (12:05)
- Continued the Chat-only safe refactor lane with session rail / recent sessions extraction.
- Change: extracted the recent-sessions dropdown UI from `projects/mission-control/components/chat/MissionControlChatSurface.tsx` into new module `projects/mission-control/components/chat/chat-session-rail.tsx`.
- New module now owns: recent-sessions trigger button, dropdown list rendering, session label formatting, age formatting, active-session badge state, and session metadata pills.
- Why: this was the remaining major safe structural Chat slice from the late-night roadmap, and it keeps the current Chat cleanup moving without reopening Tasks or runtime-hardening work yet.
- Boundaries preserved: actual session switching remains parent-wired via callback; parent still owns open-state, refs, bridge state, and routing/runtime control.
- Verification: `npm run build` passed cleanly. Fresh preview restart also completed; after one warm-up retry, `http://127.0.0.1:3005/general/chat` and `http://127.0.0.1:3005/general/agents` both returned 200.
- Rollback: revert the commit created for this extraction slice.

## Chat hardening follow-up (12:45)
- Before starting runtime hardening, checked the Nerve repo at `/tmp/openclaw-nerve` to re-anchor the roadmap against the original audit basis.
- Relevant Nerve patterns confirmed:
  - `src/hooks/useChatRecovery.ts` uses a recovery generation ref to discard stale async recovery results after session switches or newer chat-final application.
  - `src/hooks/useWebSocket.ts` uses connection-generation tracking so older sockets cannot overwrite newer connection state.
  - `server/lib/kanban-store.test.ts` explicitly rejects stale run completion against a newer rerun/session key.
- Applied the first Mission Control equivalent in `projects/mission-control/hooks/useRuntimeBridge.ts`.
- Change: added session-generation invalidation for `load(...)` so late runtime-bridge fetches cannot apply summary/error/loading completion to a newer selected session after a switch.
- Implementation details:
  - added `activeSessionKeyRef` and `sessionGenerationRef`
  - `load(...)` now captures requested session + generation at start and discards late success/error/finalizer work if either changed
  - `switchSession(...)` now increments generation before clearing transcript state and starting the new session load
- Why: this is the cleanest first runtime-hardening step after the structural Chat refactor, and it matches the stale-state discipline we saw in Nerve without broadening into a risky rewrite.
- Verification: `npm run build` passed, `bash scripts/preview-restart.sh` completed successfully, and `http://127.0.0.1:3005/general/chat` plus `/general/agents` both returned 200 after restart.
- Rollback: revert the commit created for this hardening slice.

## Chat hardening follow-up (12:57)
- Continued the Nerve-inspired stale-state hardening pass in `projects/mission-control/hooks/useRuntimeBridge.ts`.
- Change: guarded late `sendPrompt(...)` and `abortPrompt(...)` completions against session switches.
- Why: after the first load-generation fix, the next obvious async seam was prompt send/abort work resolving after the operator had already switched to a different runtime session. Without a guard, an old send/abort acknowledgement could still overwrite the newer session’s `sendState`, `sendError`, `activeRunId`, or event list.
- Implementation details:
  - both `sendPrompt(...)` and `abortPrompt(...)` now capture the current `sessionGenerationRef`
  - after `ensureSessionExists(...)`, `rpc('chat.send', ...)`, and `rpc('chat.abort', ...)`, they bail out if either the generation changed or the active session key no longer matches the session they started for
  - this mirrors the Nerve pattern of invalidating late async completions when the active session/run already moved on
- Verification: `npm run build` passed, `bash scripts/preview-restart.sh` completed successfully, and `http://127.0.0.1:3005/general/chat` plus `/general/agents` both returned 200 after restart.
- Rollback: revert the commit created for this hardening slice.

## Savepoint / handover (13:07)
- Wrote a fresh handover document for post-account-switch resume:
  - `projects/_ops/mc-savepoint-2026-04-12-afternoon.md`
- Purpose: let future-me resume immediately after quota/account switch without reconstructing the Chat refactor + hardening context from scratch.
- Contents include:
  - Morning Meeting fixes and doc hygiene changes
  - completed Chat structural refactor slices
  - Nerve repo references used for stale-state hardening
  - completed `useRuntimeBridge.ts` hardening commits
  - current verified preview/build state
  - explicit recommended next step and resume order
