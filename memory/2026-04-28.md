# 2026-04-28

## Nightly Memory Extraction [02:00 GMT+8]
No active sessions found other than this cron run. Reviewed today's daily memory and entity files.

## Key Decisions
- Trading section rebuild named **BOILER ROOM** with Atreus-style nav (Overview, Portfolio, Watchlist, Health, Analytics, News, Screener, Bots); Analytics absorbs Charts/Technical, News absorbs Reports
- Trading should follow Atreus as primary reference, not cling to old Market Intel artifacts; Health/Analytics/News are explicit first-class pages
- Phase 1 + static Overview skeleton + placeholder page shells = approved first implementation slice

## Project Status Updates

### Mission Control - Trading Lab (BOILER ROOM)
- Trading domain shell implemented in `projects/mission-control-lab` with new identity branding (BOILER ROOM vs MARVIN'S ROOM)
- Consolidated Atreus-style sidebar nav applied
- Static Overview skeleton with market tape, account P&L, sample chart, health/watchlist/exposure/news previews, and section links
- Placeholder shells for all sub-routes: Portfolio, Watchlist, Health, Analytics, News, Screener, Bots, Signals, ticker detail
- Analytics has tabs: Performance, Attribution, Exposure, Dividends, Charts/Technical
- News has tabs: Market News, Watchlist News, Earnings/Calendar, Reports, Filings
- First slice validated: `npm run lint` + `npm run build` passed; known warnings only (middleware, autonomous runner trace, NFT via files route)
- Runtime verified working at Lab lane (ports 3015/3017) after stale bundle fix (had to rebuild via maintenance-lock flow)
- Next likely work: visual polishing toward Atreus/stock-dashboard screenshots

### Mission Control - Mobile audit completed (Apr 27)
- Skills, Crons, Memory, Files mobile optimization completed in Lab
- Mobile-only changes promoted to Dashboard (sidebar hidden, usable viewport width, Memory/Files two-column collapses cleanly, Skills single-column card treatment)
- All Dashboard validations passed: lint, build, service health, route checks (all HTTP 200), lane smoke
- Mobile CDP confirmed: sidebar hidden, no horizontal overflow, rails scroll, visible actions limited to chat/workspace
- Only dismissible note: task-toast overlap on Skills (known separate issue)

### Mission Control - Chat first-paint + Agents mobile (Apr 27)
- Chat first-paint optimization promoted (transcript hydrates after page load, split runtime/run state, softened internal labels, mobile grid class, composer control wrapping)
- Agents page mobile QA completed; promoted to Dashboard (mobile sidebar hidden, rail scroll hint, visible actions limited)
- Dashboard Chat diffs untouched throughout

### Morning meeting decisions (Apr 27)
- SI-2026-0427-001: TOOLS.md Nexos stale caveat → updated to treat any Nexos reappearance as drift
- SI-2026-0427-002: AGENTS.md daily-memory startup scope → adjusted to today as primary, yesterday only when useful in morning/afternoon, older notes only when referenced/needed/high-risk
- SI-2026-0427-003: TOOLS.md hardcoded Next.js version → changed to point to package.json as source of truth (verified 16.2.0)
- SI-2026-0427-004: TOOLS.md Search Scope Clarifier → rewrote as operational tool-routing guidance (not removed)
- SI-2026-0427-005: SOUL.md cross-reference to IDENTITY.md → added Continuity note that IDENTITY.md is secondary legacy reference
- Cleanup: mirrored useful content from stray `_ops/mission-control-dashboard-learnings-2026-04-22.md` into `.learnings/errors.md` and deleted the stray file

### OpenClaw loop-pattern audit
- Wrote `projects/_ops/openclaw-loop-pattern-audit-2026-04-27.md` documenting a no-progress retry cluster in an autonomous task session
- Root-cause hypothesis: session lacked strong pivot gate after missing reference file and consecutive low-information diagnostic probes
- Recommendation: bounded no-progress pivot guard + prompt rule to switch after two empty/low-information probes

### Deterministic scheduler (overnight Apr 27→28)
- All scheduled tasks completed cleanly: auto-signal-dispatcher (every 15min), custom-news-feed-monitor (every 30min), rss-feed-monitor (every 30min, randomized delay), reddit-monitor (hourly)
- No failures or restarts observed in cron-run-log

## Entities
- Trading section: new Lab project BOILER ROOM in `projects/mission-control-lab`
- Mobile audit: Skills/Crons/Memory/Files mobile fixes promoted to Dashboard
- Morning meeting: 5 decisions applied to TOOLS.md, AGENTS.md, SOUL.md
- Loop-pattern audit artifact written to `projects/_ops/openclaw-loop-pattern-audit-2026-04-27.md`

## Autonomous Home Improvement [08:30 GMT+8]
- Changed: added a `Gate Modes` section to `AUTONOMY.md` documenting `workspace`, `queue`, and `improve` behavior, including improve-mode's outside-active-window exception and `decision=run/skip` handling.
- Why: autonomy runs were relying on script comments and implied behavior, which created avoidable operator ambiguity.
- Expected benefit: faster, safer autonomy execution with less friction when choosing the right gate mode or interpreting gate output.
- Rollback: remove the `Gate Modes` section from `AUTONOMY.md` if it becomes redundant or drifts from the script.
## Morning Meeting Decision [10:05]
- Report: nightly-security-review
- Finding: INFO-001 / market-intel reasoning_engine relative-path crash
- Decision: Approved
- Problem: `projects/market-intel/src/reasoning_engine.py` loaded `data/patterns.json`, `signals.json`, and `model_feedback.json` via CWD-relative paths, so hourly runs could fail whenever cron executed outside the market-intel root.
- Risk: low security risk, but recurring operational failure in the reasoning layer meant enhanced signal output could be missing despite the base signal pipeline still running.
- Action taken: patched `reasoning_engine.py` to anchor all three data files via `Path(__file__).resolve().parent.parent / "data" / ...` and load them from the resolved data directory.
- Verification: `python3 -m py_compile projects/market-intel/src/reasoning_engine.py` passed; direct instantiation loaded `patterns 42`, `signals_loaded True`, `feedback_loaded True` from outside the project subdirectory.
- Rollback: revert `projects/market-intel/src/reasoning_engine.py` to the previous relative-path version if needed.
- Notes: security review correctly classified this as operational rather than exploitability risk.

## Morning Meeting Decision [10:11]
- Report: self-improvement
- Finding: SI-2026-0428-001 / `_ops` superseded-doc cleanup looked unfinished
- Decision: Accepted risk
- Problem: the Apr 26 cleanup manifest still implied 40 superseded `_ops` docs needed moving, creating the appearance of an unfinished housekeeping task.
- Risk: low; mainly workspace-clutter confusion if the manifest were trusted over the live directory state.
- Action taken: verified the listed superseded docs are already gone from `projects/_ops/`, consistent with Philippe's manual cleanup; removed one leftover breadcrumb file `projects/_ops/scripts/__pycache__/openclaw_retry_preflight.cpython-314.pyc` and the now-empty `__pycache__` directory.
- Verification: current `projects/_ops/` listing no longer contains the old 40-file superseded set; `pycache_exists False`, `breadcrumb_exists False` after removal.
- Rollback: restore the deleted `.pyc` only if specifically needed, otherwise n/a.
- Notes: treat the original self-improvement finding as stale rather than still-open work.

## Morning Meeting Decision [10:13]
- Report: self-improvement
- Finding: SI-2026-0428-002 / retired `enrichment_ab_review.py` still present
- Decision: Approved
- Problem: `scripts/cron_tasks/enrichment_ab_review.py` remained in the workspace after the `enrichment-ab-review` task was removed from the deterministic scheduler, leaving dead script clutter.
- Risk: low; mainly confusion about whether the script still participates in active cron behavior.
- Action taken: deleted `scripts/cron_tasks/enrichment_ab_review.py` after checking for active scheduler/script references.
- Verification: workspace script grep returned no active scheduler references; `exists_after=no` for the deleted script path.
- Rollback: restore `scripts/cron_tasks/enrichment_ab_review.py` from git if needed for reference.
- Notes: legacy mentions remain only in historical memory/entity records, not in active scheduler code.

## Morning Meeting Decision [10:15]
- Report: self-improvement
- Finding: SI-2026-0428-004 / `memory/executor-subagent-queue.json` looked orphaned
- Decision: Adjusted
- Problem: the file looked stale because it has 14 old entries and no active pending/spawned work, creating the impression that it might be an abandoned artifact.
- Risk: removing or resetting it would be unsafe because it is still a live queue/history ledger read by Mission Control, Lab, autonomy helpers, queue-state tooling, and the autonomous task executor.
- Action taken: investigated live references and current queue state; did not remove or reset the file.
- Verification: `scripts/queue_state.py status` shows `spawned: 0`, `pending: 0`, `completed: 13`, `blocked: 1`; active code references remain in `projects/mission-control/lib/autonomous.ts`, `projects/mission-control/scripts/run-autonomous-task.mjs`, `projects/mission-control-lab/lib/autonomous.ts`, `projects/mission-control-lab/scripts/run-autonomous-task.mjs`, `scripts/autonomy_gate.py`, `scripts/queue_state.py`, `scripts/queue_triage.py`, and `scripts/autonomous-task-executor.py`.
- Rollback: n/a
- Notes: treat the file as a live history/queue ledger with currently idle state, not as an orphan to archive blindly.

## Morning Meeting Decision [10:18]
- Report: self-improvement
- Finding: SI-2026-0428-005 / stale `marvin-webhook-receiver.service` note in `TOOLS.md`
- Decision: Approved
- Problem: `TOOLS.md` carried a fuzzy historical note implying the host webhook service may or may not exist, without distinguishing what is actually provable from inside the container.
- Risk: low; mainly operational ambiguity about the real webhook supervision truth.
- Action taken: verified available evidence first. Confirmed the host-service install artifacts and runbook exist in the workspace, older daily memory records the service as active under host systemd, but direct `systemctl status marvin-webhook-receiver.service` cannot be verified from inside this container because systemd is not the container init system. Updated `TOOLS.md` to state that clearly and to anchor current verified truth on container-local health/recovery paths.
- Verification: `systemctl status marvin-webhook-receiver.service` inside container failed with `System has not been booted with systemd as init system`; `deploy/marvin-webhook-receiver.service`, `deploy/install-marvin-webhook-receiver.sh`, and `docs/runbooks/webhook-receiver-host-service.md` all exist; current receiver process exists as `python3 -m src.webhook_receiver`.
- Rollback: revert the `TOOLS.md` note change if a future host-side check establishes a crisper permanent statement.
- Notes: if host-level certainty is needed later, verify from the host with `sudo systemctl status marvin-webhook-receiver.service --no-pager`.

## Morning Meeting Decision [10:22]
- Report: platform-health-council
- Finding: model-backed task issue / `nightly-memory-extraction` partial failure from forbidden home-relative path
- Decision: Approved
- Problem: the job updated daily memory successfully but still failed the run because entity-update behavior remained too permissive, allowing a forbidden `~/.openclaw/workspace/life/projects/mission-control.md` edit attempt.
- Risk: low to medium operational noise and partial extraction loss; daily memory can still update, but entity promotion may fail and cron health remains noisy.
- Action taken: tightened the live cron job payload for `nightly-memory-extraction` so entity updates may write only to existing allowed `items.json` files under `/data/.openclaw/workspace/life/...`; explicitly banned legacy `.md` entity targets such as `life/projects/mission-control.md`; preserved skip-only behavior for any ambiguous or forbidden paths.
- Verification: live cron job `7201afb2-6ff2-4ce7-8c24-e12d4413b9f9` now contains the new `ENTITY FILE RULES` section and explicit `items.json` examples; current daily memory confirms the run had partial success (`Nightly Memory Extraction [02:00 GMT+8]` entry present) while prior cron state recorded the forbidden `.md` path failure.
- Rollback: revert the `nightly-memory-extraction` cron payload to the previous text if this over-constrains extraction behavior.
- Notes: this addresses the real failure mode more directly than wrapper-status tweaking; next overnight run should confirm whether the path leak is fully gone.

## Morning Meeting Decision [10:24]
- Report: platform-health-council
- Finding: operational issue / Mission Control `npm audit` blocked by npm cache corruption
- Decision: Deferred
- Problem: `npm audit` for Mission Control is currently blocked by an `ENOENT` cache error under `/data/.npm/_cacache/...`, reducing dependency-audit hygiene.
- Risk: low runtime risk; current app behavior is unaffected, but audit/update operations are degraded.
- Action taken: no cache cleanup during Morning Meeting.
- Verification: deferred by Philippe for follow-up after the Morning Meeting.
- Rollback: n/a
- Notes: keep as a post–Morning Meeting to-do; likely fix path is cache cleanup plus fresh install in `projects/mission-control`.

## Session Closeout [11:29 GMT+8]
- Morning Meeting completed across nightly security review, self-improvement, and Platform Health Council findings.
- Security review remained clean at 0 critical / 0 high; the one operational reasoning-engine path issue was fixed and committed.
- Self-improvement follow-up cleaned the stale `_ops` cache breadcrumb, removed the retired `enrichment_ab_review.py` script, clarified the webhook host-service note in `TOOLS.md`, and confirmed `executor-subagent-queue.json` remains a live ledger rather than an orphan.
- Platform Health Council follow-up hardened the `nightly-memory-extraction` cron payload so entity updates must target canonical `items.json` files, and deferred the Mission Control npm-cache/audit cleanup as an explicit after-meeting to-do.
- Post-meeting follow-up completed: Mission Control npm cache / `npm audit` blocker resolved; safe middleware-to-proxy convention cleanup also completed and committed.

## Post-Morning-Meeting Follow-up [11:57 GMT+8]
- Task: resolved the deferred Mission Control npm cache / `npm audit` blocker from Platform Health Council.
- Problem: `npm audit` had been blocked by npm cache corruption under `/data/.npm/_cacache`, preventing dependency-audit hygiene.
- Action taken: ran `npm cache verify`, which completed successfully after garbage collection and confirmed audit could execute again. Then addressed the real advisories surfaced by audit with a bounded dependency update in `projects/mission-control`: `next` `16.2.0` → `16.2.4`, `eslint-config-next` `16.2.0` → `16.2.4`, and an npm `overrides.postcss` pin to `8.5.12` because latest Next still declares vulnerable `postcss@8.4.31`.
- Verification: `npm install` completed; installed versions verified as `next 16.2.4`, `eslint-config-next 16.2.4`, `postcss 8.5.12`; `npm audit --audit-level=moderate` reports `found 0 vulnerabilities`; `npm run lint` passed; `npm run build` passed under Next `16.2.4` with the same known middleware / dynamic runner / NFT warnings.
- Rollback: remove the `overrides.postcss` entry and revert `projects/mission-control/package.json` + `package-lock.json` to the previous Next `16.2.0` versions if the patch update causes unexpected runtime issues.

## Mission Control Follow-up [12:08 GMT+8]
- Task: fixed the safe Next.js middleware convention warning without touching the dynamic autonomous runner trace or NFT tracing warnings.
- Action taken: renamed `projects/mission-control/middleware.ts` to `projects/mission-control/proxy.ts` and changed the exported handler from `middleware()` to `proxy()` while preserving the matcher and auth logic unchanged.
- Verification: `npm run lint` passed; `npm run build` passed under Next `16.2.4`; the middleware deprecation warning is gone. The two remaining build warnings are the known dynamic autonomous runner trace and NFT tracing warnings, intentionally left untouched.
- Rollback: rename `proxy.ts` back to `middleware.ts` and export `middleware()` again if needed.

## Mission Control Trading Lab Overview polish [14:28 GMT+7]
- Philippe reviewed the BOILER ROOM Overview and requested final Atreus-style refinements before calling the page done-for-now.
- Action taken in `projects/mission-control-lab` only: removed leftover subheads from Top Movers/Earnings/News Pulse; aligned Pinned Watchlists table columns across watchlist blocks; reduced watchlist spacing to avoid unnecessary desktop horizontal scroll; upgraded the Total Account Value chart from placeholder styling to a green Atreus-style line/area chart with grid lines, endpoint dot, and better-centered range tabs.
- Verification: Lab `npm run lint` passed; Lab `npm run build` passed with known existing warnings only; Lab runtime refreshed with maintenance-lock flow; live `/trading` confirmed the removed subheads are absent and chart/range markers are present.
- Rollback: revert the focused Lab Trading files from the next commit if the visual direction needs to be reset.

## Mission Control Trading Lab Overview signoff [15:05 GMT+7]
- Philippe confirmed the BOILER ROOM Overview is now directionally right after the final watchlist alignment fix.
- Latest focused fixes after the main polish commit:
  - `1b566bf Fix Lab watchlist overflow`: removed the tiny desktop horizontal overflow in Pinned Watchlists by tightening the watchlist table wrapper and column allocation.
  - `74fd60b Align Lab watchlist day column`: realigned the DAY header with sparkline cells and right-aligned the numeric watchlist columns consistently.
- Current done-for-now Overview state:
  - no redundant top title block;
  - Atreus-style account card with total value, day P/L, all-time P/L, centered range tabs, and green line/area chart;
  - Portfolio Health uses Atreus categories: Concentration, ESG Alignment, Risk Level, Diversification;
  - Pinned Watchlists are clean, column-aligned, and free of micro horizontal scroll on desktop;
  - Top Movers, Earnings, and News Pulse remain as compact secondary modules without extra subtitles.
- Validation evidence: Lab lint/build passed repeatedly with known existing warnings only; Lab runtime refreshed through the maintenance-lock flow; Philippe visually confirmed the final watchlist/day-column fix.
- Next recommended build step: start `/trading/ticker/[symbol]` as the companion Atreus ticker-detail page, using `stock-dashboard-2.jpg` as the main reference and keeping the Overview as current durable truth.

## Mission Control Trading Lab ticker detail prototype [15:50 GMT+7]
- Task: built the next BOILER ROOM Lab slice for `/trading/ticker/[symbol]` as a static/sample ticker-detail research page.
- Action taken in `projects/mission-control-lab` only: expanded the ticker page from a minimal shell into an Atreus-inspired layout using the approved Overview visual direction: market tape, back link, quote header, follow affordance, header stats, tabs, price chart, company profile, chart stats, financial highlights with sparklines, recent news, resources, financial overview bars, and key ratios.
- Data posture: sample/static data only; no provider/API integration yet. The page is shaped for a future server-side cached ticker profile adapter rather than a global ticker database.
- Verification: Lab `npm run lint` passed; Lab `npm run build` passed; Lab restarted via `scripts/lab-restart.sh`; `scripts/lab-health.sh` passed; HTTP checks returned 200 for `/trading`, `/trading/ticker/NVDA`, and `/trading/ticker/MSFT`; rendered HTML contained expected ticker sections.
- Known warnings: Lab build still reports the existing middleware convention warning, dynamic autonomous runner trace warning, and NFT tracing warning; intentionally untouched.
- Commit: `bdb1897 Build Lab trading ticker detail prototype`.
- Rollback: revert commit `bdb1897` if the ticker prototype direction needs to be reset.

## Mission Control Trading Lab Zero Sum review + ticker polish [16:25 GMT+7]
- Philippe reviewed the BOILER ROOM ticker prototype positively and requested header polish: move `+ Follow` from the quote identity area to the tab row, rename it to watchlist language with state colouring, and reduce stock-price size so it does not overpower the company name.
- Applied Lab-only UI polish in `projects/mission-control-lab`: moved the action into a right-aligned tab-row control, changed sample state to `− Watchlist`, added subtle watched/unwatched styling, and reduced the price type scale.
- Zero Sum reference analysis: screenshot/repo show a WSJ-style ticker page with a compact nav/search/timeframe control row, large but restrained quote header, key metric rail, deep price chart with moving averages, then company profile, cash/debt, balance-sheet visualizations, ratings radar, and cash-vs-debt trend modules. Useful for future BOILER ROOM depth, but the visual language should remain Overview/Atreus-derived.
- Zero Sum data architecture finding: Next.js frontend + Flask backend + yfinance; persistent `./data/cache` mounted via Docker; scheduler warms market/fundamental data; `/api/stock-detail/<ticker>` serves a combined cached detail payload from batch JSON; ticker pages use ISR/revalidate 300 and hydrate with client fetches for fuller prices/news; cache headers and circuit breaker/rate-limit posture are explicit. This supports our earlier direction: server-side cached ticker profile adapter, not a full prefilled global DB or direct UI-to-provider calls.
- Verification: Lab `npm run lint` passed; Lab `npm run build` passed; Lab restarted; `scripts/lab-health.sh` passed; `/trading/ticker/NVDA` and `/trading/ticker/MSFT` returned 200; rendered HTML includes the tab-row and `− Watchlist` control.
- Known Lab warnings remain the existing middleware convention, dynamic autonomous runner trace, and NFT tracing warnings; intentionally untouched.
- Commit: `71eb627 Polish Lab ticker watchlist controls`.

## Mission Control Trading Lab ticker profile contract [16:42 GMT+7]
- Philippe approved proceeding from the polished ticker prototype into the data-contract layer.
- Added Lab-only trading contract files under `projects/mission-control-lab/lib/trading/`: `contracts.ts` defines `TickerProfile`, quote/profile/price/financial/news/resource/source metadata types; `ticker-profile.ts` exposes `getTickerProfile(symbol)` as a mock adapter returning the existing static data in the future provider-ready shape.
- Refactored `/trading/ticker/[symbol]` to consume `getTickerProfile()` instead of importing ticker fixture internals directly; the page now reads quote, price series, profile, financial overview, source map, and freshness metadata through the contract.
- Scope remained static/sample and Lab-only. No live providers, no Dashboard promotion, no broker/execution wiring.
- Verification: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed; Lab restarted after a transient start-script race; `scripts/lab-health.sh` passed; `/trading/ticker/NVDA` and `/trading/ticker/MSFT` returned 200; rendered HTML includes `NVIDIA Corporation`, `− Watchlist`, and `contract-ready`.
- Known Lab build warnings remain the existing middleware convention, dynamic autonomous runner trace, and NFT tracing warning; intentionally untouched.
- Commit: `ff3d885 Add Lab ticker profile contract`.

## Mission Control Trading Lab ticker cache/source pipeline [16:51 GMT+7]
- Continued the approved provider-ready sequence after the `TickerProfile` contract layer.
- Added Lab-only cache/source plumbing under `projects/mission-control-lab/lib/trading/`: `cache.ts` provides sanitized symbol JSON cache read/write with TTL and atomic rename; `sources.ts` defines provider source interfaces/results; `sources/sample.ts` moves the existing sample fixture into a source adapter.
- Updated `getTickerProfile()` to check the on-demand ticker-profile cache first, then iterate source adapters, write the resulting profile to cache, and return it. This mirrors the Zero Sum lesson without introducing live provider calls yet.
- Added `projects/mission-control-lab/data/trading/.gitignore` so generated `ticker-profiles/` cache files stay runtime-local.
- Scope remained Lab-only/static-provider. No Yahoo/SEC/Defeat-Beta network integration, no Dashboard promotion, no broker/execution wiring.
- Verification: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed; Lab restarted (preview script again had a PID-capture race, but direct follow-up showed service healthy); `scripts/lab-health.sh` passed; `/trading/ticker/NVDA` and `/trading/ticker/MSFT` returned 200; rendered HTML includes `NVIDIA Corporation`, `− Watchlist`, and `contract-ready`; runtime cache files were generated for NVDA/MSFT and are ignored.
- Known Lab build warnings remain the existing middleware convention, dynamic autonomous runner trace, and NFT tracing warning; intentionally untouched.
- Commit: `326e59f Add Lab ticker profile cache pipeline`.

## Mission Control Trading Lab symbol-aware mock ticker profiles [16:59 GMT+7]
- Philippe noticed that route symbols changed in some labels while company name/profile still showed NVIDIA for every ticker. Treated this as unacceptable mock behavior, not a product-intent feature.
- Updated `projects/mission-control-lab/lib/trading/sources/sample.ts` so the sample source is symbol-aware for NVDA, MSFT, TSM, and AAPL, with distinct company names, exchanges, quote snippets, market cap/P/E/sector/industry, summaries, facts, news, and selected ratios where useful.
- Added a generic fallback for unknown symbols such as XYZ that clearly labels provider-pending fields instead of pretending the ticker is NVIDIA.
- Cleared runtime ticker-profile cache before verification so stale cached NVDA-shaped profiles did not mask the fix.
- Verification: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed; `scripts/lab-health.sh` passed; `/trading/ticker/NVDA`, `/MSFT`, `/TSM`, `/AAPL`, and `/XYZ` returned 200; rendered HTML now shows distinct names for Microsoft, TSM, Apple, and a provider-pending fallback for XYZ.
- Lab restart attempted during verification exposed existing stale Lab port holders/EADDRINUSE behavior; direct health confirmed the Lab process serving 3015/3016/3017/3022/3023 was healthy and route checks passed. Live Dashboard processes were not touched.
- Commit: `4103745 Make Lab ticker sample profiles symbol aware`.

## Mission Control Trading Lab Yahoo provider adapter [17:22 GMT+7]
- Philippe approved trying the first actual provider adapter slice. Kept it narrow: read-only, server-side, cached, and fail-soft behind the existing `getTickerProfile()` source pipeline.
- Probed Yahoo endpoints. Python urllib without a browser user-agent hit 429, but Node fetch/curl with a browser-ish user-agent succeeded for the chart endpoint and search endpoint. QuoteSummary/quote endpoints returned 401/crumb errors, so this slice intentionally uses only Yahoo chart/search, not crumb-dependent endpoints.
- Added `projects/mission-control-lab/lib/trading/sources/yahoo.ts` and placed `yahooTickerProfileSource` before the sample source in `ticker-profile.ts`.
- Yahoo adapter currently fills live chart/search-backed: symbol, company name, exchange, currency, quote price/change/change %, Yahoo as-of time, price series, chart stats, sector, and industry where search provides it. It retains sample fallback for profile narrative, financial highlights, news, resources, filings, market cap/P/E when Yahoo chart metadata lacks those fields, and unsupported/missing tickers.
- Verification: `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed. Cleared ticker cache. Lab route checks returned 200 for NVDA/MSFT/TSM/AAPL/FAKEZZZ. Rendered HTML showed Yahoo-backed outputs for real tickers and sample fallback for FAKEZZZ. Cache inspection confirmed MSFT profile `fresh/yahoo` with live price `$424.82`, while FAKEZZZ profile remained `sample/sample` with `$—`.
- Lab restart again showed existing EADDRINUSE/PID-capture noise, but route checks and rendered output succeeded against the Lab service. Live Dashboard processes were not touched.
- Known unrelated build warnings persisted: middleware convention, autonomous runner dynamic trace, and NFT tracing.
- Commit: `cb7a55c Add Lab Yahoo ticker source adapter`.

## Mission Control Trading Lab Yahoo daily change fix [17:34 GMT+7]
- Philippe noticed AMZN showed `$261.12 +72.13 (+38.17%)`, which was obviously not a daily move. Confirmed bug: Yahoo adapter compared `regularMarketPrice` to `chartPreviousClose` from the 1Y chart, effectively the start-of-range close, not the previous trading day.
- Fixed `projects/mission-control-lab/lib/trading/sources/yahoo.ts` to derive daily change from the last two valid daily closes in the Yahoo 1Y chart series, falling back only if needed.
- Verification probe for AMZN showed Yahoo chart values: price `261.12`, `chartPreviousClose` `188.99`, last close `261.119995...`, previous daily close `263.989990...`; corrected output after cache clear is `$261.12 -2.87 -1.09%`.
- `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed. Cleared ticker cache. Route/cache checks confirmed AMZN `fresh/yahoo` `$261.12 -2.87 -1.09%`, MSFT `fresh/yahoo` `$424.82 +0.20 +0.05%`, and FAKEZZZ sample fallback `$— +0.00 +0.00%`.
- Lab health passed after restart despite repeated preview-script EADDRINUSE/PID-capture noise; final ports/processes were healthy on Lab ports. Live Dashboard processes were not touched.
- Commit: `2e3c4f5 Fix Lab Yahoo daily change calculation`.

## Mission Control Trading Lab ticker mobile layout pass [18:22 GMT+7]
- Philippe requested making `/trading/ticker/[symbol]` mobile-friendly before continuing with provider/fundamental slices. Treated this as a foundation change so later modules do not compound desktop-first layout debt.
- Updated `projects/mission-control-lab/components/pages/trading/trading.module.css` with a dedicated max-width 560px ticker layout: compact quote header, smaller ticker mark, wrapped price row, 2-column stat grid, full-width watchlist action, horizontally scrollable tabs/range controls with larger tap targets, shorter mobile chart, reduced chart-axis density, card padding/radius tuning, mobile-friendly profile/key-ratio rows, single-column financial highlight cards, tighter news/resources, and compressed bar chart.
- Also hardened the trading shell mobile rule with a `:has(.trading-shell-main)` fallback because the CSS-module class on the main shell prevented the original `.app-shell-grid.trading-shell-grid` override from reliably hiding the sidebar in the first screenshot.
- Verification: `npx tsc --noEmit`, `npm run lint`, and repeated `npm run build` passed. Lab health passed. `/trading/ticker/AMZN` and `/trading/ticker/TSM` returned 200 and rendered Yahoo-backed AMZN content.
- Screenshot verification used Chromium headless at 390x1200. First screenshot showed sidebar squeezing the page; after shell fix the sidebar was gone and content flow was correct. Final CDP measurement at 390px showed `documentElement.scrollWidth` and `body.scrollWidth` both 390, so there is no true page-wide horizontal overflow; remaining offscreen elements are contained inside intentional horizontal scroll rows for market tape and tabs.
- Screenshot artifacts were written under `projects/mission-control-lab/tmp/mobile-check/` and remain untracked/ignored.
- Known unrelated build warnings persisted: middleware convention, autonomous runner dynamic trace, and NFT tracing.
- Commit: `2d2efe1 Improve Lab ticker mobile layout`.
