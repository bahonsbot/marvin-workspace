# Daily Memory — 2026-04-16

## Mission Control rollback-baseline recovery + OpenClaw upgrade audit [23:23 GMT+7]
- What changed: treated the rolled-back workspace as canonical truth, rebuilt the Mission Control preview from that state, verified the local preview route is healthy, cleaned the nested `projects/mission-control` git state so generated/runtime junk is no longer tracked, and completed a full upstream OpenClaw audit from `2026.3.8` to `v2026.4.12`.
- Verified runtime truth: local Mission Control preview is healthy on `/general/chat`; public preview HTTP still routes into the app, but preview HTTPS is currently misrouted to default nginx / `404`, and that host-side HTTPS issue was intentionally deferred until the later live-build phase.
- Git-state result: the nested Mission Control repo is now re-anchored to the current steady workspace source tree instead of polluted build/runtime output, with the preview env example kept tracked while `.next`, `node_modules`, and preview-runtime junk are ignored.
- Upgrade recommendation: target **OpenClaw `v2026.4.12`** only as an **isolated rehearsal upgrade**, not a direct live jump.
- Key reasoning: April brings meaningful QMD, isolated-session/cron, webchat/control-surface, and security-hardening improvements, but the current deployment still depends on dangerous Control UI compatibility flags (`allowInsecureAuth`, `dangerouslyDisableDeviceAuth`, `dangerouslyAllowHostHeaderOriginFallback`), so combining the core upgrade with HTTPS/proxy hardening or auth cleanup would be unnecessarily risky.
- Explicit first-pass rule: keep **Dreaming** and **Active Memory** disabled during the first rehearsal; prove base runtime compatibility first, then test new memory features separately.
- Artifact: wrote `projects/_ops/openclaw-v2026.4.12-upgrade-audit-2026-04-16.md` and committed it in workspace git as `5ff1634` (`Add OpenClaw v2026.4.12 upgrade audit`).
