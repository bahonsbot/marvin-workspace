# Daily Memory — 2026-04-16

## Mission Control rollback-baseline recovery + OpenClaw upgrade audit [23:23 GMT+7]
- What changed: treated the rolled-back workspace as canonical truth, rebuilt the Mission Control preview from that state, verified the local preview route is healthy, cleaned the nested `projects/mission-control` git state so generated/runtime junk is no longer tracked, and completed a full upstream OpenClaw audit from `2026.3.8` to `v2026.4.12`.
- Verified runtime truth: local Mission Control preview is healthy on `/general/chat`; public preview HTTP still routes into the app, but preview HTTPS is currently misrouted to default nginx / `404`, and that host-side HTTPS issue was intentionally deferred until the later live-build phase.
- Git-state result: the nested Mission Control repo is now re-anchored to the current steady workspace source tree instead of polluted build/runtime output, with the preview env example kept tracked while `.next`, `node_modules`, and preview-runtime junk are ignored.
- Upgrade recommendation: target **OpenClaw `v2026.4.12`** only as an **isolated rehearsal upgrade**, not a direct live jump.
- Key reasoning: April brings meaningful QMD, isolated-session/cron, webchat/control-surface, and security-hardening improvements, but the current deployment still depends on dangerous Control UI compatibility flags (`allowInsecureAuth`, `dangerouslyDisableDeviceAuth`, `dangerouslyAllowHostHeaderOriginFallback`), so combining the core upgrade with HTTPS/proxy hardening or auth cleanup would be unnecessarily risky.
- Explicit first-pass rule: keep **Dreaming** and **Active Memory** disabled during the first rehearsal; prove base runtime compatibility first, then test new memory features separately.
- Artifact: wrote `projects/_ops/openclaw-v2026.4.12-upgrade-audit-2026-04-16.md` and committed it in workspace git as `5ff1634` (`Add OpenClaw v2026.4.12 upgrade audit`).

## OpenClaw rehearsal execution plan [23:35 GMT+7]
- What changed: turned the `v2026.4.12` audit into an execution-grade rehearsal runbook at `projects/_ops/openclaw-v2026.4.12-rehearsal-execution-plan-2026-04-16.md`, with a concrete smoke-test matrix for gateway startup, Mission Control core pages, runtime bridge/history, QMD memory health, Telegram, cron, and sessions.
- Key planning decision: the first rehearsal should run in an isolated `rehearsal-412` lane with a separate gateway target and Mission Control preview env, while keeping Dreaming, Active Memory, HTTPS/proxy hardening, and Control UI security cleanup out of scope.
- Important operational note: remote HTTPS remains a separate prerequisite for STT and secure browser-context features, but it does not block the base upgrade rehearsal as long as the browser-facing origin question is kept separate from the runtime-validation lane.
- Critical Telegram rule: do not let a rehearsal runtime compete with the live runtime for the same Telegram bot lane; use a secondary bot or a short exclusive ownership window for any real routing test.
- Expected benefit: the next OpenClaw upgrade step can be judged from a repeatable proof loop instead of a vague “update and poke around” session.

## Rehearsal lane bring-up + smoke pass [00:48 GMT+7]
- What changed: built the isolated `v2026.4.12` rehearsal lane for real instead of leaving it as a plan.
- Rehearsal runtime facts:
  - CLI install: `/data/.openclaw/rehearsals/v2026.4.12/cli`
  - state root: `/data/.openclaw/rehearsals/v2026.4.12/state`
  - gateway: `ws://127.0.0.1:19001`
  - Mission Control rehearsal preview: `3015/3016/3017`
  - live Mission Control preview preserved on `3005/3006/3007`
  - Telegram disabled in rehearsal config
  - Dreaming / Active Memory kept disabled
- Verified passes:
  - rehearsal config validated cleanly
  - rehearsal gateway health returned `{"ok":true,"status":"live"}`
  - rehearsal status showed reachable gateway metadata on `19001`
  - rehearsal Mission Control routes returned `200` for Home, Chat, Agents, Crons, Tasks, Memory, and Files
  - rehearsal runtime-bridge history returned `200` for `agent:main:main`
  - rehearsal cron status/list succeeded
  - rehearsal sessions registry responded
  - rehearsal memory backend reported QMD once local toolchain PATH was corrected
  - rehearsal `memory search "Philippe" --max-results 3 --json` returned results after QMD collection cleanup in isolated state
- Important failure discovered and fixed in workspace source: `projects/mission-control/scripts/preview-stop.sh` used broad `pkill` patterns (`next-server`, sidecar, proxy), so a rehearsal stop/restart could kill the normal preview too. Fixed it to stop only the PID-file-backed process group for the selected runtime dir.
- Important rehearsal-only compatibility wrinkle: the isolated QMD state carried a legacy `memory` collection while `v2026.4.12` search expected `memory-dir-main`; removing the legacy collection inside the isolated rehearsal state allowed managed search to succeed. This should be treated as a real migration risk to document before any live upgrade.
- Minor note: one QMD boot-time embed pass timed out during search, but the search still completed and returned results.

## Disposable rehearsal cron run [01:05 GMT+7]
- What changed: executed one inert write-surface proof in the isolated `v2026.4.12` lane instead of stopping at read-only smoke checks.
- Job shape: isolated one-shot cron job, no delivery, keep-after-run for inspection, message `Reply exactly with OK and nothing else.`, model `MiniMax-M2.7`.
- Verified result:
  - manual run enqueued cleanly
  - run history recorded `status: ok`
  - summary was `OK`
  - delivery stayed `not-delivered`
  - duration was about `15.6s`
  - fresh rehearsal cron session keys appeared in the isolated session store
- Cleanup: removed the disposable job after verification.
- Decision: rehearsal is now good enough to stop expanding and move to a narrow live upgrade window plan.
- Artifact: wrote `projects/_ops/openclaw-v2026.4.12-live-upgrade-window-plan-2026-04-16.md`.

## Self-Reflection [01:05 GMT+7]
CONTEXT: isolated runtime rehearsal / preview wiring
REFLECTION: the first attempt failed because I treated preview stop/restart as a harmless helper path, but the script was using broad process-kill patterns that were incompatible with parallel preview lanes. The QMD migration check also proved that `memory status` alone is not enough; the first real search matters.
LESSON: when validating a rehearsal upgrade, include one bounded write-surface proof and one real `memory search`, not just health/status endpoints.

## Nightly Memory Extraction [2026-04-16 02:00 GMT+8]

**Session coverage:** main session `6b92c06f` (18:29–02:14 GMT+8, ~1157 messages) + current cron session.

**Durable facts extracted:**

Entity updates applied:
- `life/projects/mission-control.md`: Added 6 new Apr 15–16 facts — VPS rollback rebuild, HTTPS/TTS/STT deferral, OpenClaw v2026.4.12 upgrade audit, isolated rehearsal lane smoke-test pass, QMD collection mismatch migration risk, preview-stop.sh pkill isolation bug fix; updated summary section.
- `life/projects/marvin-config.md`: Added QMD collection mismatch risk (legacy `memory` vs `memory-dir-main` expected by v2026.4.12); updated summary.

Key durable facts from today:
- Second VPS rollback (Apr 15 evening): git truth corrupted; workspace truth used as canonical baseline; Mission Control preview rebuilt from current workspace; local preview healthy; host-side HTTPS still misrouted (intentionally deferred to live build phase).
- HTTPS/TTS/STT deferred: microphone capture requires secure browser context; visible in Mission Control composer; Philippe deferred HTTPS work to live build phase.
- OpenClaw v2026.4.12 upgrade path: audit doc at `projects/_ops/openclaw-v2026.4.12-upgrade-audit-2026-04-16.md`; recommendation is isolated rehearsal upgrade only, not direct live jump; Dreaming and Active Memory stay disabled during first rehearsal.
- Isolated v2026.4.12 rehearsal lane verified (Apr 16 early morning): separate gateway on :19001, MC preview on 3015/3016/3017, Telegram disabled; all smoke-test items passed.
- QMD collection mismatch is a real live-migration risk: legacy `memory` collection vs expected `memory-dir-main`; must clean collection before live upgrade.
- preview-stop.sh had broad `pkill` patterns incompatible with parallel preview lanes; fixed with PID-file-backed process group targeting.

No new life/people/companies/entity facts requiring extraction today.

## Live window checklist + feature delta [02:24 GMT+7]
- What changed: converted the live upgrade plan into an exact minute-by-minute execution checklist for the approved `13:00-13:40 Asia/Ho_Chi_Minh` window.
- Checklist artifact: `projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md`
- Key structure:
  - freeze + baseline snapshot
  - host-side global npm install to `openclaw@2026.4.12`
  - host-side restart only
  - immediate version/health proof
  - explicit QMD `memory search` proof as a hard gate
  - cron/session proof
  - Mission Control HTTP proof
  - one bounded live chat proof
  - explicit rollback lane back to `2026.3.8`
- Additional artifact: wrote a curated feature summary at `projects/_ops/openclaw-v2026.4.12-feature-delta-2026-04-16.md`, ordered by what is actually interesting for this setup rather than by raw changelog order.
- Main upgrade-story takeaway: the strongest reasons to upgrade are Active Memory, durable Task Flow, first-class Codex provider support, much better Dreaming/memory-wiki, richer media generation, and better local/self-hosted model/provider surfaces.

## Operator script + expanded March→April feature ranking [02:36 GMT+7]
- What changed: compressed the live window into a short operator script for the actual afternoon execution and expanded the feature-delta ranking to cover everything after `2026.3.8`, not just April.
- New artifact: `projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md`
- Operator-script posture: short host-side sequence with exact gates for version, gateway health, real QMD `memory search`, cron/sessions, Mission Control HTTP checks, bounded live chat proof, and immediate rollback commands.
- Updated artifact: `projects/_ops/openclaw-v2026.4.12-feature-delta-2026-04-16.md`
- Expanded ranking insight: the March releases added several genuinely important items that belong in the story, especially dashboard v2 / stronger Control UI, fast mode, real signed-in Chrome attach + browser profiles, provider-plugin + Ollama/local-model improvements, multimodal memory indexing, ACP resume/bridge continuity, and better cron session targeting.
- Bottom-line upgrade story after expansion: the meaningful arc is not just April polish; it is a larger March+April jump in orchestration, operator UX, memory, browser automation, and local-model/provider support.

## Wrap-up / handoff for tomorrow morning [02:45 GMT+7]
- State at wrap:
  - live OpenClaw lane still healthy on the current runtime
  - live Mission Control preview healthy on `3005/3006/3007`
  - rehearsal OpenClaw `v2026.4.12` lane healthy on `19001`
  - rehearsal Mission Control preview healthy on `3015/3016/3017`
- User decision: stop for the night and continue tomorrow morning.
- Planned next execution block already chosen: live upgrade window target remains `13:00-13:40 Asia/Ho_Chi_Minh` with the short operator script and full checklist prepared.
- Important open note for tomorrow: Control UI / Mission Control occasionally surfaced fallback-model chatter even while runtime status still showed active `gpt-5.4`; logged as a pending operational readout issue, but no proof yet of a real runtime model drop.
- No additional core-doc changes needed tonight beyond the runbooks/operator script/feature summary already written.

## Self-Reflection [02:45 GMT+7]
CONTEXT: end-of-session closeout after rehearsal, runbooks, and afternoon live-window prep
REFLECTION: the actual technical state is now in much better shape than the emotional state of the session felt during the preview 502 and fallback chatter. Writing the operator script and trimming the scope back to one afternoon runtime window was the right way to stop the sprawl.
LESSON: after a messy multi-step rehearsal, end with a short executable runbook and one clear next window instead of letting unresolved UI noise keep the whole plan feeling unstable.

## Workspace Home-Improvement — TOOLS.md runtime truth refresh [08:22 GMT+7]
- What changed: updated `TOOLS.md` to reflect the current Mission Control / rehearsal runtime truth instead of older partial notes. Added the current Next.js `16.2.0` + React `19.2.0` stack, documented the PID-file-backed `preview-stop.sh` safety behavior, added the `v2026.4.12` isolated rehearsal posture and approved live-window note, and documented the QMD `memory` vs `memory-dir-main` migration risk plus the need for a real `memory search` preflight.
- Why: the self-improvement review and overnight upgrade work surfaced documentation drift that could mislead the live upgrade window or future preview-lane work.
- Expected benefit: lower operator confusion during the `v2026.4.12` upgrade window, less risk of sibling preview lanes being killed accidentally, and cleaner core-doc alignment with the actual Mission Control stack.
- Rollback: `git checkout -- TOOLS.md memory/2026-04-16.md`

## Workspace Autonomy Executor [09:00 GMT+7]
- What changed: ran `python3 scripts/autonomy_gate.py workspace`, received `decision: run`, then ran `python3 scripts/autonomous-task-executor.py` once as scheduled.
- Result: executor selected `[Other] Analyze: OpenClaw loop-pattern audit`, found no direct executor for that task type, and wrote `memory/executor-last-result.json` with outcome `queued_for_subagent` and details `Needs sub-agent or manual routing`.
- Notification handling: no Telegram message was sent to `goal-tasks` because reminder rules suppress notifications for `queued_for_subagent` outcomes.
- Rollback: none needed; this was a scheduled executor pass plus result logging.

## workspace-home-improvement pass [09:15]
**What changed:** Added `--dry-run` flag to `projects/market-intel/scripts/save_signal_review.py`
**Why:** The script had no way to preview a review before writing — operators had to run blind or manually comment-out save lines. Dry-run shows the exact payload that would be written and exits 0 without touching any files.
**Expected benefit:** Safer signal review authoring; prevents accidental writes during workflow testing and makes the review contract visible before commit.
**Rollback:** `git checkout projects/market-intel/scripts/save_signal_review.py`
**Verification:** syntax OK; `--help` shows flag; smoke test with `--dry-run` confirmed zero file mutations; tracked_signals.json unchanged after dry-run vs. backup diff.

## Live-upgrade Docker path clarification [12:58 GMT+7]
- What changed: updated `projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md`, `projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md`, and `TOOLS.md` so the upgrade runbook matches the real Hostinger Docker setup instead of reading like a bare-host install.
- Specific edits: added the live container name `openclaw-ktrt-openclaw-1`, documented the standard host-side `docker exec` entry forms for root and `node`, switched the upgrade/rollback install steps to `docker exec ... npm install -g ...`, and switched the restart guidance to the host-side container restart path instead of `openclaw gateway restart` from inside the container.
- Why: Philippe flagged a real confusion risk before the cautious upgrade window. The docs already had the right intent about host-side control, but the concrete commands were too easy to read as non-Docker host commands.
- Expected benefit: lower chance of running the right command in the wrong place during the live window, cleaner operator handoff, and less ambiguity about when to use root vs `node`.
- Rollback: `git checkout -- TOOLS.md projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md memory/2026-04-16.md`

## Live-upgrade triage note added [13:14 GMT+7]
- What changed: appended a short deployment-specific triage note to `projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md` so the operator checklist itself now highlights the only failure classes that really matter for this Hostinger Docker lane.
- Focus: right container namespace, install-path sanity, real `memory search` proof, Mission Control route/history/chat proof, and strict no-scope-creep during the live window.
- Why: the Reddit-style generic breakage list is useful as background, but too noisy for a real cutover. The checklist now points attention at the actual kill switches for this deployment.
- Rollback: `git checkout -- projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md memory/2026-04-16.md`

## Live upgrade failure + VPS rollback report [13:40 GMT+7]
- Philippe reported the real live `v2026.4.12` attempt failed and the VPS was rolled back afterward.
- Reported upgrade behavior:
  - `docker exec -i openclaw-ktrt-openclaw-1 bash -lc 'npm install -g openclaw@2026.4.12'` completed with npm deprecation warnings only; those warnings were noisy but not the core failure.
  - after container restart, Gateway Dashboard UI appeared updated, but post-upgrade validation failed hard enough to trigger rollback.
- Reported hard failures during/after validation:
  - `openclaw --version` still read `2026.3.8` instead of `2026.4.12`
  - CLI surfaces like `status`, `memory`, `cron`, and `sessions` failed on config validation because `channels.telegram.streaming` with current value `{"mode":"off"}` was treated as invalid under the attempted runtime
  - Mission Control preview on `:3005` was down during the post-restart proof window
  - later rollback/login attempts reached a Hostinger/OpenClaw boot screen where OpenClaw logged plugin requirement skips (`telegram`, `whatsapp`, `oxylabs-ai-studio-openclaw`) and then exited with code `1` before device approval could complete
- Operational outcome: Philippe executed the pinned rollback commands, but the environment did not return to a comfortably usable state from the UI path, so a full VPS rollback was used as the final recovery path.
- Immediate lesson: for future live attempts, do a target-version config/schema preflight first, especially around Telegram config keys like `channels.telegram.streaming`; do not trust Dashboard/UI vibes as the version gate; treat preview restore as an explicit step after gateway restart rather than assuming it will just come back on its own.

## Retry-preflight hardening + preview recovery [14:07 GMT+7]
- What changed:
  - restored Mission Control preview with the hardened helper path (`projects/mission-control/scripts/preview-restart.sh`) and re-verified `3005` core pages + runtime-bridge history.
  - created `projects/_ops/scripts/openclaw_retry_preflight.py` to gate future live retries on a proven rollback-compatibility check.
  - added `projects/_ops/openclaw-v2026.4.12-retry-preflight-2026-04-16.md` documenting the exact Telegram config trap and the corrected rollback sequence.
  - patched both live runbooks (`...live-execution-checklist...` and `...live-operator-script...`) to require raw config backup + retry-preflight pass before mutation, and to restore the raw config backup during rollback before restarting `2026.3.8`.
- Proven finding from temp-config simulation:
  - `2026.3.8` accepts `channels.telegram.streaming: "off"`.
  - `2026.4.12` accepts legacy scalar and normalizes to nested `{"mode":"off"}`.
  - `2026.3.8` rejects that nested shape.
  - therefore rollback can fail unless the raw pre-upgrade config is restored before any `2026.3.8` restart.
- Validation evidence:
  - preflight script fails without rollback-backup.
  - preflight script passes with a byte-identical backup of `/data/.openclaw/openclaw.json`.
- Rollback for this doc/script set: `git checkout -- projects/_ops/scripts/openclaw_retry_preflight.py projects/_ops/openclaw-v2026.4.12-retry-preflight-2026-04-16.md projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md memory/2026-04-16.md`

## Retry-preflight package committed [14:29 GMT+7]
- What changed: finalized and committed the safer live-retry guardrails as workspace commit `6fbeff1` (`Add safer preflight for OpenClaw retry upgrade`).
- Included artifacts:
  - `projects/_ops/scripts/openclaw_retry_preflight.py`
  - `projects/_ops/openclaw-v2026.4.12-retry-preflight-2026-04-16.md`
  - patched live operator/checklist runbooks
  - refined `.learnings/errors.md` entry for the Apr 16 failed live upgrade
- Verified behavior after commit:
  - `python3 -m py_compile projects/_ops/scripts/openclaw_retry_preflight.py` passed
  - preflight fails when `--rollback-backup` is missing
  - preflight passes when given a byte-identical backup of `/data/.openclaw/openclaw.json`
  - passing output explicitly requires restoring the saved raw config backup before any `2026.3.8` restart
- Durable operator rule from this point: before any future live `v2026.4.12` retry, first save a raw copy of `/data/.openclaw/openclaw.json`, then run the retry-preflight script against that backup; if it does not end with `PRECHECK RESULT: PASS`, do not touch the live lane.

## Bootstrap truncation fix applied [14:55 GMT+7]
- What changed: patched live config `/data/.openclaw/openclaw.json` to set `agents.defaults.bootstrapMaxChars: 50000` and `agents.defaults.bootstrapPromptTruncationWarning: "once"`.
- Why: Control UI showed repeated bootstrap truncation warnings because `TOOLS.md` (`45501` chars) and `MEMORY.md` (`37142` chars) exceeded the default per-file bootstrap limit (`20000`), while total bootstrap size stayed well below the default total cap.
- Verification:
  - computed current bootstrap-file total at `98578` chars, so no `bootstrapTotalMaxChars` increase was needed
  - `openclaw config validate` passed after the edit
  - `openclaw status --json` still succeeded after the change
- Safety/rollback:
  - pre-edit backup saved to `/tmp/openclaw.json.bootstrap-pre-2026-04-16-1555`
  - rollback command: `cp /tmp/openclaw.json.bootstrap-pre-2026-04-16-1555 /data/.openclaw/openclaw.json`
- Runtime expectation: existing warning messages already shown in chat will remain, but new bootstrap passes should no longer truncate `TOOLS.md` / `MEMORY.md` on the default per-file budget.

## Live retry gate corrected before second attempt [15:20 GMT+7]
- What surfaced from Philippe's pasted preflight output: baseline version/install-path/HTTP checks were good, but the saved memory gate was misleading because the node-shell PATH did not include the local QMD/Bun wrappers.
- Actual blocker found during direct validation:
  - live agent QMD YAML still named the daily-memory collection `memory`
  - the agent QMD SQLite already contained `memory-dir-main`
  - target `v2026.4.12` CLI search failed because it expected `memory-dir-main` in managed config, saw legacy `memory` instead, and treated the collection as missing
- Safe repair applied:
  - backed up `/data/.openclaw/agents/main/qmd/xdg-config/qmd/index.yml` and `.../xdg-cache/qmd/index.sqlite` into `projects/_ops/logs/live-upgrade-2026-04-16-1300/qmd-backup/`
  - updated the live agent QMD YAML key from `memory` to `memory-dir-main` (DB already had matching docs)
  - verified direct `qmd search Philippe -c memory-dir-main --json` works with agent XDG env
  - verified target rehearsal CLI `/data/.openclaw/rehearsals/v2026.4.12/cli/node_modules/.bin/openclaw memory search ... --json` now returns real results
- Runbook correction: patched `projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md` so the live window now exports `PATH=/data/.local/bin:/data/bin:/data/.bun/bin:$PATH`, captures `command -v qmd` / `command -v bun`, and uses the target `v2026.4.12` CLI search as the real pre-mutation memory gate.

## Live upgrade succeeded with path-split cleanup note [15:52 GMT+7]
- Outcome: live OpenClaw lane is now running `2026.4.12` and passed the core post-upgrade proof after Mission Control preview was restarted.
- Verified passes:
  - runtime status reports `"runtimeVersion": "2026.4.12"`
  - `openclaw memory search --query "Philippe" --max-results 3 --json` returned real results when run with the active runtime PATH (`/data/.npm-global/bin` first)
  - Mission Control HTTP checks returned `200` for `/general/home`, `/general/chat`, `/general/agents`, `/general/crons`, and runtime-bridge history
- Important post-upgrade wrinkle:
  - container runtime processes (`openclaw`, `openclaw-gateway`) are launching with PATH beginning `/data/.npm-global/bin`, so the live service is using the newly installed `2026.4.12` CLI from `/data/.npm-global/...`
  - `/usr/local/bin/openclaw` still points at the old `/usr/local/lib/node_modules/openclaw` install (`2026.3.8`)
  - consequence: some shells can still resolve the stale CLI unless PATH is normalized or `/usr/local` is updated to the same version
- Immediate repair during validation: Mission Control preview on `3005` was down after the container restart because the preview runtime did not auto-come-back with the app container; rebuilt/restarted it with `projects/mission-control/scripts/preview-restart.sh`, after which the core routes returned `200` again.
- Operational takeaway: future live install commands must verify where `npm -g` is actually writing inside the container and must include an explicit install-path sanity check after upgrade, otherwise the runtime can succeed while `/usr/local/bin/openclaw` stays stale.


## Mission Control post-upgrade follow-up, Crons tab fix, and Chat bridge recheck [18:25 GMT+7]
- User-reported follow-up after the live `2026.4.12` upgrade:
  - OpenClaw Control surfaced leaked/raw system messages containing internal exec completion payloads and log fragments; treat this as a separate Control-UI bug, not a Mission Control preview issue.
  - Mission Control Skills page and most of Crons recovered after the PATH hardening + preview restart.
  - Mission Control Chat symptoms narrowed from total bridge failure to intermittent stale live updates: hard refresh briefly showed current tool activity, then the page later stopped reflecting new live work.
- Confirmed live state after rebuilding/restarting Mission Control preview:
  - `/general/skills` no longer renders the invalid-config fallback.
  - `/api/skills` returns `200` with clean skill data.
  - `/api/cron/jobs` returns populated cron data (`Jobs (24)` on the rendered page).
- Concrete UI bug fixed:
  - `projects/mission-control/components/pages/GeneralCronsPage.tsx` was reading `searchParams` as a plain object.
  - Under Next.js 16 this caused `?tab=runs` to be ignored, so `Recent runs` looked clickable but still rendered the Jobs tab.
  - Patched the page to await/resolve async `searchParams` before computing the active tab, then rebuilt and restarted preview.
  - Verification after restart: `/general/crons` renders schedule-heavy Jobs content, while `/general/crons?tab=runs` now renders the runs-table view instead of the Jobs view.
- Chat bridge recheck after gateway restart:
  - direct proxy-level probes against `ws://127.0.0.1:3005/api/runtime-bridge/ws?bridgeToken=...` can complete the full handshake (`connect.challenge` -> `connect` -> `hello-ok`) and successfully stream live `chat.send` / `agent` events through the Mission Control preview path.
  - a 40-second idle connected probe stayed healthy, received periodic `health` and `tick` events, and continued receiving live `agent` events, which means the current preview-side transport path is healthy at the protocol level after restart.
- Current interpretation:
  - the earlier hard Mission Control Chat failure is no longer reproducible from the live proxy path after the latest restart.
  - if Chat still appears stale in the browser, the remaining problem is likely browser-side session/UI state or a cached client bundle rather than the preview transport itself.

- Additional user confirmation later the same evening: OpenClaw Control still surfaced raw exec-completion/system payloads in the chat area, including internal probe summaries from the Mission Control bridge checks (`delta-basil`, `plaid-meadow`). This confirms the leak is reproducible after the gateway restart and is not explained by stale pre-restart UI noise.
- User also reported Mission Control Chat looked somewhat healthier: latest assistant message arrived normally, but tool/live activity still sometimes required a hard refresh to appear. Current posture remains: transport path healthy after restart, but browser-side live-update staleness may still exist intermittently.
# Daily Memory — 2026-04-16

## Mission Control rollback-baseline recovery + OpenClaw upgrade audit [23:23 GMT+7]
- What changed: treated the rolled-back workspace as canonical truth, rebuilt the Mission Control preview from that state, verified the local preview route is healthy, cleaned the nested `projects/mission-control` git state so generated/runtime junk is no longer tracked, and completed a full upstream OpenClaw audit from `2026.3.8` to `v2026.4.12`.
- Verified runtime truth: local Mission Control preview is healthy on `/general/chat`; public preview HTTP still routes into the app, but preview HTTPS is currently misrouted to default nginx / `404`, and that host-side HTTPS issue was intentionally deferred until the later live-build phase.
- Git-state result: the nested Mission Control repo is now re-anchored to the current steady workspace source tree instead of polluted build/runtime output, with the preview env example kept tracked while `.next`, `node_modules`, and preview-runtime junk are ignored.
- Upgrade recommendation: target **OpenClaw `v2026.4.12`** only as an **isolated rehearsal upgrade**, not a direct live jump.
- Key reasoning: April brings meaningful QMD, isolated-session/cron, webchat/control-surface, and security-hardening improvements, but the current deployment still depends on dangerous Control UI compatibility flags (`allowInsecureAuth`, `dangerouslyDisableDeviceAuth`, `dangerouslyAllowHostHeaderOriginFallback`), so combining the core upgrade with HTTPS/proxy hardening or auth cleanup would be unnecessarily risky.
- Explicit first-pass rule: keep **Dreaming** and **Active Memory** disabled during the first rehearsal; prove base runtime compatibility first, then test new memory features separately.
- Artifact: wrote `projects/_ops/openclaw-v2026.4.12-upgrade-audit-2026-04-16.md` and committed it in workspace git as `5ff1634` (`Add OpenClaw v2026.4.12 upgrade audit`).

## OpenClaw rehearsal execution plan [23:35 GMT+7]
- What changed: turned the `v2026.4.12` audit into an execution-grade rehearsal runbook at `projects/_ops/openclaw-v2026.4.12-rehearsal-execution-plan-2026-04-16.md`, with a concrete smoke-test matrix for gateway startup, Mission Control core pages, runtime bridge/history, QMD memory health, Telegram, cron, and sessions.
- Key planning decision: the first rehearsal should run in an isolated `rehearsal-412` lane with a separate gateway target and Mission Control preview env, while keeping Dreaming, Active Memory, HTTPS/proxy hardening, and Control UI security cleanup out of scope.
- Important operational note: remote HTTPS remains a separate prerequisite for STT and secure browser-context features, but it does not block the base upgrade rehearsal as long as the browser-facing origin question is kept separate from the runtime-validation lane.
- Critical Telegram rule: do not let a rehearsal runtime compete with the live runtime for the same Telegram bot lane; use a secondary bot or a short exclusive ownership window for any real routing test.
- Expected benefit: the next OpenClaw upgrade step can be judged from a repeatable proof loop instead of a vague “update and poke around” session.

## Rehearsal lane bring-up + smoke pass [00:48 GMT+7]
- What changed: built the isolated `v2026.4.12` rehearsal lane for real instead of leaving it as a plan.
- Rehearsal runtime facts:
  - CLI install: `/data/.openclaw/rehearsals/v2026.4.12/cli`
  - state root: `/data/.openclaw/rehearsals/v2026.4.12/state`
  - gateway: `ws://127.0.0.1:19001`
  - Mission Control rehearsal preview: `3015/3016/3017`
  - live Mission Control preview preserved on `3005/3006/3007`
  - Telegram disabled in rehearsal config
  - Dreaming / Active Memory kept disabled
- Verified passes:
  - rehearsal config validated cleanly
  - rehearsal gateway health returned `{"ok":true,"status":"live"}`
  - rehearsal status showed reachable gateway metadata on `19001`
  - rehearsal Mission Control routes returned `200` for Home, Chat, Agents, Crons, Tasks, Memory, and Files
  - rehearsal runtime-bridge history returned `200` for `agent:main:main`
  - rehearsal cron status/list succeeded
  - rehearsal sessions registry responded
  - rehearsal memory backend reported QMD once local toolchain PATH was corrected
  - rehearsal `memory search "Philippe" --max-results 3 --json` returned results after QMD collection cleanup in isolated state
- Important failure discovered and fixed in workspace source: `projects/mission-control/scripts/preview-stop.sh` used broad `pkill` patterns (`next-server`, sidecar, proxy), so a rehearsal stop/restart could kill the normal preview too. Fixed it to stop only the PID-file-backed process group for the selected runtime dir.
- Important rehearsal-only compatibility wrinkle: the isolated QMD state carried a legacy `memory` collection while `v2026.4.12` search expected `memory-dir-main`; removing the legacy collection inside the isolated rehearsal state allowed managed search to succeed. This should be treated as a real migration risk to document before any live upgrade.
- Minor note: one QMD boot-time embed pass timed out during search, but the search still completed and returned results.

## Disposable rehearsal cron run [01:05 GMT+7]
- What changed: executed one inert write-surface proof in the isolated `v2026.4.12` lane instead of stopping at read-only smoke checks.
- Job shape: isolated one-shot cron job, no delivery, keep-after-run for inspection, message `Reply exactly with OK and nothing else.`, model `MiniMax-M2.7`.
- Verified result:
  - manual run enqueued cleanly
  - run history recorded `status: ok`
  - summary was `OK`
  - delivery stayed `not-delivered`
  - duration was about `15.6s`
  - fresh rehearsal cron session keys appeared in the isolated session store
- Cleanup: removed the disposable job after verification.
- Decision: rehearsal is now good enough to stop expanding and move to a narrow live upgrade window plan.
- Artifact: wrote `projects/_ops/openclaw-v2026.4.12-live-upgrade-window-plan-2026-04-16.md`.

## Self-Reflection [01:05 GMT+7]
CONTEXT: isolated runtime rehearsal / preview wiring
REFLECTION: the first attempt failed because I treated preview stop/restart as a harmless helper path, but the script was using broad process-kill patterns that were incompatible with parallel preview lanes. The QMD migration check also proved that `memory status` alone is not enough; the first real search matters.
LESSON: when validating a rehearsal upgrade, include one bounded write-surface proof and one real `memory search`, not just health/status endpoints.

## Nightly Memory Extraction [2026-04-16 02:00 GMT+8]

**Session coverage:** main session `6b92c06f` (18:29–02:14 GMT+8, ~1157 messages) + current cron session.

**Durable facts extracted:**

Entity updates applied:
- `life/projects/mission-control.md`: Added 6 new Apr 15–16 facts — VPS rollback rebuild, HTTPS/TTS/STT deferral, OpenClaw v2026.4.12 upgrade audit, isolated rehearsal lane smoke-test pass, QMD collection mismatch migration risk, preview-stop.sh pkill isolation bug fix; updated summary section.
- `life/projects/marvin-config.md`: Added QMD collection mismatch risk (legacy `memory` vs `memory-dir-main` expected by v2026.4.12); updated summary.

Key durable facts from today:
- Second VPS rollback (Apr 15 evening): git truth corrupted; workspace truth used as canonical baseline; Mission Control preview rebuilt from current workspace; local preview healthy; host-side HTTPS still misrouted (intentionally deferred to live build phase).
- HTTPS/TTS/STT deferred: microphone capture requires secure browser context; visible in Mission Control composer; Philippe deferred HTTPS work to live build phase.
- OpenClaw v2026.4.12 upgrade path: audit doc at `projects/_ops/openclaw-v2026.4.12-upgrade-audit-2026-04-16.md`; recommendation is isolated rehearsal upgrade only, not direct live jump; Dreaming and Active Memory stay disabled during first rehearsal.
- Isolated v2026.4.12 rehearsal lane verified (Apr 16 early morning): separate gateway on :19001, MC preview on 3015/3016/3017, Telegram disabled; all smoke-test items passed.
- QMD collection mismatch is a real live-migration risk: legacy `memory` collection vs expected `memory-dir-main`; must clean collection before live upgrade.
- preview-stop.sh had broad `pkill` patterns incompatible with parallel preview lanes; fixed with PID-file-backed process group targeting.

No new life/people/companies/entity facts requiring extraction today.

## Live window checklist + feature delta [02:24 GMT+7]
- What changed: converted the live upgrade plan into an exact minute-by-minute execution checklist for the approved `13:00-13:40 Asia/Ho_Chi_Minh` window.
- Checklist artifact: `projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md`
- Key structure:
  - freeze + baseline snapshot
  - host-side global npm install to `openclaw@2026.4.12`
  - host-side restart only
  - immediate version/health proof
  - explicit QMD `memory search` proof as a hard gate
  - cron/session proof
  - Mission Control HTTP proof
  - one bounded live chat proof
  - explicit rollback lane back to `2026.3.8`
- Additional artifact: wrote a curated feature summary at `projects/_ops/openclaw-v2026.4.12-feature-delta-2026-04-16.md`, ordered by what is actually interesting for this setup rather than by raw changelog order.
- Main upgrade-story takeaway: the strongest reasons to upgrade are Active Memory, durable Task Flow, first-class Codex provider support, much better Dreaming/memory-wiki, richer media generation, and better local/self-hosted model/provider surfaces.

## Operator script + expanded March→April feature ranking [02:36 GMT+7]
- What changed: compressed the live window into a short operator script for the actual afternoon execution and expanded the feature-delta ranking to cover everything after `2026.3.8`, not just April.
- New artifact: `projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md`
- Operator-script posture: short host-side sequence with exact gates for version, gateway health, real QMD `memory search`, cron/sessions, Mission Control HTTP checks, bounded live chat proof, and immediate rollback commands.
- Updated artifact: `projects/_ops/openclaw-v2026.4.12-feature-delta-2026-04-16.md`
- Expanded ranking insight: the March releases added several genuinely important items that belong in the story, especially dashboard v2 / stronger Control UI, fast mode, real signed-in Chrome attach + browser profiles, provider-plugin + Ollama/local-model improvements, multimodal memory indexing, ACP resume/bridge continuity, and better cron session targeting.
- Bottom-line upgrade story after expansion: the meaningful arc is not just April polish; it is a larger March+April jump in orchestration, operator UX, memory, browser automation, and local-model/provider support.

## Wrap-up / handoff for tomorrow morning [02:45 GMT+7]
- State at wrap:
  - live OpenClaw lane still healthy on the current runtime
  - live Mission Control preview healthy on `3005/3006/3007`
  - rehearsal OpenClaw `v2026.4.12` lane healthy on `19001`
  - rehearsal Mission Control preview healthy on `3015/3016/3017`
- User decision: stop for the night and continue tomorrow morning.
- Planned next execution block already chosen: live upgrade window target remains `13:00-13:40 Asia/Ho_Chi_Minh` with the short operator script and full checklist prepared.
- Important open note for tomorrow: Control UI / Mission Control occasionally surfaced fallback-model chatter even while runtime status still showed active `gpt-5.4`; logged as a pending operational readout issue, but no proof yet of a real runtime model drop.
- No additional core-doc changes needed tonight beyond the runbooks/operator script/feature summary already written.

## Self-Reflection [02:45 GMT+7]
CONTEXT: end-of-session closeout after rehearsal, runbooks, and afternoon live-window prep
REFLECTION: the actual technical state is now in much better shape than the emotional state of the session felt during the preview 502 and fallback chatter. Writing the operator script and trimming the scope back to one afternoon runtime window was the right way to stop the sprawl.
LESSON: after a messy multi-step rehearsal, end with a short executable runbook and one clear next window instead of letting unresolved UI noise keep the whole plan feeling unstable.

## Workspace Home-Improvement — TOOLS.md runtime truth refresh [08:22 GMT+7]
- What changed: updated `TOOLS.md` to reflect the current Mission Control / rehearsal runtime truth instead of older partial notes. Added the current Next.js `16.2.0` + React `19.2.0` stack, documented the PID-file-backed `preview-stop.sh` safety behavior, added the `v2026.4.12` isolated rehearsal posture and approved live-window note, and documented the QMD `memory` vs `memory-dir-main` migration risk plus the need for a real `memory search` preflight.
- Why: the self-improvement review and overnight upgrade work surfaced documentation drift that could mislead the live upgrade window or future preview-lane work.
- Expected benefit: lower operator confusion during the `v2026.4.12` upgrade window, less risk of sibling preview lanes being killed accidentally, and cleaner core-doc alignment with the actual Mission Control stack.
- Rollback: `git checkout -- TOOLS.md memory/2026-04-16.md`

## Workspace Autonomy Executor [09:00 GMT+7]
- What changed: ran `python3 scripts/autonomy_gate.py workspace`, received `decision: run`, then ran `python3 scripts/autonomous-task-executor.py` once as scheduled.
- Result: executor selected `[Other] Analyze: OpenClaw loop-pattern audit`, found no direct executor for that task type, and wrote `memory/executor-last-result.json` with outcome `queued_for_subagent` and details `Needs sub-agent or manual routing`.
- Notification handling: no Telegram message was sent to `goal-tasks` because reminder rules suppress notifications for `queued_for_subagent` outcomes.
- Rollback: none needed; this was a scheduled executor pass plus result logging.

## workspace-home-improvement pass [09:15]
**What changed:** Added `--dry-run` flag to `projects/market-intel/scripts/save_signal_review.py`
**Why:** The script had no way to preview a review before writing — operators had to run blind or manually comment-out save lines. Dry-run shows the exact payload that would be written and exits 0 without touching any files.
**Expected benefit:** Safer signal review authoring; prevents accidental writes during workflow testing and makes the review contract visible before commit.
**Rollback:** `git checkout projects/market-intel/scripts/save_signal_review.py`
**Verification:** syntax OK; `--help` shows flag; smoke test with `--dry-run` confirmed zero file mutations; tracked_signals.json unchanged after dry-run vs. backup diff.

## Live-upgrade Docker path clarification [12:58 GMT+7]
- What changed: updated `projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md`, `projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md`, and `TOOLS.md` so the upgrade runbook matches the real Hostinger Docker setup instead of reading like a bare-host install.
- Specific edits: added the live container name `openclaw-ktrt-openclaw-1`, documented the standard host-side `docker exec` entry forms for root and `node`, switched the upgrade/rollback install steps to `docker exec ... npm install -g ...`, and switched the restart guidance to the host-side container restart path instead of `openclaw gateway restart` from inside the container.
- Why: Philippe flagged a real confusion risk before the cautious upgrade window. The docs already had the right intent about host-side control, but the concrete commands were too easy to read as non-Docker host commands.
- Expected benefit: lower chance of running the right command in the wrong place during the live window, cleaner operator handoff, and less ambiguity about when to use root vs `node`.
- Rollback: `git checkout -- TOOLS.md projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md memory/2026-04-16.md`

## Live-upgrade triage note added [13:14 GMT+7]
- What changed: appended a short deployment-specific triage note to `projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md` so the operator checklist itself now highlights the only failure classes that really matter for this Hostinger Docker lane.
- Focus: right container namespace, install-path sanity, real `memory search` proof, Mission Control route/history/chat proof, and strict no-scope-creep during the live window.
- Why: the Reddit-style generic breakage list is useful as background, but too noisy for a real cutover. The checklist now points attention at the actual kill switches for this deployment.
- Rollback: `git checkout -- projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md memory/2026-04-16.md`

## Live upgrade failure + VPS rollback report [13:40 GMT+7]
- Philippe reported the real live `v2026.4.12` attempt failed and the VPS was rolled back afterward.
- Reported upgrade behavior:
  - `docker exec -i openclaw-ktrt-openclaw-1 bash -lc 'npm install -g openclaw@2026.4.12'` completed with npm deprecation warnings only; those warnings were noisy but not the core failure.
  - after container restart, Gateway Dashboard UI appeared updated, but post-upgrade validation failed hard enough to trigger rollback.
- Reported hard failures during/after validation:
  - `openclaw --version` still read `2026.3.8` instead of `2026.4.12`
  - CLI surfaces like `status`, `memory`, `cron`, and `sessions` failed on config validation because `channels.telegram.streaming` with current value `{"mode":"off"}` was treated as invalid under the attempted runtime
  - Mission Control preview on `:3005` was down during the post-restart proof window
  - later rollback/login attempts reached a Hostinger/OpenClaw boot screen where OpenClaw logged plugin requirement skips (`telegram`, `whatsapp`, `oxylabs-ai-studio-openclaw`) and then exited with code `1` before device approval could complete
- Operational outcome: Philippe executed the pinned rollback commands, but the environment did not return to a comfortably usable state from the UI path, so a full VPS rollback was used as the final recovery path.
- Immediate lesson: for future live attempts, do a target-version config/schema preflight first, especially around Telegram config keys like `channels.telegram.streaming`; do not trust Dashboard/UI vibes as the version gate; treat preview restore as an explicit step after gateway restart rather than assuming it will just come back on its own.

## Retry-preflight hardening + preview recovery [14:07 GMT+7]
- What changed:
  - restored Mission Control preview with the hardened helper path (`projects/mission-control/scripts/preview-restart.sh`) and re-verified `3005` core pages + runtime-bridge history.
  - created `projects/_ops/scripts/openclaw_retry_preflight.py` to gate future live retries on a proven rollback-compatibility check.
  - added `projects/_ops/openclaw-v2026.4.12-retry-preflight-2026-04-16.md` documenting the exact Telegram config trap and the corrected rollback sequence.
  - patched both live runbooks (`...live-execution-checklist...` and `...live-operator-script...`) to require raw config backup + retry-preflight pass before mutation, and to restore the raw config backup during rollback before restarting `2026.3.8`.
- Proven finding from temp-config simulation:
  - `2026.3.8` accepts `channels.telegram.streaming: "off"`.
  - `2026.4.12` accepts legacy scalar and normalizes to nested `{"mode":"off"}`.
  - `2026.3.8` rejects that nested shape.
  - therefore rollback can fail unless the raw pre-upgrade config is restored before any `2026.3.8` restart.
- Validation evidence:
  - preflight script fails without rollback-backup.
  - preflight script passes with a byte-identical backup of `/data/.openclaw/openclaw.json`.
- Rollback for this doc/script set: `git checkout -- projects/_ops/scripts/openclaw_retry_preflight.py projects/_ops/openclaw-v2026.4.12-retry-preflight-2026-04-16.md projects/_ops/openclaw-v2026.4.12-live-execution-checklist-2026-04-16.md projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md memory/2026-04-16.md`

## Retry-preflight package committed [14:29 GMT+7]
- What changed: finalized and committed the safer live-retry guardrails as workspace commit `6fbeff1` (`Add safer preflight for OpenClaw retry upgrade`).
- Included artifacts:
  - `projects/_ops/scripts/openclaw_retry_preflight.py`
  - `projects/_ops/openclaw-v2026.4.12-retry-preflight-2026-04-16.md`
  - patched live operator/checklist runbooks
  - refined `.learnings/errors.md` entry for the Apr 16 failed live upgrade
- Verified behavior after commit:
  - `python3 -m py_compile projects/_ops/scripts/openclaw_retry_preflight.py` passed
  - preflight fails when `--rollback-backup` is missing
  - preflight passes when given a byte-identical backup of `/data/.openclaw/openclaw.json`
  - passing output explicitly requires restoring the saved raw config backup before any `2026.3.8` restart
- Durable operator rule from this point: before any future live `v2026.4.12` retry, first save a raw copy of `/data/.openclaw/openclaw.json`, then run the retry-preflight script against that backup; if it does not end with `PRECHECK RESULT: PASS`, do not touch the live lane.

## Bootstrap truncation fix applied [14:55 GMT+7]
- What changed: patched live config `/data/.openclaw/openclaw.json` to set `agents.defaults.bootstrapMaxChars: 50000` and `agents.defaults.bootstrapPromptTruncationWarning: "once"`.
- Why: Control UI showed repeated bootstrap truncation warnings because `TOOLS.md` (`45501` chars) and `MEMORY.md` (`37142` chars) exceeded the default per-file bootstrap limit (`20000`), while total bootstrap size stayed well below the default total cap.
- Verification:
  - computed current bootstrap-file total at `98578` chars, so no `bootstrapTotalMaxChars` increase was needed
  - `openclaw config validate` passed after the edit
  - `openclaw status --json` still succeeded after the change
- Safety/rollback:
  - pre-edit backup saved to `/tmp/openclaw.json.bootstrap-pre-2026-04-16-1555`
  - rollback command: `cp /tmp/openclaw.json.bootstrap-pre-2026-04-16-1555 /data/.openclaw/openclaw.json`
- Runtime expectation: existing warning messages already shown in chat will remain, but new bootstrap passes should no longer truncate `TOOLS.md` / `MEMORY.md` on the default per-file budget.

## Live retry gate corrected before second attempt [15:20 GMT+7]
- What surfaced from Philippe's pasted preflight output: baseline version/install-path/HTTP checks were good, but the saved memory gate was misleading because the node-shell PATH did not include the local QMD/Bun wrappers.
- Actual blocker found during direct validation:
  - live agent QMD YAML still named the daily-memory collection `memory`
  - the agent QMD SQLite already contained `memory-dir-main`
  - target `v2026.4.12` CLI search failed because it expected `memory-dir-main` in managed config, saw legacy `memory` instead, and treated the collection as missing
- Safe repair applied:
  - backed up `/data/.openclaw/agents/main/qmd/xdg-config/qmd/index.yml` and `.../xdg-cache/qmd/index.sqlite` into `projects/_ops/logs/live-upgrade-2026-04-16-1300/qmd-backup/`
  - updated the live agent QMD YAML key from `memory` to `memory-dir-main` (DB already had matching docs)
  - verified direct `qmd search Philippe -c memory-dir-main --json` works with agent XDG env
  - verified target rehearsal CLI `/data/.openclaw/rehearsals/v2026.4.12/cli/node_modules/.bin/openclaw memory search ... --json` now returns real results
- Runbook correction: patched `projects/_ops/openclaw-v2026.4.12-live-operator-script-2026-04-16.md` so the live window now exports `PATH=/data/.local/bin:/data/bin:/data/.bun/bin:$PATH`, captures `command -v qmd` / `command -v bun`, and uses the target `v2026.4.12` CLI search as the real pre-mutation memory gate.

## Live upgrade succeeded with path-split cleanup note [15:52 GMT+7]
- Outcome: live OpenClaw lane is now running `2026.4.12` and passed the core post-upgrade proof after Mission Control preview was restarted.
- Verified passes:
  - runtime status reports `"runtimeVersion": "2026.4.12"`
  - `openclaw memory search --query "Philippe" --max-results 3 --json` returned real results when run with the active runtime PATH (`/data/.npm-global/bin` first)
  - Mission Control HTTP checks returned `200` for `/general/home`, `/general/chat`, `/general/agents`, `/general/crons`, and runtime-bridge history
- Important post-upgrade wrinkle:
  - container runtime processes (`openclaw`, `openclaw-gateway`) are launching with PATH beginning `/data/.npm-global/bin`, so the live service is using the newly installed `2026.4.12` CLI from `/data/.npm-global/...`
  - `/usr/local/bin/openclaw` still points at the old `/usr/local/lib/node_modules/openclaw` install (`2026.3.8`)
  - consequence: some shells can still resolve the stale CLI unless PATH is normalized or `/usr/local` is updated to the same version
- Immediate repair during validation: Mission Control preview on `3005` was down after the container restart because the preview runtime did not auto-come-back with the app container; rebuilt/restarted it with `projects/mission-control/scripts/preview-restart.sh`, after which the core routes returned `200` again.
- Operational takeaway: future live install commands must verify where `npm -g` is actually writing inside the container and must include an explicit install-path sanity check after upgrade, otherwise the runtime can succeed while `/usr/local/bin/openclaw` stays stale.


## Mission Control post-upgrade follow-up, Crons tab fix, and Chat bridge recheck [18:25 GMT+7]
- User-reported follow-up after the live `2026.4.12` upgrade:
  - OpenClaw Control surfaced leaked/raw system messages containing internal exec completion payloads and log fragments; treat this as a separate Control-UI bug, not a Mission Control preview issue.
  - Mission Control Skills page and most of Crons recovered after the PATH hardening + preview restart.
  - Mission Control Chat symptoms narrowed from total bridge failure to intermittent stale live updates: hard refresh briefly showed current tool activity, then the page later stopped reflecting new live work.
- Confirmed live state after rebuilding/restarting Mission Control preview:
  - `/general/skills` no longer renders the invalid-config fallback.
  - `/api/skills` returns `200` with clean skill data.
  - `/api/cron/jobs` returns populated cron data (`Jobs (24)` on the rendered page).
- Concrete UI bug fixed:
  - `projects/mission-control/components/pages/GeneralCronsPage.tsx` was reading `searchParams` as a plain object.
  - Under Next.js 16 this caused `?tab=runs` to be ignored, so `Recent runs` looked clickable but still rendered the Jobs tab.
  - Patched the page to await/resolve async `searchParams` before computing the active tab, then rebuilt and restarted preview.
  - Verification after restart: `/general/crons` renders schedule-heavy Jobs content, while `/general/crons?tab=runs` now renders the runs-table view instead of the Jobs view.
- Chat bridge recheck after gateway restart:
  - direct proxy-level probes against `ws://127.0.0.1:3005/api/runtime-bridge/ws?bridgeToken=...` can complete the full handshake (`connect.challenge` -> `connect` -> `hello-ok`) and successfully stream live `chat.send` / `agent` events through the Mission Control preview path.
  - a 40-second idle connected probe stayed healthy, received periodic `health` and `tick` events, and continued receiving live `agent` events, which means the current preview-side transport path is healthy at the protocol level after restart.
- Current interpretation:
  - the earlier hard Mission Control Chat failure is no longer reproducible from the live proxy path after the latest restart.
  - if Chat still appears stale in the browser, the remaining problem is likely browser-side session/UI state or a cached client bundle rather than the preview transport itself.

- Additional user confirmation later the same evening: OpenClaw Control still surfaced raw exec-completion/system payloads in the chat area, including internal probe summaries from the Mission Control bridge checks (`delta-basil`, `plaid-meadow`). This confirms the leak is reproducible after the gateway restart and is not explained by stale pre-restart UI noise.
- User also reported Mission Control Chat looked somewhat healthier: latest assistant message arrived normally, but tool/live activity still sometimes required a hard refresh to appear. Current posture remains: transport path healthy after restart, but browser-side live-update staleness may still exist intermittently.

## Shared system-event leak traced upstream [18:43 GMT+7]
- New user confirmation: the same raw `System (untrusted)` exec-completion messages appeared in both OpenClaw Control UI and Mission Control at the same time.
- Root-cause finding from installed `2026.4.12` runtime inspection:
  - background exec completions are intentionally turned into system events by the shared exec/heartbeat path (`tools.exec.notifyOnExit`, default `true`, plus the `buildExecEventPrompt()` completion handling).
  - those queued events are then formatted by the shared session-system-events layer into visible `System` / `System (untrusted)` chat lines.
- Durable interpretation:
  - this is not a Mission Control-only rendering bug.
  - the leak lives in a shared upstream system-event presentation path, so patching only Mission Control would be the wrong fix.
- Guidance for future fix direction:
  - do not blindly disable exec completion notifications just to hide the leak, because those notifications also wake the assistant when async/background commands finish.
  - the proper fix lane is to keep internal exec-completion handling while filtering or hiding those raw system-event payloads from user-visible chat surfaces.


## Mission Control live-update reconnect hardening [18:50 GMT+7]
- New user evidence: live tool/update rendering regressed again after looking temporarily better. Symptoms matched a missed-live-events pattern rather than a fully dead Chat page: running state briefly appeared, then vanished; hard refresh revealed many missed tool steps; latest assistant text could land in Control UI before Mission Control until Mission Control was hard-refreshed.
- Additional signal: the Mission Control bridge dropped for a few seconds and then reconnected.
- Strongest code-level finding in `projects/mission-control/hooks/useRuntimeBridge.ts`:
  - unexpected websocket closes with codes `1000` / `1001` were being treated as terminal/non-retry closes
  - if that happened mid-run, Mission Control could stop live-updating until a manual refresh
  - even after reconnect, the client did not force a transcript/history resync on successful handshake, so missed tool/chat events could stay invisible until a hard reload
- Patch applied:
  - retry unexpected clean websocket closes unless the session was explicitly rejected
  - force `load(true)` + `hydrateHistory(..., { force: true })` after successful reconnect/handshake so missed run events are pulled back into the transcript
- Verification: `npm run build` passed, then Mission Control preview was rebuilt/restarted successfully.
- Current interpretation: this is the best concrete Mission Control-side fix for the stale-live-updates symptom. It does not prove Control UI is fixed, but it aligns with the observed pattern of brief transport drops followed by missing live replay.

- Additional recurrence after the reconnect-hardening patch and preview restart: raw `System (untrusted)` / `do not relay` exec-completion messages still appeared in both Control UI and Mission Control for the new build/restart probe runs (`briny-lagoon`, `wild-falcon`). This separates the issues cleanly: Mission Control reconnect handling can improve independently, but the exec-completion leak remains a shared upstream presentation problem unaffected by the client reconnect patch.

## New hard-refresh evidence: transcript gap + reorder after tool completion [19:20 GMT+7]
- Philippe provided a concrete UI sequence from OpenClaw Control showing the active run still failing to hydrate/render cleanly without manual refresh.
- Observed pattern:
  - tool runs completed at `18:04-18:05`, but the visible assistant answer did not appear until after one or more hard refreshes
  - after refresh, a completed `9 steps, 9 completed` tool group appeared before Philippe's triggering message even though it was caused by that message
  - a later `6 steps, 6 completed` research batch also appeared only after refresh
- Strong interpretation: this is not just "assistant took a while to answer". The underlying tool/run data existed, but the active transcript view missed or mis-ordered it until history hydration replayed it.
- Why it matters: this is stronger evidence for a client-side transcript merge / ordering / live-hydration bug than for a transport outage. The run completed, timestamps were preserved, and refresh reconstructed the missing steps, but the live view failed to place them correctly in sequence.
- Useful repro shape going forward: user asks a question -> assistant launches multiple tools -> live UI stalls with no final answer -> hard refresh reveals completed tool groups -> subsequent refresh may reinsert those groups ahead of the triggering user message.
- Follow-up evidence at `19:25 GMT+7`: the latest chat messages landed normally without a hard refresh. That makes the bug look intermittent/on-off rather than a permanently dead live channel, which strengthens the race-condition / client reconciliation hypothesis and makes transport-wide outage less likely.
- Additional user signal at `18:27 GMT+7`: when the bug appears, it has not self-recovered within a 10-15 second wait window so far. Treat "auto-recovers shortly if left alone" as unproven/negative for the current incident, which points more toward missing replay/reconciliation than a tiny transient hiccup.

## OpenClaw toolbox + compaction check [18:20 GMT+7]
- Verified from the live `2026.4.12` docs/runtime that experimental `update_plan` is now a real built-in tool path; `tools.experimental.planTool` exists in config, and strict-agentic OpenAI/Codex GPT-5 runs can auto-enable it with system-prompt guidance to use it only for substantial multi-step work.
- Local comparison during the user follow-up check found `update_plan` documented in the `2026.4.12` install and not present in the older `2026.3.8` install/docs that were checked, so Philippe's impression was correct: this is new relative to the old toolbox he had been seeing.
- Compaction did not show a clear "much more aggressive by default" trigger change from the version docs check. Core behavior still centers on near-limit/overflow compaction, but `2026.4.12` docs now explicitly document the pre-compaction memory-flush path.
- Most likely explanation for today's heavier compaction pressure: unusually long tool-heavy debugging plus the earlier bootstrap increase to `agents.defaults.bootstrapMaxChars: 50000`. The current docs explicitly warn that large injected files such as `TOOLS.md` and `MEMORY.md` raise context usage and can cause more frequent compaction.
