# Daily Memory — 2026-04-15

## Nightly Memory Extraction [2026-04-15 02:00 GMT+8]

**Session coverage:** main session `d89dfc35` (08:30–19:14 GMT+8, 2024 messages) + short direct session `2b061464` (17:52–18:06 GMT+8, 79 messages).

**Durable facts already captured in** `memory/2026-04-14.md`:
- All Mission Control engineering work: Next 16/React 19 migration completion, WS sidecar ~5s→~15ms latency fix, Chat model/effort readback, autonomous task execute-path repair, autonomous model-default alignment, Codex session-id/cache-key limit fix, Files+Memory async searchParams fix, sudo DECIDING fix
- Chat top-bar refinement: commits `1ad5ebd`→`a8fa137`, Philippe approved at 18:06 GMT+8
- Finalization + doc sweep completed by 19:09 GMT+8
- Savepoint: `projects/_ops/mc-savepoint-2026-04-14-afternoon.md` refreshed

**New entity facts extracted:**
- `life/projects/mission-control.md` updated with 9 new Apr 14 durable facts (Next 16/React 19 migration, WS sidecar latency fix, 5 autonomous-task/engine fixes, Chat top-bar refinement + Philippe approval)
- `life/projects/mission-control.md` summary section updated to include Apr 14

**Entity additions summary:**
- Next 16 / React 19 dependency migration (commit db7716d)
- WS sidecar connect latency fix (~5s → ~15ms)
- Autonomous task execute startup validation fix (commit 034bc23)
- Planning-only guard fix for preview log noise
- Codex session-id cache-key limit fix (shortened hashed ids)
- Autonomous model-default alignment (codex5.4 → gpt-5.4)
- Files+Memory async searchParams fix
- Sudo DECIDING stuck fix (module path resolution)
- Chat top-bar one-row refinement approved by Philippe (commits 1ad5ebd→a8fa137)

**No new life/people/companies/entity facts requiring extraction today.**

## Workspace Home-Improvement — data-manager restoration
**What:** Added `data-manager` back to `deterministic_scheduler.py` and fixed TOOLS.md cron table.

**Problem found:** `data-manager` (JSONL/log rotation >30 days, .err.log truncation, cron-run-details pruning) was silently dropped from the deterministic scheduler after its last successful run on 2026-03-24. Cron-run-log confirmed no runs on the expected Sundays (Mar 29, Apr 5, Apr 12). The `cron_runner_tasks.py` entry and the `scripts/cron_tasks/data_manager.py` script were intact — only the trigger was missing.

**Changes made:**
1. `scripts/deterministic_scheduler.py` — added `Task("data-manager", "weekly", day_of_week=0, hour=5, minute=0)` (Monday 05:00 ICT; confirmed correct by verifying `dependency-update-audit` runs Monday 10:30 and `entity-lifecycle-manager` runs Sunday 05:00 with no conflict)
2. `TOOLS.md` cron table — moved three orphaned rows (`entity-lifecycle-manager`, `data-manager`, `dependency-update-audit`) out of a stray disabled-section block into the active table with corrected schedules; added expanded description for `data-manager`; cleaned up disabled-section markdown

**Expected benefit:** JSONL and log rotation resumes on the weekly Monday 05:00 schedule, preventing unbounded log accumulation and keeping the 30-day retention policy enforced.

**Rollback:** Remove the `data-manager` Task line from `deterministic_scheduler.py`; move the three rows back to the disabled-section block in TOOLS.md if needed.

## Morning Meeting Decision [09:52 GMT+8]
- Report: self-improvement
- Finding: LOW-001
- Decision: Approved
- Problem: `TOOLS.md` Mission Control runtime notes were missing a concise Apr 14 runtime-truth summary for the now-stable WS sidecar latency fix, autonomous session-id length rule, Marvin task model-default alignment, and detached autonomous-task startup-validation hardening.
- Risk: low documentation drift; runtime behavior was already correct, but operator truth in `TOOLS.md` lagged behind `MEMORY.md` and `.learnings/errors.md`.
- Action taken: updated the Mission Control runtime section in `TOOLS.md` with one consolidated Apr 14 runtime-behavior block covering the verified fixes and current operational truth.
- Verification: reviewed the edited `TOOLS.md` block in place after the patch; no code or control-plane behavior changed.
- Rollback: `git checkout -- TOOLS.md`
- Notes: bounded docs-only Morning Meeting fix.

## Morning Meeting Decision [09:53 GMT+8]
- Report: self-improvement
- Finding: LOW-003 / CORR-20260414-1138
- Decision: Adjusted
- Problem: the correction about clearly labeling Morning Meeting mode transitions was still marked active even though Philippe no longer considers it relevant enough to keep open.
- Risk: low workflow-friction lesson only; no active operational risk.
- Action taken: marked `CORR-20260414-1138` as resolved in `.learnings/corrections.md`.
- Verification: confirmed the correction entry now shows `Status: resolved`.
- Rollback: `git checkout -- .learnings/corrections.md`
- Notes: closed by Philippe’s decision rather than by waiting for another review cycle.

## Morning Meeting Decision [09:56 GMT+8]
- Report: self-improvement
- Finding: LOW-004
- Decision: Adjusted
- Problem: `SOUL.md` wording about “messaging surfaces” was too broad and could be read as covering Mission Control or the Gateway UI, when the real intent is human messaging platforms.
- Risk: low wording ambiguity only.
- Action taken: rephrased the non-negotiable line in `SOUL.md` to target human messaging platforms explicitly and exclude Mission Control / Gateway UI.
- Verification: reviewed the updated line in `SOUL.md`; wording now names Telegram, Signal, WhatsApp, Discord, and Apple Messages explicitly.
- Rollback: `git checkout -- SOUL.md`
- Notes: Philippe clarified the intended scope.

## Mission Control Tool-Run History Cleanup [10:20 GMT+8]
- What changed: tightened `projects/mission-control/components/chat/chat-tool-groups.tsx` so history-loaded `read` / `exec` rows use summary-first previews when args are missing, and expanded tool details now wrap large read/command results in `ChatFileContentView` instead of dumping the entire body inline by default.
- Why: older or history-hydrated tool results could fall back to full `meta` text, which made refreshed morning previews show giant read bodies and long command output walls inside tool runs.
- Expected benefit: cleaner tool-run history cards, much less scroll churn after preview refresh, and safer behavior when hydrated transcript entries only have result text.
- Verification: `npm run build` passed in `projects/mission-control`, then `./scripts/preview-restart.sh` completed successfully.
- Rollback: `git checkout -- projects/mission-control/components/chat/chat-tool-groups.tsx`

## Codex CLI Default-Model Fix [10:26 GMT+8]
- What changed: set the local Codex CLI default model in `~/.codex/config.toml` to `gpt-5.3-codex`, and added a matching operational note in `TOOLS.md`.
- Why: the local Codex CLI is authenticated in ChatGPT mode; without an explicit model it was falling back to unsupported `gpt-5.2-codex` and failing immediately.
- Expected benefit: local `codex exec` runs now work without having to remember `-m gpt-5.3-codex` every time.
- Verification: `codex exec --json --full-auto -C /tmp/openclaw-nerve 'Reply with OK and nothing else.'` succeeded without an explicit `-m`.
- Rollback: remove the `model = "gpt-5.3-codex"` line from `~/.codex/config.toml`; `git checkout -- TOOLS.md`

## Mission Control Chat polish batch [10:42 GMT+8]
- What changed: widened the top-row model and thinking pills, switched visible thinking labels back to lowercase, tightened `ChatActivityStrip` overflow behavior so in-progress activity chips cannot push the transcript lane wider, and suppressed standalone lifecycle `Runtime phase` transcript rows so `Run started` / `Run finished` no longer consume transcript space as separate blocks.
- Why: the model/thinking pills were truncating longer labels, the in-progress lane could temporarily overrun its column during live activity, and lifecycle phase notes were visually noisy relative to their value.
- Expected benefit: cleaner one-row top controls, stable transcript width during active runs, and a calmer chat history with less system clutter.
- Verification: `npm run build` passed in `projects/mission-control`, then `./scripts/preview-restart.sh` completed successfully and the preview endpoint responded.
- Rollback: `git checkout -- projects/mission-control/components/chat/MissionControlChatSurface.tsx projects/mission-control/components/chat/chat-activity-strip.tsx projects/mission-control/lib/chat/runtime-bridge-transcript.ts`

## Mission Control long-task visibility improvement [11:05 GMT+8]
- What changed: added a compact runtime-status pill to the Chat top bar just left of `Stop`, with calm `Ready / Working / Finalizing / Attention / Disconnected` states derived from live runtime status; also cleared lingering `activeRunId` values on final/error paths in `useRuntimeBridge.ts`, and tightened `AGENTS.md` so long tasks require a kickoff, one delayed progress update if needed, and a terminal user-facing completion/blocker message once tools finish.
- Why: longer tasks were sometimes leaving Philippe uncertain whether Marvin was still working, already done, or silently stalled.
- Expected benefit: clearer top-bar status during long runs, fewer false "maybe he's still busy" waits, and a stronger behavioral rule so completed long tasks are explicitly closed out.
- Verification: `npm run build` passed in `projects/mission-control`; runtime-state cleanup is in the final/error/send-failure paths; `AGENTS.md` now explicitly defines long-task closure expectations.
- Rollback: `git checkout -- projects/mission-control/components/chat/MissionControlChatSurface.tsx projects/mission-control/hooks/useRuntimeBridge.ts AGENTS.md`

## Mission Control Files search + preview polish [12:17 GMT+8]
- What changed: added a workspace-wide, name-only file/folder search to the Files page (`q` query-param based, capped at 50 results), reduced `Recent files` from 8 to 3, and made file selections that target `#file-preview` auto-scroll to the preview section reliably when the preview loads.
- Why: Philippe wanted a separate Files-page search for folder/file names rather than file contents, a shorter recent-files history, and less friction when selecting a file from the browser.
- Expected benefit: faster file finding without content search clutter, less noisy recent-file history, and a more obvious handoff from browser click to live preview.
- Verification: `npm run build` passed in `projects/mission-control`; `./scripts/preview-restart.sh` completed successfully and the preview endpoint responded after restart.
- Rollback: `git checkout -- projects/mission-control/components/files/FilesPreviewSection.tsx projects/mission-control/components/files/RecentFilesStrip.tsx projects/mission-control/components/pages/GeneralFilesPage.tsx projects/mission-control/lib/adapters/files.ts projects/mission-control/lib/types/contracts.ts`

## Mission Control Files search-results scroller [13:05 GMT+8]
- What changed: constrained the Files name-search results section to a fixed-height shell with its own internal scroll area instead of letting the whole page grow vertically with long result sets.
- Why: Philippe liked the feature but the uncapped vertical list made the Files page too long when many name matches were returned.
- Expected benefit: easier browsing of search results without pushing the preview and rest of the page too far down.
- Verification: `npm run build` passed in `projects/mission-control`; `./scripts/preview-restart.sh` completed successfully and the preview endpoint responded after restart.
- Rollback: `git checkout -- projects/mission-control/components/pages/GeneralFilesPage.tsx`

## Mission Control shell cleanup [13:18 GMT+8]
- What changed: hid the bottom sidebar date completely when the left navigation is collapsed, and removed the unused green `P` profile pill from the top shell bar so the search icon now sits alone at the far right.
- Why: Philippe wanted the collapsed sidebar cleaner and the placeholder profile control removed because there is no real profile-switching use case in the current setup.
- Expected benefit: less visual noise in the shell chrome and a tidier collapsed navigation state.
- Verification: `npm run build` passed in `projects/mission-control`; `./scripts/preview-restart.sh` completed successfully and the preview endpoint responded after restart.
- Rollback: `git checkout -- projects/mission-control/components/shell/Sidebar.tsx projects/mission-control/components/shell/TopTabBar.tsx`

## User verification + placement note [11:09 GMT+8]
- Philippe confirmed the Mission Control tool-run history cleanup fixed the morning refresh issue where collapsed `read` results could dump huge document bodies.
- For long-task visibility, Philippe preferred a persistent Mission Control status indicator in the Chat top bar, ideally centered or just left of `Stop`; the implemented placement is just left of `Stop`.
- The long-task status-signaling patch was committed as `5f32540` (`Improve Mission Control long-task status signaling`).

## Queue wakeup replay resolution [10:19 GMT+8]
- What changed: processed the pending queue item `[Career] Draft: Creative-tool automation script plan` by verifying the already-existing Apr 13 deliverables (`projects/creative-practice/blender-automation-spec-turntable-scene-setup-2026-04-13.md` and `projects/creative-practice/blender-automation-implementation-plan-turntable-scene-setup-2026-04-13.md`), marking the queue entry completed, appending a replay-handled line to `memory/tasks-log.md`, and removing the stale matching task block from `AUTONOMOUS.md` Open Backlog.
- Why: the task had already been completed and reconciled in the Mission Control structured store on Apr 13, but a duplicate pending queue entry plus stale AUTONOMOUS backlog block were still lingering.
- Expected benefit: delegation queue is clean again, backlog truth is less misleading, and the same creative-automation task will not be re-run pointlessly.
- Verification: both artifact files exist, queue counts are now `pending=0` / `spawned=0`, and `read_autonomous_sections()` now reports `open_backlog=4` / `in_progress=0` after removing the stale block.
- Rollback: restore the removed AUTONOMOUS task block, remove the replay line from `memory/tasks-log.md`, and change the queue entry back from `completed` if a re-run is actually desired.
- Notes: attempted Telegram start announcement to `goal-tasks`, but `sessions_send` was blocked in this session by `tools.sessions.visibility=tree`, so no cross-session notification could be sent from here.

## Mission Control file editor search upgrade [11:45 GMT+8]
- What changed: upgraded the shared `CodeMirrorEditor` from a styled textarea to a real CodeMirror 6 editor, enabled built-in search/find via `@codemirror/search`, preserved `Cmd/Ctrl+S`, preserved read-only handling, added a restrained Mission Control-specific CM theme/highlighting, and added language extension mapping/wrapping rules for common file types used in Files and Memory editors.
- Why: Mission Control lacked real in-editor find/search, while Nerve already proved the CM6 search path is the right foundation.
- Expected benefit: Files and Memory editors now support real `Cmd/Ctrl+F` / `Ctrl+F` find behavior and a materially better editing foundation without redesigning the surrounding surfaces.
- Verification: `npm run build` passed in `projects/mission-control`, `./scripts/preview-restart.sh` completed successfully, and the preview endpoint responded after restart. Remaining Next warnings are pre-existing unrelated autonomous-task / tracing warnings.
- Rollback: `git checkout -- projects/mission-control/components/editor/CodeMirrorEditor.tsx projects/mission-control/components/editor/editorTheme.ts projects/mission-control/components/editor/languageMap.ts`

## Mission Control editor find-button follow-up [11:53 GMT+8]
- What changed: added a visible `Find` button to the shared Mission Control editor header in `CodeMirrorEditor.tsx`; clicking it focuses the CM6 editor and opens the built-in search panel programmatically via `openSearchPanel()`.
- Why: browser `Cmd/Ctrl+F` can still win when focus is outside the editor, which made the new editor search feel unreliable in practice.
- Expected benefit: Files and Memory editors now expose an obvious, deterministic search entry point without relying on focus state.
- Verification: `npm run build` passed in `projects/mission-control`; `./scripts/preview-restart.sh` completed successfully and the preview endpoint responded.
- Rollback: `git checkout -- projects/mission-control/components/editor/CodeMirrorEditor.tsx`

## Mission Control continuity save [13:30 GMT+8]
- What changed: created a new detailed Mission Control savepoint at `projects/_ops/mc-savepoint-2026-04-15-afternoon.md`; updated `MEMORY.md`, `life/projects/mission-control.md`, and `TOOLS.md` with the now-stable Apr 15 Mission Control posture; added two reusable UI/rendering lessons to `.learnings/errors.md`.
- Why: Philippe asked for a proper save before touching anything else, with continuity captured across daily memory, durable docs, learnings, and a formal `_ops` savepoint.
- Expected benefit: future Mission Control work can resume from a stable checkpoint instead of reconstructing today’s accepted state from chat history.
- Verification: savepoint file written successfully; durable docs updated in place; learnings entries appended without overwriting earlier content.
- Rollback: `git checkout -- MEMORY.md TOOLS.md life/projects/mission-control.md .learnings/errors.md` and remove `projects/_ops/mc-savepoint-2026-04-15-afternoon.md` if the savepoint itself needs to be discarded.

## Specialist seat path verification [14:06 GMT+8]
- What I checked: whether the Apr 13 Link/specialist-seat path fix is still live, and whether specialist seats still have reliable awareness of their actual workspace files.
- What I found: the Mission Control seat bridge still injects absolute shared-workspace paths for Link (`projects/mission-control/scripts/seat-bridge.mjs`), so yesterday’s fix is still present. But the seat-local specialist mounts like `/data/.openclaw/workspace-job-advisor` are still mostly scaffolds, while the real continuity/application files live under `/data/.openclaw/workspace/agent-workspaces/...`.
- Result: Link did not regress in the bridge itself; the failure was triggered by a user-supplied relative path (`agent-workspaces/job-advisor/...`) that is still unsafe inside specialist seats. The same structural fragility likely applies to Japin, Johan, and Milou.
- Expected next fix: normalize user-supplied `agent-workspaces/<seat>/...` paths to absolute shared-workspace paths and tighten the specialist skill docs so they stop implying that the seat-local mount is the source of truth.

## Specialist workspace unification [14:22 GMT+8]
- What changed: kept the seat-local specialist roots (`/data/.openclaw/workspace-<seat>`) as runtime/bootstrap shells, but aliased their content layer back to the shared specialist workspace with symlinks for `memory`, `artifacts`, `.learnings`, `MEMORY.md`, `SKILLS.md`, `WORKSPACE.md`, plus shared `skills/` and `agent-workspaces/`. Also added `scripts/specialist_workspace_aliases.py` to check/recreate those aliases and updated Link's skill docs to prefer absolute shared-workspace continuity paths.
- Why: Philippe wanted to eliminate the confusing split between shared specialist truth and seat-local specialist mounts, after Link failed to resolve a shared-workspace file from a relative `agent-workspaces/job-advisor/...` path.
- Expected benefit: specialist seats now resolve both seat-local-style paths (`memory/...`) and shared-workspace-style paths (`agent-workspaces/<seat>/...`) against the same canonical specialist data, reducing path confusion for Link, Japin, Johan, and Milou.
- Verification: the previously failing resume path now resolves successfully at both `/data/.openclaw/workspace-job-advisor/agent-workspaces/job-advisor/memory/applications/philippe-master-resume-2026-04-13.md` and `/data/.openclaw/workspace-job-advisor/memory/applications/philippe-master-resume-2026-04-13.md`; `python3 scripts/specialist_workspace_aliases.py` reports all specialist aliases OK.
- Rollback: remove the created symlinks from `/data/.openclaw/workspace-{language-tutor,sportsbet-advisor,trading-advisor,job-advisor}/` and restore any replaced empty directories as needed; revert `TOOLS.md`, `MEMORY.md`, `.learnings/errors.md`, `skills/job-advisor/SKILL.md`, and `scripts/specialist_workspace_aliases.py` in git if the documented posture needs to be undone.

## Link voice-presence tweak [15:29 GMT+8]
- What changed: tightened Link's seat voice and activation prompts so she gives one short in-character acknowledgment when enough material is already present, and one short verbal handoff when she finishes meaningful drafting work; also logged the no-silent-completion preference in Link's seat-specific corrections.
- Why: Philippe liked the improved task execution after the workspace unification fix, but correctly pointed out that Link still felt too silent: "a 'hi' would've been nice."
- Expected benefit: Link should feel like a present specialist instead of a mute drafting lane, without becoming chatty or theatrical.
- Verification: updated `agent-workspaces/job-advisor/SOUL.md`, `projects/mission-control/lib/agents/definitions.ts`, `projects/mission-control/scripts/seat-bridge.mjs`, and `agent-workspaces/job-advisor/.learnings/corrections.md` in sync so both the specialist voice and Mission Control activation path reflect the new behavior.
- Rollback: `git checkout -- agent-workspaces/job-advisor/SOUL.md projects/mission-control/lib/agents/definitions.ts projects/mission-control/scripts/seat-bridge.mjs agent-workspaces/job-advisor/.learnings/corrections.md`

## Specialist seat voice parity [15:39 GMT+8]
- What changed: checked Japin, Johan, and Milou and confirmed they were missing the same brief greeting + handoff behavior Link just received. Added matching `Interaction habit` sections to their seat SOUL files, introduced a reusable `withSpecialistPresenceHandoff(...)` helper in both Mission Control specialist activation paths (`lib/agents/definitions.ts` and `scripts/seat-bridge.mjs`), and documented the rule for future-added specialists in `projects/mission-control/docs/agent-seat-bridge.md`.
- Why: Philippe wanted the other specialist agents checked too, and wanted future-added ones to inherit the same policy instead of relying on one-off seat edits.
- Expected benefit: Japin, Johan, Milou, and Link should all behave as present-but-brief specialists rather than silent worker lanes, and new specialists have a documented/default path to inherit the same behavior.
- Verification: `npm run build` passed in `projects/mission-control`; `./scripts/preview-restart.sh` completed successfully and the preview endpoint responded after restart.
- Rollback: `git checkout -- projects/mission-control/lib/agents/definitions.ts projects/mission-control/scripts/seat-bridge.mjs projects/mission-control/docs/agent-seat-bridge.md agent-workspaces/language-tutor/SOUL.md agent-workspaces/sportsbet-advisor/SOUL.md agent-workspaces/trading-advisor/SOUL.md`

## Link seat transcript-gap diagnosis [15:55 GMT+8]
- What I checked: whether Link actually stayed silent in the Hospitable Q&A run, and why the current session could not see a Link session in `sessions_list`.
- What I found: Link did reply in her own specialist session log under `/data/.openclaw/agents/job-advisor/sessions/9671395b-bd45-42b1-8620-0aa90795c0f1.jsonl` — first with a short acknowledgment after the initial role brief, then with a full draft-answer message after Philippe sent the questions. Mission Control surfaced the tool run and status transition, but not the assistant text. The specialist session also lives under the `job-advisor` agent storage, so it is not visible from this current session's `sessions_list` view even though the bridge registry exists under the main runtime.
- Result: this is a real seat-transcript/bridge bug, not Link refusing to talk. The raw messages exist; the visible Mission Control transcript missed them.
- Expected next fix: audit the specialist seat bridge / transcript merge path so agent-level specialist assistant messages are mirrored into the visible Mission Control transcript as reliably as tool events.

## Specialist seat transcript-history fix [16:04 GMT+8]
- What changed: patched `projects/mission-control/lib/runtime-bridge-history.ts` so Mission Control loads session history from the agent-specific session root that matches the target session key (for example `agent:job-advisor:main` -> `/data/.openclaw/agents/job-advisor/sessions`) instead of assuming all seat history lives under the main agent's session directory.
- Why: Link did speak in her own raw specialist session, but Mission Control missed the assistant text because the history loader was only reading `/data/.openclaw/agents/main/sessions`, which does not contain the real seat transcript for specialist agents.
- Expected benefit: seat switches should now hydrate real specialist history, and post-run specialist replies should show up in the visible Mission Control transcript instead of only tool activity and status changes.
- Verification: `npm run build` passed in `projects/mission-control`; `./scripts/preview-restart.sh` completed successfully; direct API check to `http://127.0.0.1:3005/api/runtime-bridge/history?sessionKey=agent%3Ajob-advisor%3Amain` now returns Link's previously missing assistant acknowledgment and full draft-answer message.
- Rollback: `git checkout -- projects/mission-control/lib/runtime-bridge-history.ts .learnings/errors.md`

## Late save refresh [17:58 GMT+8]
- What changed: refreshed the continuity layer after the final specialist-seat fixes by appending the live-hydration lesson to `.learnings/errors.md`, updating `TOOLS.md` and `MEMORY.md` with the corrected specialist-seat storage/runtime truth, and expanding `projects/_ops/mc-savepoint-2026-04-15-afternoon.md` with a late-afternoon specialist-seat addendum.
- Why: Philippe confirmed Milou's chat flow now runs smoothly after the live specialist transcript patch and wanted the latest good state captured before a later fresh session.
- Expected benefit: the next session can resume from an accurate post-fix checkpoint instead of stopping at the earlier afternoon savepoint.
- Verification: savepoint updated in place; durable docs updated in place; learnings now include both the hard-refresh-history fix and the later live-hydration fix.
- Rollback: `git checkout -- .learnings/errors.md TOOLS.md MEMORY.md projects/_ops/mc-savepoint-2026-04-15-afternoon.md`

## OpenClaw upgrade audit + rollback-safe git stabilization [23:40 GMT+7]
- What changed: rebuilt the Mission Control preview from current workspace truth after the VPS rollback, verified the local preview route is healthy, stabilized the nested `projects/mission-control` git state by removing generated-runtime junk from tracking while preserving the current source tree, and wrote a new upstream upgrade audit at `projects/_ops/openclaw-v2026.4.12-upgrade-audit-2026-04-16.md`.
- Why: Philippe wanted the rolled-back workspace treated as canonical truth, then wanted a careful assessment of whether moving from OpenClaw `2026.3.8` to `v2026.4.12` is worth pursuing, with special attention to security-tightening risks.
- Expected benefit: Mission Control now has a clean rollback-safe local baseline again, and the next upgrade work can proceed from an audited staged plan instead of piecemeal guesswork.
- Verification: local preview returns `200` on `/general/chat`; nested Mission Control repo and outer workspace Mission Control slice were re-anchored with local commits; audit concludes `v2026.4.12` is a good rehearsal target but not a direct live jump, and recommends keeping Dreaming + Active Memory off for the first upgrade rehearsal.
- Rollback: revert the local git cleanup commits if needed; remove `projects/_ops/openclaw-v2026.4.12-upgrade-audit-2026-04-16.md` if the audit draft itself is discarded.
