# 2026-02-24

## Rollback Recovery + Stabilization (13:50-14:20)

- Philippe restored VPS from previous snapshot after morning config corruption incident.
- Root cause context: invalid non-schema provider keys (`type`, `token`, `endpoint`) had caused config invalidation and gateway boot instability.
- Verified current `/data/.openclaw/openclaw.json` is valid and healthy.
- Confirmed model routing state after rollback:
  - Primary: `minimax/MiniMax-M2.5`
  - Fallback: `openai-codex/gpt-5.3-codex`
  - Nexos provider block absent.

## Cron Reliability Fixes

- Diagnosed `platform-health-council` "cron announce delivery failed" as delivery-path flakiness, not model/provider config failure.
- Applied fix: set `platform-health-council` `delivery.mode=none` and re-ran successfully.
- Re-enabled `nightly-security-review` and set it to explicit Telegram-in-message flow with cron delivery disabled.
- Recreated malformed legacy `daily-backup` job into valid schema, then removed it again per Philippe preference.
- Current preference: Philippe handles daily VPS snapshot manually (no daily-backup cron).

## False-Positive Cleanup

- Updated sample `config/token-manifest.json` dates to remove stale expired-token false criticals.
- Updated platform-health-council prompt to treat empty vector/chunk tables as INFO when embedding provider is intentionally not configured.

## Dependency Hygiene

- Updated `requirements.txt` pin from `requests==2.32.3` to `requests==2.32.5`.
- Verified `.venv` already on `2.32.5` and test suite passed.

## Process Lesson Reinforced

- For high-risk config/model/provider changes: prefer schema-safe CLI edits over manual JSON key surgery.
- Continue confirmation-first execution for non-trivial fixes.

## Nexos Disable (CLI Path, Safe)

- Removed Nexos provider block via `openclaw config unset models.providers.nexos`.
- Removed Nexos aliases via `openclaw config unset agents.defaults.models.nexos/...`.
- Primary = MiniMax-M2.5, Fallback = Codex only. No config corruption.

## Telegram Group Routing Setup

- Created Telegram groups for cron job delivery:
  - nightly-security-review: `-1003855426803`
  - platform-health-council: `-1003504843228`
  - smart-auto-updater-daily: `-1003802382625`
  - self-improvement: `-1003721620909`
- Added all groups to Telegram allowlist (`channels.telegram.groups`).
- Each cron now sends explicit Telegram messages in its own group (delivery.mode=none).

## Security Hardening

- Disabled `gateway.controlUi.dangerouslyDisableDeviceAuth` (was re-enabled after restart).
- Added Telegram command sender allowlist: `channels.telegram.groupAllowFrom` and `allowFrom` set to `["8471960624"]`.
- Replaced unsafe `eval()` in `codex_test.py` with `math.prod()`.
- Verified `auth.json` permissions are `600` and file is gitignored.

## Self-Improvement Cron

- Created `self-improvement` cron: daily at 04:00 ICT, MiniMax-M2.5.
- Scope: AGENTS.md, MEMORY.md, TOOLS.md, SOUL.md, IDENTITY.md, USER.md, HEARTBEAT.md, last 3 daily notes.
- First run found 10 issues (2 critical, 3 high, 3 medium, 2 low). Awaiting confirmation to apply fixes.

## Core Docs Updates (Self-Improvement Action)

- Populated `TOOLS.md` with GitHub, Telegram, VPS, cron, skills, models, scripts, environment details.
- Clarified group chat rules in AGENTS.md + MEMORY.md: "Quality > quantity" applies to groups 4+, smaller groups get more engagement.
- Reset HEARTBEAT.md: active hours 08:00-23:00 ICT, MiniMax-M2.5, reasoning disabled, no Telegram broadcast.
- Removed `BOOTSTRAP.md` per original AGENTS.md rule, moved hosting context to IDENTITY.md.

## Weekly Reminder Cron

- Created `weekly-reminder` cron: Saturday 20:00 ICT (8PM).
- Sends friendly check-in to Philippe via Telegram.

