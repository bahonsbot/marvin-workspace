# 2026-04-11

## Nightly Memory Extraction

**Source session:** `agent:main:main` (44735e1a) — Philippe's Mission Control work session, Apr 10 afternoon/evening KL time (05:31–07:25 UTC).

**Session summary:** Philippe used Mission Control to complete the Johan + Milou specialist seat setup and a bundled UI polish pass.

### Durable facts extracted

**Mission Control:**
- Johan + Milou direct specialist runtime completed: both moved from Marvin-routed specialist posture to direct specialist (routing: direct, chatReadiness: ready, workspaceReadiness: ready, actionMode: control); Johan→sportsbet-advisor agent, Milou→trading-advisor agent; dedicated workspaces seeded
- Johan + Milou added to Chat seat dropdown; seat dropdown made scrollable
- Agents page UI batch: quiet seats mapped to green palette (amber/quiet treatment removed); "Present but quiet" label → "Ready now"; "N hidden by default" → simple down-arrow indicator
- Vantage promoted from staged/marvin-routed to truthful internal-only lead-seat posture aligned with content-team child seats (Signals Scout, Keyword/Gap Analyst, Editorial Strategist, Writer/Draft Publisher, GSC Performance Analyst)
- Active green agent-card styling brightened to lighter vibrant green (commit c46a892)
- OpenAI temporary overload occurred mid-session; retry batch was re-sent successfully

**Agent team:**
- Existing items at-001 through at-007: accessCount bumped to 2 (referenced during this session)
- New: Durable agent identities locked (Apr 5) — Sudo, Vantage, Johan, Milou, Japin with roles and team compositions
- New: Johan + Milou direct specialist runtime completed (Apr 10)

### Entity updates applied
- `life/projects/mission-control.md` — 5 new fact rows added, Summary section updated
- `life/projects/agent-team/items.json` — 7 existing items accessCount bumped; 2 new items added (at-008, at-009)

### Cron jobs observed
- `nightly-memory-extraction` (this job): ran at 02:00 KL / 18:00 UTC
- `self-improvement` (21h ago): ran normally
- `platform-health-council` (22h ago): ran normally
- `nightly-security-review` (21.5h ago): delivered to Telegram
- `market-signal-generator` (2.2h ago): last of many runs
- `signal-accuracy-review` (3h ago): ran normally
- `daily-task-generator` (17h ago): ran normally
- Japanese Lesson cron also fired (~11h ago)

### No Morning Meeting held today.

## Workspace Home-Improvement Pass (09:04 KL)

**What changed:** `scripts/index_memory_health.sh` — added inline documentation (usage, purpose, when-to-run, related files).

**Why:** The script is referenced in `docs/runbooks/memory-system-health.md` as part of QMD maintenance but had no inline documentation. Makes it harder to onboard or debug without it.

**Expected benefit:** Easier to understand, run, and maintain; reduces tribal knowledge dependency.

**Rollback if needed:** Revert the file to the previous version from git — the script was functional, just undocumented.

## Mission Control Tasks sync fix (10:00 KL)

**What changed:** Fixed the generator / Tasks-board sync mismatch so daily-generated backlog tasks now land in the structured Tasks store as well as `AUTONOMOUS.md`. Also tightened legacy import so manually deleted legacy tasks stay suppressed instead of being silently re-imported, and clarified the Tasks UI buttons to `Pull from md` and `Apply board truth`.

**Why:** New tasks generated into `AUTONOMOUS.md` could be removed by sync cleanup because the board was treated as truth but the generator was not updating the board. That broke trust in the Tasks flow.

**Expected benefit:** Generated tasks now appear on the Tasks board immediately, cleanup no longer wipes valid new tasks by accident, and manual deletions are respected.

**Rollback if needed:** Revert `scripts/daily-task-generator.py`, `projects/mission-control/lib/autonomous.ts`, `projects/mission-control/lib/adapters/tasks.ts`, `projects/mission-control/components/pages/TasksBoardSwitcher.tsx`, and restore `projects/mission-control/data/autonomous-tasks.json` from git if the new sync path misbehaves.

## Morning Meeting follow-up (10:19 KL)

**Approved and applied:** Restarted the autonomous trading bot watchdog via `projects/autonomous-trading-bot/scripts/webhook_watchdog.sh`.

**Why:** Platform Health Council reported the receiver as healthy but the watchdog inactive, leaving auto-recovery weaker than intended.

**Expected benefit:** Restores automatic restart coverage if the webhook receiver dies unexpectedly.

**Verification:** Watchdog process is running (`bash scripts/webhook_watchdog.sh`, PID 95610). Trading bot `/health` and `/health/auth` both returned OK after restart.

**Rollback:** Kill the watchdog process if needed; receiver health remains independently verifiable via `/health` and `/health/auth`.

## Morning Meeting follow-up (10:23 KL)

**Approved and applied:** Added a small curated `MEMORY.md` update for durable Mission Control truths only.

**What was promoted:**
- Johan + Milou direct specialist-seat posture
- Vantage as truthful internal lead seat
- Mission Control Tasks board as source of truth, with `AUTONOMOUS.md` as mirror/reconciliation surface

**Why:** These are durable operating truths, not just daily chronology, so they belong in curated memory.

**Expected benefit:** Future sessions get the right Mission Control posture quickly without turning `MEMORY.md` into a diary.

**Rollback:** Revert the added Mission Control bullets in `MEMORY.md` if the curated scope later feels too broad.

## Mission Control UI micro-tweaks (10:33 KL)

**What changed:**
- changed the top-left shell label from `Mission Control` to `Marvin’s Room`
- replaced the Home quote rotator with deterministic daily famous quotes including author attribution
- brightened the Agents-page green health indicator palette for ready/quiet/active states

**Why:** Small UX polish pass before deeper project work, plus the Home quote block now needs real attributed quotes rather than internal placeholder lines.

**Expected benefit:** More personality in the shell, better quote experience on Home, and fresher visual status cues on Agents.

**Verification:** `npm run build` passed in `projects/mission-control`.

**Rollback:** Revert `projects/mission-control/components/shell/TopTabBar.tsx`, `projects/mission-control/lib/adapters/home.ts`, `projects/mission-control/lib/types/contracts.ts`, `projects/mission-control/components/pages/GeneralHomePage.tsx`, and `projects/mission-control/components/agents/AgentSeatCard.tsx` if needed.

## Pre-compaction memory flush (13:04 KL)

- Mission Control Tasks sync bug was fixed earlier today: generated backlog tasks now sync into the structured Tasks store, `AUTONOMOUS.md` remains the mirror/reconciliation layer, and suppressed manually deleted legacy tasks are no longer silently re-imported.
- Morning Meeting outcomes: nightly security review accepted with no action, trading-bot watchdog restart approved and completed, self-improvement finding narrowed so only durable truths were promoted.
- Curated `MEMORY.md` update added durable Mission Control truths for Johan + Milou direct specialist posture, Vantage as a truthful lead seat, and the Tasks board as source of truth.
- Mission Control micro-tweaks completed: top-left shell label changed to `Marvin’s Room`, Home quote card now shows deterministic daily famous quotes with author attribution, and Agents-page green status indicators were brightened.
- Current specialist default runtime models confirmed from seat definitions: Johan, Milou, and Japin all default to `codex5.4` with medium thinking.

## End-of-session housekeeping (13:45 KL)

**What was cleaned up:**
- added a reusable correction clarifying that `MEMORY.md` is for durable curated truth, not daily backfill
- marked the earlier Tasks-board authority correction as resolved now that the generator/store sync bug is fixed
- logged the Apr 11 Mission Control Tasks sync bug in `.learnings/errors.md`
- updated `TOOLS.md` so the current Tasks sync semantics are explicit: generated tasks must hit the structured store, `Pull from md` imports markdown, and `Apply board truth` reconciles markdown from the board

**Why:** Keeps today’s fixes from living only in session context and reduces future rediscovery.

**Expected benefit:** Better continuity after compaction, cleaner Morning Meeting suppression, and less chance of rebreaking Tasks sync semantics later.

## Link specialist setup (14:05 KL)

**What changed:** Added a new direct specialist seat, **Link** (`job-advisor`), to Mission Control using the same blueprint as Johan, Milou, and Japin.

**Implemented:**
- Mission Control specialist definition for `job-advisor`
- direct seat activation target: `agent:job-advisor:main`
- dedicated workspace scaffold at `agent-workspaces/job-advisor/`
- continuity/memory structure for candidate profile, active continuity, recurring corrections, and application history
- chat activation/session-label wiring so Link appears as a real specialist seat in Mission Control
- matching runtime agent directory scaffold at `/data/.openclaw/agents/job-advisor/agent`

**Role posture:** ATS-safe resume tailoring, cover-letter drafting, and role-specific application support. Open-position crawling is intentionally deferred for later.

**Verification:** `npm run build` passed in `projects/mission-control`, and Mission Control preview was restarted successfully.

**Follow-up left intentionally open:** dedicated skill wiring and final avatar.

## Link polish + ClawHub check (14:28 KL)

**What changed:**
- updated Link's Agents-page summary to the new recruitment-expert line Philippe requested
- wired Link avatar support to `uploads/mission-control/avatars/Link.png`
- rebuilt and restarted Mission Control preview

**ClawHub status check:**
- CLI is present and current commands include `search`, `explore`, and `inspect`
- yes, I can browse/search/inspect skills without installing them
- live search is currently rate-limited from this environment (`Rate limit exceeded`), so discovery works in principle but may need a retry/backoff when the registry is hot

## Link custom skill build (15:06 KL)

**What changed:** Built a new workspace skill at `skills/job-advisor/` for Link instead of adopting a third-party skill wholesale.

**Design choice:** Hybrid of the strongest parts found in ClawHub review:
- `resume-optimizer` → workflow backbone for create / tailor / analyze / export
- `resume-assistant` → modular separation and ATS-review / scoring posture
- explicitly avoided the app-like local datastore model from `resume-builder-ai`

**Implemented:**
- `skills/job-advisor/SKILL.md`
- references for resume workflow, cover-letter workflow, and ATS review checklist
- validation passed via `quick_validate.py`
- packaged successfully to `dist/job-advisor.skill`
- updated Link workspace `SKILLS.md` to point at the real custom skill
- updated Mission Control seat metadata so Link now advertises `job-advisor`
- updated `TOOLS.md` to include the new local skill
- rebuilt and restarted Mission Control preview

**Why:** Best long-term fit is a custom Link skill that uses our own continuity model and specialist-seat architecture, instead of installing a generic standalone resume app skill.

**Verification:** skill validated, packaged, Mission Control build passed, preview restarted successfully.

## Link skill posture hardening (15:25 KL)

**What changed:** Strengthened Link's seat posture so the custom `job-advisor` skill is the explicit first-class workflow rather than just background scaffolding.

**Implemented:**
- Link activation prompt now explicitly says to read `skills/job-advisor/SKILL.md` first
- Link workspace starter files now include `SKILLS.md`
- removed stale `provisional` wording from Link's workspace files
- clarified that the workspace provides Link's voice + continuity, while the reusable workflow lives in the custom skill

**Why:** Prevents Link from drifting into ad-hoc resume help when a real specialist skill now exists.

**Verification:** Mission Control build passed and preview restarted successfully.

## Humanizer skill refresh (15:34 KL)

**What changed:** Reviewed the current `humanizer` skill against the updated Wikipedia `Signs of AI writing` guide and updated the skill accordingly.

**Main updates:**
- added caveats so the skill treats AI tells as signs, not proof
- clarified that Wikipedia-specific markup/citation forensics should not be over-applied to normal prose
- expanded the notability/media-coverage section to cover newer over-attribution patterns
- added new applicable patterns for title-case headings, phrasal templates / placeholder text, and Markdown / formatting leakage
- removed the nonstandard `version` frontmatter field so the skill validates cleanly against the AgentSkills schema

**Why:** The source guide evolved, and the local skill needed both new signals and a cleaner boundary between generally useful prose editing and Wikipedia-only artifact detection.

**Verification:** `quick_validate.py` passed for `skills/humanizer/`.

## Link toolkit expansion (15:40 KL)

**What changed:** Added the `humanizer` skill to Link's toolkit and workspace guidance.

**Implemented:**
- Link seat arsenal now includes both `job-advisor` and `humanizer`
- Link workspace `SKILLS.md` now explicitly positions `humanizer` as the finishing pass for making resumes and cover letters sound less templated or AI-polished while preserving the actual claims

**Why:** Resume and cover-letter work benefits from a final de-AI polish pass, especially after ATS-oriented tailoring.

**Verification:** Mission Control build passed after the toolkit update.

## Custom News source swap (17:30 KL)

**What changed:** Replaced Volkskrant with IEX as the third source in the Custom News digest.

**New priority order:**
1. FD
2. NRC
3. IEX (`https://rss.app/feeds/NV6eLUUDbfKCeMbI.xml`)

**Why:** Volkskrant was blocked by DPG Media's consent/WAF path from this environment, while IEX is a better Dutch markets-oriented fit for the reader anyway.

**Verification:** Python compile passed, `custom_news_feed_monitor.py` ran successfully, and the generated digest now shows feeds `FD`, `NRC`, and `IEX` with 30 published items.

## End-of-day savepoint prep (17:46 KL)

**What was captured:**
- durable memory updated for Link's final posture (custom `job-advisor` skill + `humanizer` companion skill)
- durable Mission Control Home posture updated to reflect paired `Market Watch` + `Custom News` readers and the removal of `Current Tracks`
- `TOOLS.md` updated with the new active runner-backed cron `custom-news-feed-monitor` and the Home/news data-flow notes
- `.learnings/errors.md` updated with the resolved Volkskrant RSS/WAF failure and the IEX replacement decision
- a new comprehensive Mission Control savepoint will be written in `projects/_ops/` so the next session can resume without rediscovery

**Why:** Today's Mission Control work crossed UI, cron, feed ingestion, specialist-seat architecture, custom skills, and content-policy posture. It needs a proper handoff, not just scattered daily notes.

## Mission Control savepoint written (17:46 KL)

**Artifact:** `projects/_ops/mission-control-comprehensive-savepoint-2026-04-11-evening.md`

**Scope captured:**
- Morning Meeting outcomes and the Tasks truth-model fix
- Link creation, custom skill build, avatar, toolkit, and hardened skill-first seat posture
- humanizer refresh against the updated Wikipedia source
- Home-page transition from `Current Tracks` to paired `Market Watch` + `Custom News`
- runner-backed `custom-news-feed-monitor` setup, source-ranking rules, Volkskrant failure analysis, and final IEX replacement
- current accepted product posture plus smart next steps / caveats for the next build session

**Intent:** Give the next session a full Mission Control handoff without rediscovery.

## Mission Control audit calibration + live-lane direction (19:03 KL)

**Philippe feedback on the Apr 11 audit:**
- **Approved:** break up the big Chat and Tasks files; Nerve-inspired improvements for stale-state protection, granular memoization, optimistic UI discipline, richer execution-stage visibility, better recovery/merge behavior, and a stronger server-push mindset.
- **Deferred:** keep the Chat mic icon for now because STT/TTS is still intended once Mission Control is live on HTTPS.
- **Disagreed / keep as-is:** Skills header posture, Home-only bottom strip, legacy stub route cleanup, Tasks toolbar FLOATING-control cleanup, local-storage disclaimer for Personal/Projects boards, Home empty-state redesign, and more actionable Agents-page activation.
- **Adjust:** tighten page-to-page rhythm, but do it narrowly rather than as a broad visual rework.

**Product direction update:**
- Future implementation planning should optimize for a **live Mission Control** as the primary lane, while also accounting for the previously deferred **dev/sandbox lane** as a later parallel environment rather than a replacement for the live/built lane.
- Use `projects/_ops/mission-control-dev-sandbox-lane-plan-2026-03-31.md` as the baseline for that dual-lane posture: built/live remains canonical, sandbox is separate and promotion-based.
