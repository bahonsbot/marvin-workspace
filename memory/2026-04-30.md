# 2026-04-30

## Home-improvement pass [07:30 GMT+7]
- What changed: clarified `AGENTS.md` startup guidance so a missing `memory/YYYY-MM-DD.md` is treated as normal and should be created on first meaningful write instead of being treated like a startup failure.
- Why: a missing daily note is common early in the day and was creating avoidable lookup friction/`ENOENT` confusion during startup and autonomous runs.
- Expected benefit: smoother session startup and less wasted probing when the new daily memory file has not been created yet.
- Verification: re-read the updated `AGENTS.md` startup section and confirmed the new guidance is present.
- Rollback: remove the added sentence from `AGENTS.md` if a stricter startup requirement is later preferred.

## Morning Meeting Decision [11:30 GMT+7]
- Report: platform-health-council
- Finding: signal-accuracy-review cron delivery contract mismatch
- Decision: Approved
- Problem: the `signal-accuracy-review` job was configured with `delivery.mode='none'`, but its prompt required a Telegram summary to `-1003850594375`, causing the run to fail closed at delivery time.
- Risk: low; nightly signal-accuracy review output was not reaching the market-signals group and the job surfaced as an error.
- Action taken: updated the cron job so delivery is explicit (`announce -> telegram:-1003850594375`) and rewrote the prompt contract so the run returns a Telegram-ready final summary instead of trying to send a message internally and then replying `NO_REPLY`.
- Verification: live cron registry now shows `deliveryPreviews.signal-accuracy-review = announce -> telegram:-1003850594375`; previous `lastError` evidence matched `⚠️ ✉️ Message failed`, confirming the original failure mode.
- Rollback: restore the previous cron payload/delivery fields for job `signal-accuracy-review` if this contract change proves incompatible.
- Notes: next scheduled run is tonight at 22:00 ICT; existing `lastRunStatus=error` reflects the pre-fix run and should clear on the next successful execution.

## Morning Meeting Decision [11:34 GMT+7]
- Report: nightly-security-review
- Finding: 2026-04-30 nightly security review clean
- Decision: Accepted risk
- Problem: none requiring action; only standing accepted-risk items remain.
- Risk: low and unchanged; Lab Basic Auth remains intentional for the non-production lane, and `preview.motiondisplay.cloud` TLS mismatch remains a known deferred issue with no dashboard/lab impact.
- Action taken: none
- Verification: reviewed `memory/security/2026-04-30-nightly-security-review.md`; verdict is CLEAN with 0 critical, 0 high, 0 medium, and both trading bot health endpoints were confirmed paper-only and healthy.
- Rollback: n/a
- Notes: suppress repeat discussion unless the state changes.

## Morning Meeting Decision [11:35 GMT+7]
- Report: platform-health-council
- Finding: signal-accuracy-review cron delivery contract mismatch
- Decision: Approved
- Problem: the `signal-accuracy-review` cron job failed because its prompt required Telegram delivery while the cron contract had no valid external delivery target.
- Risk: low; the nightly signal-accuracy review was not publishing its result and surfaced as an avoidable cron error.
- Action taken: updated the cron job to use explicit Telegram announce delivery to `-1003850594375` and rewrote the prompt so the run returns a Telegram-ready final summary instead of attempting internal delivery plus `NO_REPLY`.
- Verification: cron delivery preview now resolves explicitly to `announce -> telegram:-1003850594375`; previous failure evidence matched `⚠️ ✉️ Message failed`.
- Rollback: restore the previous cron payload and delivery fields for job `signal-accuracy-review` if needed.
- Notes: the existing `lastRunStatus=error` is from the pre-fix run and should clear on the next scheduled execution tonight.

## Morning Meeting Decision [11:37 GMT+7]
- Report: self-improvement
- Finding: SI-2026-0430-001 / `.learnings/corrections.md` stale active/resolved bookkeeping
- Decision: Adjusted
- Problem: the review claimed a broad set of corrections had `Closed:` dates while still being marked `Status: active`, creating apparent review noise.
- Risk: low; this is bookkeeping hygiene only.
- Action taken: verified the cited corrections file directly and did not apply a bulk housekeeping pass, because the finding was overstated. The obvious entries with `Closed:` dates are already marked `resolved`, and the named examples do not justify broad churn.
- Verification: inspected `.learnings/corrections.md` around the cited entries; `CORR-20260414-1830` and `CORR-20260414-0109` are already `resolved`, while several later entries marked `active` are still live durable rules rather than stale closures.
- Rollback: n/a
- Notes: useful meta-lesson only — future self-improvement reviews should verify actual inconsistency before counting stale active entries.

## Morning Meeting Decision [11:49 GMT+7]
- Report: self-improvement
- Finding: SI-2026-0430-002 / `TOOLS.md` missing active Mission Control Lab lane notes
- Decision: Approved
- Problem: `TOOLS.md` documented Mission Control runtime basics but under-described the active Lab lane, including its role, path, and runtime ports.
- Risk: low; this creates operator drift and makes future sessions more likely to confuse Dashboard/live runtime with the Lab BOILER ROOM lane.
- Action taken: updated `TOOLS.md` Mission Control runtime notes to explicitly distinguish Dashboard as the live/stable lane and Mission Control Lab as the experimental BOILER ROOM/Trading lane, and added Lab ports `3015/3016/3017/3022/3023`.
- Verification: re-read the updated `TOOLS.md` section and confirmed the Dashboard/Lab distinction, lane-role rule, and Lab port list are present.
- Rollback: revert the `TOOLS.md` Mission Control runtime section if lane structure changes again.

## Morning Meeting Adjustment [12:03 GMT+7]
- Topic: Self-Improvement Item 2 / Mission Control Lab description in `TOOLS.md`
- Adjustment: narrowed the wording after Philippe clarified that Lab is not restricted to Trading/BOILER ROOM.
- What changed: `TOOLS.md` now describes `projects/mission-control-lab` as the experimental Mission Control lane in general, and explicitly notes that Trading / BOILER ROOM is only the current active Lab project, not the sole intended Lab scope.
- Why: the earlier wording was too narrow and risked hard-coding a temporary project focus into durable runtime notes.
- Verification: re-read the Mission Control runtime notes in `TOOLS.md` and confirmed the Dashboard/live-stable vs Lab/experimental distinction plus the corrected scope wording.

## Morning Meeting Decision [12:06 GMT+7]
- Report: self-improvement
- Finding: SI-2026-0430-004 / `SOUL.md` scope reminder omits explicit policy-file references
- Decision: Deferred
- Problem: the review suggested making `SOUL.md` explicitly point to `AUTONOMY.md`, `SUBAGENT-POLICY.md`, and `HEARTBEAT.md`.
- Risk: very low; this is documentation clarity only.
- Action taken: none
- Verification: Philippe explicitly deferred this on the basis that the policy linkage is already explained elsewhere and does not need to be repeated in every document.
- Rollback: n/a
- Notes: avoid duplicating the same policy pointers across multiple docs unless a real confusion pattern appears.

## Morning Meeting Decision [12:08 GMT+7]
- Report: self-improvement
- Finding: SI-2026-0430-005 / `AGENTS.md` Market Intel Data Hygiene rule may need broader scope wording later
- Decision: Deferred
- Problem: the review suggested that the Market Intel data-hygiene note may age into overly narrow wording now that Trading/BOILER ROOM work is active elsewhere.
- Risk: very low; this is wording drift only.
- Action taken: none
- Verification: Philippe explicitly deferred this until later Market Intel polishing work.
- Rollback: n/a
- Notes: revisit when Market Intel projects are polished more broadly.

## Morning Meeting Decision [12:12 GMT+7]
- Report: self-improvement
- Finding: SI-2026-0430-006 / `AUTONOMY.md` No-Progress Pivot Gate lacks explicit source reference
- Decision: Deferred
- Problem: the review suggested adding explicit provenance references to the loop-pattern audit and linked correction entry.
- Risk: very low; the rule itself is already present and clear enough.
- Action taken: none
- Verification: Philippe explicitly deferred this because the current explanation is already sufficient.
- Rollback: n/a
- Notes: only revisit if provenance inside `AUTONOMY.md` becomes genuinely useful later.

## Morning Meeting Decision [12:02 GMT+7]
- Report: self-improvement
- Finding: SI-2026-0430-003 / `AUTONOMOUS.md` stale markdown backlog versus structured task store truth
- Decision: Deferred
- Problem: the review suggested that `AUTONOMOUS.md` may still contain stale backlog markdown even though `projects/mission-control/data/autonomous-tasks.json` is the authoritative task state.
- Risk: low to medium; this is mainly planning/reconciliation drift rather than a live runtime fault.
- Action taken: none
- Verification: Philippe explicitly deferred this for now rather than doing a Morning Meeting cleanup pass on task/backlog state.
- Rollback: n/a
- Notes: revisit later as a bounded audit/cleanup, not a broad rewrite.

## Frontend skill maintenance [12:16-13:26 GMT+7]
- Philippe asked to compare the existing `skills/frontend-skill/SKILL.md` against OpenAI's newer frontend prompt guidance and merge only genuinely new, non-duplicative guidance.
- Reviewed the OpenAI frontend prompt instructions and updated the local frontend skill with compact additions rather than copying the source wholesale.
- Added/clarified guidance around:
  - app-first usable surfaces instead of default marketing heroes for tools/apps
  - familiar control semantics (icon buttons, segmented controls, toggles, tabs, numeric inputs, tooltips)
  - preferring the app's existing icon system or a standard set like Lucide over ad hoc custom SVG icons
  - no nested cards or page sections styled as floating cards
  - layout stability/text-fit rules across mobile and desktop
  - avoiding dark/blurred/generic imagery when users need to inspect the real thing
  - extra litmus checks for usable first screens, domain-native controls, and stability with real data
- Commit created:
  - `44e4c53` `Update frontend skill with new prompt guidance`
- Philippe then asked for a second-pass tightening review without applying changes.
- Produced a line-by-line redundancy review and identified likely trim candidates, but Philippe decided the current document is good as-is because some overlap usefully reinforces important rules.
- Final decision: keep `skills/frontend-skill/SKILL.md` unchanged after the first merge; do not apply any tightening edits.

## Session closeout [13:26 GMT+7]
- Morning Meeting and follow-up frontend-skill maintenance are fully saved to daily memory.
- Current committed outputs from this session include:
  - `44e4c53` `Update frontend skill with new prompt guidance`
- Remaining tracked session changes to keep together in closeout commit:
  - `AGENTS.md` startup clarification for missing daily memory file
  - `TOOLS.md` Mission Control Dashboard/Lab runtime note improvements
  - `memory/2026-04-30.md` Morning Meeting + frontend-skill closeout
  - `.learnings/corrections.md` any reusable lesson from the skill-tightening review if kept

## Cleanup pass [14:07-14:19 GMT+7]
- Philippe asked for an ultra-conservative cleanup retry after a prior cleanup attempt had proven too optimistic.
- Adopted a stricter rule: no delete/revert actions until a read-only audit separated items into `110% safe`, `probably safe`, and `hands off`.
- Read-only triage concluded that only three Chromium temp-profile directories were clearly disposable with near-zero risk:
  - `tmp/chrome-profile-mobile-pages/`
  - `tmp/chrome-profile-dashboard/`
  - `tmp/chrome-profile-mobile-pages-2/`
- Removed those three temp-profile directories only, then inspected `artifacts/` before touching anything else.
- `artifacts/mission-control-audit/` turned out to be a screenshot evidence archive for Mission Control / Lab / Dashboard audits, not generic runtime state.
- After Philippe explicitly approved it, removed all PNG files under `artifacts/mission-control-audit/` (82 files) and staged the already-deleted tracked cleanup under `projects/_ops/_superseded-2026-04-26/`.
- Important boundary preserved during this pass: did not touch Lab runtime dirs, Dashboard runtime dirs, `.next/` caches, `.lab-runtime/`, trading-bot runtime state, or broad tracked dirt outside the explicitly approved buckets.
- Result: workspace is somewhat cleaner, but broad unrelated tracked dirt still exists and should not be treated as implicitly approved cleanup.

## Lab Trading data-provider decision [14:20 GMT+7]
- Philippe decided that global market coverage matters more than analyst-estimate reliability for the Mission Control Lab Trading section.
- Revised provider direction: prefer EODHD `EOD+Intraday - All World Extended` as the first paid provider candidate over FMP Starter, because global coverage fits the intended workflow better.
- Intended hybrid stack:
  - EODHD Extended for global EOD/intraday/delayed market data, technicals/screener, and any included historical dividends/splits.
  - SEC EDGAR for US fundamentals, filings, and insider Form 4s.
  - Wikipedia/Wikidata/Wikimedia for company profiles, logos, and basic metadata.
  - Free macro sources later for economic/macro data.
  - Optional unofficial Yahoo-style estimate/recommendation source only as best-effort and clearly labeled, never as fabricated data.
- Product rule reaffirmed: estimates can remain unavailable/manual-broker-check if no reliable source exists; do not overpay solely to automate analyst estimates.
- Next action: once Philippe has signed up/subscribed, collect the API token and exact plan/access details, then run a read-only provider smoke test against representative tickers before wiring UI modules.

## EODHD subscription smoke test [14:38-14:50 GMT+7]
- Philippe saved the EODHD token in `.env` as `EODHD_API_KEY` and confirmed the plan is for personal/internal Lab use first.
- Screenshot entitlement readout for plan `EOD (Historical Data) Extended + Intraday`:
  - enabled: EOD Historical Data, Technical API Data, Split Data Feed, Dividends Data Feed, Exchanges List API Data, Exchange Details API Data, News API Data;
  - disabled/not included: All-In-One package, Fundamental Data, Calendar Data, Tick Data.
- Verified `.env` contains `EODHD_API_KEY` without printing the secret.
- Read-only endpoint smoke tests passed:
  - EOD: `AAPL.US`, `ASML.AS`, `TSM.US` returned 200 with recent OHLCV rows.
  - Realtime/delayed: `AAPL.US` returned quote fields.
  - Intraday: `AAPL.US` 5m returned 395 bars for a 7-day window.
  - Dividends: `AAPL.US` returned recent dividend rows with declaration/record/payment fields.
  - Splits: `AAPL.US` returned the 2020 4:1 split.
  - Technical API: `AAPL.US` SMA returned values.
  - Search: `TSM`, `ASML`, `VNM`, `VCB`, `BHP`, `NESN` returned useful exchange-suffixed candidates, including Vietnam tickers `VNM.VN` and `VCB.VN`.
  - Exchanges list returned 73 exchanges.
  - News API returned rows for `AAPL.US` but includes long article content and should be mapped conservatively.
  - Global EOD spot checks passed for `BHP.AU`, `NESN.SW`, and `VCB.VN`; `7203.TSE` failed because search mapped `7203` to `KLSE`, so ticker suffix discovery must rely on search/exchange metadata rather than guessed suffixes.
- Expected-off endpoints correctly failed with 403:
  - `/fundamentals/AAPL.US`
  - `/calendar/earnings?symbols=AAPL.US`
- Implementation implication: wire EODHD as a provider for search/global quotes/EOD/intraday/dividends/splits/technicals/news/exchange metadata, while keeping fundamentals/calendar/estimates unavailable or sourced from SEC/Yahoo-style alternatives.

## Lab Trading EODHD provider build [15:50-16:15 GMT+7]
- Continued BOILER ROOM / Trading work under `projects/mission-control-lab` only.
- Used EODHD's official Node library README as endpoint reference, but did not add the package dependency; implemented direct server-side fetches to keep the adapter small.
- Added `lib/trading/sources/eodhd.ts` and wired it as first ticker-profile source before Yahoo/sample.
- EODHD adapter now supports symbol resolution/search, delayed quote, EOD/intraday price ranges, dividends, splits, news, exchange metadata, and derived technical/range metrics.
- Ticker profile contract now supports supplemental sections for dividends, technicals, estimates, and ownership.
- Ticker page now renders provider-backed dividends/technicals when present while keeping estimates/ownership honest-unavailable.
- SEC adapter was relaxed to allow use from EODHD-backed profiles for US fundamentals/filings without requiring a sample base profile.
- Validation:
  - `npx tsc --noEmit` passed.
  - `npm run lint` passed.
  - `npm run build` passed with the two known unrelated Turbopack warnings around autonomous task dynamic runner tracing / next.config NFT tracing.
  - Production smoke on temporary port 3099 returned HTTP 200 for `/trading/ticker/AAPL`, `/trading/ticker/ASML`, `/trading/ticker/VCB.VN` and showed EODHD-backed Dividends/Technicals content.
  - Cache inspection confirmed `AAPL.US`, `ASML.US`, and `VCB.VN` profiles using EODHD for quote/prices/news; AAPL used SEC for financials; ASML/VCB correctly kept fundamentals unavailable under the EODHD plan.
- Commit: `e7ff16b Add Lab EODHD ticker provider`.

## Lab preview restart after EODHD build [16:17 GMT+7]
- Philippe asked to stop the current Lab build and restart the latest content.
- Used `projects/mission-control-lab/scripts/preview-restart.sh`, which runs PID/port-scoped Lab stop, build, and start. Did not touch live Dashboard.
- Restart completed successfully. New Lab runtime PIDs observed via health: proxy `69724`, Next `69720`, ws-sidecar `69714`, Piper `69716`, Moonshine `69718`.
- `scripts/lab-health.sh` passed for Lab ports `3015/3016/3017/3022/3023`.
- Route checks on `http://127.0.0.1:3015` returned:
  - `/trading` 200;
  - `/trading/ticker/AAPL` 200 with EODHD, Latest dividend, 1Y range high, Technicals visible;
  - `/trading/ticker/ASML` 200 with EODHD, Latest dividend, 1Y range high, Technicals visible;
  - `/trading/ticker/VCB.VN` 200 with EODHD, Latest dividend, 1Y range high, Technicals visible.
- Note: `preview-restart.sh` ends with a hardcoded `curl -I http://127.0.0.1:3005`, which returns a redirect and does not reflect Lab's configured proxy port; actual Lab health and smoke were checked on 3015.

## Lab ticker search control [16:31-16:40 GMT+7]
- Philippe approved using the top-bar magnifying glass for ticker search and noted that the later current-price auto-refresh should display the actual quote delay when the API exposes it.
- Implemented Lab ticker search:
  - added `app/api/trading/search/route.ts` backed by EODHD `/search`;
  - added `components/shell/TickerSearch.tsx` as a client top-bar search popover;
  - replaced the placeholder magnifying glass button in `TopTabBar` with the search control;
  - added shell styling for the search panel and result rows.
- Search result rows preserve canonical EODHD symbols and exchange suffixes, showing symbol, name, country, currency, type, primary flag, and previous-close date. Selecting a result navigates to `/trading/ticker/<SYMBOL.EXCHANGE>`.
- Validation:
  - `npx tsc --noEmit` passed;
  - `npm run lint` passed;
  - `npm run build` passed with the same known unrelated Turbopack warnings;
  - temporary production check on port 3099 confirmed `/api/trading/search?q=ASML` returns both `ASML.US` and `ASML.AS`;
  - restarted Lab preview with `scripts/preview-restart.sh` and `scripts/lab-health.sh` passed on ports `3015/3016/3017/3022/3023`;
  - live route `http://127.0.0.1:3015/api/trading/search?q=ASML` returns exchange-specific results and `/trading` serves the top-bar search markup.
- Commit: `e79eac4 Add Lab ticker search control`.
- Next intended slice: current-price auto-refresh with visible update time and provider delay wording. If EODHD does not expose exact delay in quote payloads, show a clear fallback such as “EODHD delayed quote, exact delay not reported.”

## Lab ticker search numeric/local listing expansion [17:00 GMT+7]
- Philippe noticed that searching `TSM` only showed ADR/secondary listings and asked if search can also find numeric local listings.
- Confirmed EODHD search for `TSM` alone misses Taiwan `2330.TW`, while `Taiwan Semiconductor Manufacturing` returns `2330.TW` and Taiwan OTC/local results.
- Updated Lab ticker search to merge direct ticker search with lightweight company-name expansion:
  - added server-side expansion/ranking in `app/api/trading/search/route.ts`;
  - added `TSM -> Taiwan Semiconductor Manufacturing` fallback expansion;
  - allowed spaces in `fetchEodhdSearch()` instead of stripping company-name queries to a concatenated token;
  - increased search results from 8 to 10 and added small UI copy explaining that ADRs plus local listings are shown when EODHD can infer the company name.
- Validation:
  - `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed;
  - restarted Lab preview and `scripts/lab-health.sh` passed;
  - live `http://127.0.0.1:3015/api/trading/search?q=TSM` returns `TSM.US`, `TSM.BA`, and now `2330.TW` among the visible top results.
- Commit: `f0c8235 Expand Lab ticker search aliases`.
- Known limitation: this is not a universal symbol crosswalk. Japanese primary listings still appear absent from the enabled EODHD exchange list; for broader ADR-to-local mapping later, add a curated alias/crosswalk layer or provider-backed exchange-symbol lookup if EODHD exposes one for the plan.

## Lab ticker current-price auto-refresh [17:15 GMT+7]
- Philippe asked to add current-price auto-refresh with latest update time, refresh state, and honest provider-delay wording.
- Implemented Lab-only quote refresh:
  - added `app/api/trading/quote/[symbol]/route.ts` for canonical exchange-suffixed EODHD realtime/delayed quote refresh;
  - added `components/pages/trading/TickerQuoteRefresh.tsx` client header component;
  - ticker header now polls every 90 seconds while the page is visible and supports manual refresh;
  - UI displays price, change, original EODHD quote time, local quote/fetch timestamps, refresh state (`Auto-refresh on`, `Refreshing…`, `Updated`, `Refresh failed`), and explicit delay text.
- Delay wording: EODHD realtime response did not expose an exact exchange delay in the tested payload, so the UI says: `EODHD delayed quote, exact exchange delay not reported by this endpoint.`
- Validation:
  - `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed;
  - temporary production API check on port 3099 returned JSON with price/change/updatedAt/fetchedAt/providerDelay for `AAPL.US`;
  - restarted Lab preview and `scripts/lab-health.sh` passed;
  - live `http://127.0.0.1:3015/api/trading/quote/AAPL.US` returned the refreshed quote payload;
  - live `/trading/ticker/AAPL` markup includes the auto-refresh state, quote timestamps, refresh button, and provider-delay wording.
- Commit: `44a2173 Add Lab ticker quote auto refresh`.

## Lab quote auto-refresh polish [17:35 GMT+7]
- Philippe reviewed the first quote auto-refresh UI from a phone photo and flagged three issues: it did not visibly auto-refresh, the `Auto-refresh on` control appeared unclickable/disabled, and the text stack under the price was too dense.
- Root causes/observations:
  - Older cached ticker profiles could lack `quote.symbol`, so the client component disabled refresh even though the page knew `ticker.symbol`.
  - EODHD realtime fetches were still eligible for Next fetch revalidation in the shared fetch helper, which weakened the “fresh fetch” semantics of manual/auto quote refresh.
  - UI copy used two metadata rows plus a button, crowding the quote header.
- Fixes applied:
  - `TickerQuoteRefresh` now receives `ticker.symbol` from the ticker page and falls back to it when cached quote data lacks `quote.symbol`.
  - EODHD realtime quote fetch now uses a no-store path in the provider helper.
  - Auto refresh triggers immediately on mount, then every 60 seconds while visible, plus on visibility return.
  - Manual control is now a clear `Refresh` button instead of an ambiguous disabled-looking `Auto-refresh on` label.
  - Metadata compressed to one single-line status: updated time, checked time, and short delay note (`Delayed quote, exact delay not reported`).
- Validation:
  - `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed;
  - temporary production check on port 3099 confirmed `/api/trading/quote/ASML.AS` and compact ticker markup;
  - Lab preview restart initially hit the known cold-start health race, but follow-up `scripts/lab-health.sh` passed and live 3015 checks confirmed the compact refresh line and quote API.
- Commit: `4cf2263 Tighten Lab quote refresh UI`.

## Lab quote refresh two-line status refinement [17:52 GMT+7]
- Philippe approved the direction but asked for two final UI adjustments: remove seconds from refresh status timestamps, split status over two lines with `Delayed quote` as the second line, and place the `Refresh` button underneath the status instead of beside it.
- Updated `TickerQuoteRefresh.tsx`:
  - local times now render as HH:MM only;
  - status text now renders as line 1 `Updated HH:MM · checked HH:MM` (or `Refreshing HH:MM · checked HH:MM` during refresh);
  - line 2 is simply `Delayed quote`;
  - refresh button sits below the two status lines.
- Updated ticker CSS so the quote refresh block is a small stacked layout with whitespace between text and button, aligned right on desktop and left in the collapsed breakpoint.
- Validation:
  - `npx tsc --noEmit`, `npm run lint`, and `npm run build` passed;
  - temporary production check on port 3099 and live Lab check on 3015 confirmed `Updated HH:MM`, `checked HH:MM`, `Delayed quote`, `Refresh`, and the new stack classes in markup;
  - `scripts/lab-health.sh` passed after restart.
- Commit: `bcf74da Refine Lab quote refresh status layout`.

## Lab quote refresh follow-up todo [17:57 GMT+7]
- Philippe approved the quote refresh polish and asked to defer one small UI tweak to a next build.
- Todo: change the ticker quote status from two lines to three short lines:
  - `Updated [time]`
  - `Refresh [time]`
  - `Delayed quote`
- Keep the refresh button below the status block with breathing room.

## Lab quote refresh follow-up wording correction [18:15 GMT+7]
- Philippe corrected the deferred quote-refresh status todo: the second line should be `Refreshed: [time]`, not `Refresh [time]`.
- Desired next-build status block:
  - `Updated: [time]`
  - `Refreshed: [time]`
  - `Delayed quote`
- Styling note: make `Delayed quote` a subtly different grey than the first two lines, but not bolder.

## Lab Trading GitHub data-source crawl [18:20 GMT+7]
- Philippe proposed GitHub-based data/source candidates for missing Trading data: `defeat-beta/defeatbeta-api`, `JerBouma/FinanceDatabase`, `ranaroussi/yfinance`, `JerBouma/FinanceToolkit`, and `Leo4815162342/dukascopy-node`.
- GitHub crawl used `gh` metadata/API plus README/license/package docs. Current high-level assessment:
  - DefeatBeta: active Python/Apache-2.0 project, strong candidate for a separate sidecar/prototype; claims Yahoo-backed/HuggingFace-hosted market/fundamental data, statements, earnings calendars, splits/dividends, revenue breakdowns, transcripts, DCF, reports, and LLM analysis. Best fit: research/analytics sidecar, not immediate core provider until smoke-tested for symbol coverage/licensing/source freshness.
  - FinanceDatabase: active MIT project, strong static/reference enrichment source for company profile fields and symbol universe, e.g. country, sector, industry, exchange/market, currency, type-like classifications. It explicitly is not up-to-date fundamentals/stock data. Best fit: local/static metadata cache to enrich profile/header gaps.
  - yfinance: very active Apache-2.0 wrapper around Yahoo public endpoints. Useful as an unofficial best-effort fallback for Yahoo-only fields like estimates/recommendations/ownership, but README repeats Yahoo personal-use/terms caveat. Best fit: internal research/prototype fallback only, clearly labelled.
  - FinanceToolkit: active MIT analytics library but generally depends on Financial Modeling Prep API for core data, with Yahoo fallback. Best fit: later Analytics page formulas/ratios/models if data is supplied by our own providers; not a new missing-data provider by itself unless FMP is adopted.
  - dukascopy-node: active MIT TypeScript/Node project for historical/realtime tick data across FX/crypto/CFDs/some assets. Best fit: later market microstructure/backtesting/FX/crypto tick-data lane, not company fundamentals/profile/estimates.
- Recommendation direction: start with FinanceDatabase enrichment first because it is low-risk and directly improves current bare company profiles; then test DefeatBeta in a separate Python sidecar/container for analytics and missing data; keep yfinance unofficial/research-only; treat FinanceToolkit and dukascopy-node as later Analytics/backtesting lanes.

## Lab Trading provider decision savepoint [18:26 GMT+7]
- Philippe approved wiring `FinanceDatabase` and `yfinance` into Mission Control Lab Trading first.
- Revised source posture:
  - `FinanceDatabase`: use as static/reference enrichment for company/profile/header fields such as ISIN, country, sector, industry, exchange/market, currency, symbol universe, and type-like classifications.
  - `yfinance`: treat as a serious/popular usable source for missing Yahoo-derived ticker data such as analyst estimates, price targets, recommendations, ownership-ish modules, dividends, market cap, profile fields, sector/industry, and other gaps left by EODHD Extended.
  - `DefeatBeta` and `FinanceToolkit`: defer to the later Analytics page workstream; DefeatBeta remains a promising Python/sidecar experiment, FinanceToolkit a later calculation/analytics layer.
- Implementation constraint remains Lab-only under `projects/mission-control-lab`; do not touch live Dashboard without explicit approval.
- Before implementation, preserve current EODHD/SEC/Wikipedia behavior and honest unavailable states. New providers should enrich/fill gaps, not silently replace trusted provider-backed fields without clear source mapping.

## Lab Trading FinanceDatabase + yfinance enrichment [17:59 GMT+7]
- Implemented Lab-only provider enrichment for Mission Control Lab Trading and committed it as `f56e4eb Add Lab FinanceDatabase and yfinance enrichment`.
- Added a Python yfinance bridge (`projects/mission-control-lab/scripts/trading_yfinance_bridge.py`) and server wrapper (`lib/trading/sources/yfinance-bridge.ts`) to fill missing profile/header fields, valuation ratios, analyst estimates/price targets/recommendation trend, ownership percentages, and dividend summary fields. US EODHD symbols such as `AAPL.US` are mapped to Yahoo-style `AAPL` for yfinance enrichment.
- Added FinanceDatabase static search/profile support:
  - `scripts/build-finance-database-index.mjs` builds an ignored local index from `JerBouma/FinanceDatabase` equities CSV.
  - `lib/trading/sources/finance-database.ts` reads the local index for search/reference facts.
  - Search now merges FinanceDatabase results with EODHD results without replacing EODHD as primary market-data backbone.
- Added `lib/trading/sources/reference-enrichment.ts` so every ticker profile returned by the existing source chain is enriched after provider fetch and before cache write. This preserves EODHD/SEC/Wikipedia behavior while filling gaps.
- Validation passed: `npx tsc --noEmit`, `npm run lint`, `npm run build` (known unrelated Turbopack warnings only), temp Next start on port 3099, live Lab preview restart and `scripts/lab-health.sh`.
- Live route smoke on port 3015 returned 200 for `/api/trading/search?q=TSM`, `/trading/ticker/AAPL.US`, `/trading/ticker/ASML.AS`, and `/trading/ticker/VCB.VN`.
- Cache inspection confirmed yfinance-backed enrichment now appears for AAPL.US, ASML.AS, and VCB.VN: market cap, P/E/country header fields, estimates available with 10 metrics, ownership available with 4 metrics. EODHD remains quote/price/dividend/technical/news backbone; SEC still owns US fundamentals/filings where available.
- FinanceDatabase generated index is intentionally ignored (`data/trading/finance-database-symbol-index.json`) because it is large/generated; rerun `node scripts/build-finance-database-index.mjs` to refresh it.
