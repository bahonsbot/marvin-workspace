# 2026-04-29

## Morning Meeting Decision [06:18 GMT+7]
- Report: self-improvement.
- Finding: SI-2026-0429-001 / stale Lab Whisper STT documentation.
- Decision: Approved.
- Problem: `projects/mission-control-lab/docs/local-whisper-stt-notes.md` documented the superseded local Whisper STT path (`@fugood/whisper.node`, whisper.cpp model env, `MISSION_CONTROL_WHISPER_MODEL*`) while Lab voice work has moved to Moonshine STT + Piper TTS.
- Risk: low; documentation-only but misleading for future Lab voice debugging.
- Action taken: deleted the stale Lab Whisper STT notes file.
- Rollback: restore `projects/mission-control-lab/docs/local-whisper-stt-notes.md` from git if the historical reference is needed.

## Morning Meeting Decision [06:19 GMT+7]
- Report: self-improvement.
- Finding: SI-2026-0429-002 / stale pending status on `.learnings/errors.md` entry `ERR-20260421-1811`.
- Decision: Approved.
- Problem: the Apr 21 broken `openclaw.json` config-edit error was still marked `Status: pending` even though the durable config-safety rule has since been captured in `MEMORY.md` and `TOOLS.md`.
- Risk: low; unresolved critical-error noise in daily reviews.
- Action taken: updated `ERR-20260421-1811` with a resolution note and changed status to `resolved`.
- Rollback: revert the `.learnings/errors.md` entry if the config-safety rule is later found incomplete.

## Morning Meeting Decision [06:26 GMT+7]
- Report: self-improvement.
- Finding: SI-2026-0429-003 / loop detection lesson not formally captured.
- Decision: Approved with adjustment.
- Problem: `projects/_ops/openclaw-loop-pattern-audit-2026-04-27.md` documented an autonomous session repeating low-information probes after a missing reference doc, but the lesson lived only in the audit doc.
- Risk: low but recurring; future autonomous runs could waste context and time by continuing same-class diagnostics after `ENOENT`, `(no output)`, or trivial broad-scan results.
- Action taken: added a correction entry to `.learnings/corrections.md` and added a small `No-Progress Pivot Gate` to `AUTONOMY.md` requiring a strategy change after two consecutive low-information probes for the same question.
- Rollback: remove the correction entry and the `AUTONOMY.md` pivot gate if the rule proves too restrictive.

## Morning Meeting Decision [06:31 GMT+7]
- Report: platform-health-council.
- Finding: INFO-001 / stale `TOOLS.md` Next.js version note.
- Decision: Adjusted.
- Problem: `TOOLS.md` still described Mission Control as `next 16.2.0`, while live Dashboard had been updated to `16.2.4`; Lab still being on `16.2.0` could create avoidable drift before future Lab-to-live promotion.
- Risk: low operational doc drift, plus mild promotion risk if Lab and live Dashboard use different Next.js patch versions.
- Action taken: aligned `projects/mission-control-lab` to `next 16.2.4`, `eslint-config-next 16.2.4`, and the `postcss 8.5.12` override; ran `npm install` so installed Lab runtime also reports Next `16.2.4`; updated `TOOLS.md` to state both Dashboard and Lab lanes are aligned.
- Verification: Lab `npm audit --audit-level=moderate` found 0 vulnerabilities; installed `next/package.json` reports `16.2.4`; `npm run lint` passed; `npm run build` passed under Next 16.2.4; `scripts/lab-health.sh` passed; `/trading`, `/trading/ticker/AMZN`, `/trading/ticker/NVDA`, and `/general/home` returned HTTP 200.
- Notes: Lab restart still emitted known EADDRINUSE/PID-capture noise, but health checks and route checks proved the bound Lab services were healthy. Existing unrelated Turbopack warnings remained intentionally untouched.
- Rollback: revert Lab `package.json`/`package-lock.json` and restore `TOOLS.md` line if the Next 16.2.4 Lab alignment causes issues.

## Morning Meeting Decision [07:00 GMT+7]
- Report: platform-health-council.
- Finding: INFO-002 / `projects/market-intel/data/enhanced_signals.json` is not strict-JSON-parseable.
- Decision: Deferred with note.
- Problem: Platform Health observed that `enhanced_signals.json` fails strict `json.load()` parsing, even though the market-intel signal pipeline currently completes successfully.
- Risk: low right now because no live signal-generation failure was observed; potential future tooling/UI issue if a strict JSON consumer reads the file.
- Decision rationale: do not mutate market-intel data during Morning Meeting while jobs are green. Inspect as a focused follow-up only if signal anomalies appear or a consumer needs strict JSON.
- Follow-up note: if revisited, inspect writer/reader expectations first, then repair the data format with a safe backup and validation against the signal pipeline.

## Morning Meeting Closeout [07:02 GMT+7]
- Philippe asked to make sure the day is properly saved before starting a fresh session.
- Confirmed `memory/2026-04-29.md` contains the Apr 29 Morning Meeting decisions and actions:
  - SI-2026-0429-001 stale Lab Whisper STT doc: approved and deleted from disk.
  - SI-2026-0429-002 stale pending config-error log: approved and marked resolved in `.learnings/errors.md`.
  - SI-2026-0429-003 no-progress loop lesson: approved with adjustment; captured in `.learnings/corrections.md` and operationalized in `AUTONOMY.md` as the No-Progress Pivot Gate.
  - Platform Health INFO-001 stale Next.js note: adjusted; Lab aligned to Dashboard on Next `16.2.4`, `TOOLS.md` updated, lint/build/audit/runtime checks passed.
  - Platform Health INFO-002 non-strict `enhanced_signals.json`: deferred with note because live signal jobs are green.
- Current continuation posture: Morning Meeting is complete. Next fresh session can proceed from committed daily memory and should not repeat the Apr 29 report review unless Philippe asks.

- Home-improvement pass:
  - What changed: removed four stale links from `docs/runbooks/README.md` that pointed to runbooks no longer present in `docs/runbooks`.
  - Why: the runbook index had dead entries, which added lookup friction during operator and autonomous doc use.
  - Expected benefit: faster navigation with a clean index and no broken local runbook references.
  - Rollback: restore the removed README entries if those runbooks are recreated.

## Mission Control Lab Trading / BOILER ROOM continuation [07:06-07:58 GMT+7]
- Philippe asked to resume Lab-only Trading / BOILER ROOM work from the Apr 28 savepoint and implementation docs.
- Context loaded for continuity:
  - `memory/2026-04-28.md`
  - `projects/mission-control-lab/docs/trading-section-lab-implementation-plan-2026-04-27.md`
  - `projects/mission-control-lab/docs/trading-section-lab-savepoint-addendum-2026-04-28.md`
- Standing decisions reaffirmed:
  - BOILER ROOM remains Lab-only under `projects/mission-control-lab`; do not promote to live `projects/mission-control` without Philippe's explicit approval.
  - `/trading` overview is directionally approved and remains the visual truth/design anchor.
  - Visual language: quiet institutional finance terminal, Atreus-led; Zero Sum is for module/data/cache inspiration rather than default visual language.
  - Yahoo quote/chart/search path is good enough for now; do not subscribe to or build around FMP yet unless a later provider gap justifies it.
- External/reference checks:
  - Screenshot assets were found under `/data/.openclaw/workspace/uploads/mission-control/`, including stock-dashboard, Zero Sum, and Portfolio Dividend Tracker images from Apr 27.
  - Zero Sum public repo reinforced useful ideas: server-side cached market/fundamental adapters, ticker detail modules, cash/debt, balance-sheet visualizations, ratings radar, metrics, market maps, watchlists, comparison, technical analysis.
  - `defeat-beta/defeatbeta-api` looked potentially useful as a later free/open provider path, but no implementation decision was made.
  - `lightweight-charts-python` is not the right immediate fit for the Next.js UI; if real client charts are needed later, prefer JS `lightweight-charts` directly.

## Mission Control Lab ticker Cash & Debt / Balance Sheet slice [07:31-07:55 GMT+7]
- Philippe approved continuing with the recommended first richer analytical module on `/trading/ticker/[symbol]`.
- Implemented and committed Lab-only ticker data/UI work:
  - Commit: `4c2fc35 Add Lab ticker balance sheet snapshot`
  - Extended `TickerProfile` contract with cash/debt and balance-sheet snapshot types.
  - Added symbol-aware sample fundamentals for NVDA, MSFT, TSM, AAPL, plus fallback values for unknown symbols.
  - Kept Yahoo as quote/price provider; financial modules are honestly marked as sample/fallback until a fundamentals provider exists.
  - Added ticker UI modules: Cash vs Debt ratio bar, Total Cash / Total Debt / Net Cash-or-Debt / FCF / Operating CF rows, Debt/Equity + Current Ratio + Net Cash/Debt KPI rail, annual Assets/Liabilities/Equity bars, Cash vs Debt trend bars.
  - Added responsive/mobile CSS for the new modules.
- Validation reported as passed:
  - `npx tsc --noEmit --pretty false`
  - `npm run lint`
  - `npm run build`
  - `scripts/lab-health.sh`
  - Route checks for `/trading/ticker/NVDA`, `MSFT`, `AAPL`, `AMZN`, and `FAKEZZZ` returned HTTP 200.
  - Cache inspection confirmed Yahoo quote layer plus sample financial fallback.
  - Known Lab warnings remained intentionally untouched.
- Philippe liked the result and asked whether the ticker page now has enough data/graphs.
- Recommendation given: ticker detail is feature-complete enough for v1; do not add more graphs now. The remaining gap is better real data behind existing modules, not more modules. Best next candidates are a fundamentals provider/SEC filings adapter or shaping Portfolio/Watchlist.

## Mission Control Lab ticker Wikimedia logo enrichment [08:58-09:10 GMT+7]
- Philippe approved continuing the Trading ticker lane by improving existing modules rather than adding more charts, and asked for Wikipedia/Wikimedia-backed company logos instead of placeholder ticker initials.
- Implemented and committed Lab-only ticker logo enrichment:
  - Commit: `87f0ee3 Add Lab ticker Wikimedia logos`
  - Added `companyLogo` to the ticker profile contract.
  - Added a server-side Wikipedia/Wikidata/Wikimedia Commons resolver for company logo files.
  - Yahoo-backed ticker profiles now enrich logos from Wikimedia when available.
  - Known ticker mappings/checks included NVDA, MSFT, AAPL, AMZN, and TSM.
  - Unknown tickers fall back to initials cleanly; fixed the fallback so unknown tickers do not inherit the NVDA sample logo.
  - Header rendering now uses the real logo when available and preserves the initials mark as fallback.
- Validation:
  - `npx tsc --noEmit --pretty false`
  - `npm run lint`
  - `npm run build`
  - `scripts/lab-health.sh`
  - Route/cache checks for NVDA, MSFT, AAPL, AMZN, TSM, and FAKEZZZ.
  - Screenshot review confirmed the AMZN logo rendered neatly; only unrelated visual note was existing toast notifications can overlap page content.
- Continuation note: ticker detail is now considered feature-complete enough for v1. Remaining value is better real data behind existing modules, especially fundamentals/SEC filings, not more ticker charts.

## Mission Control Lab ticker SEC fundamentals slice [09:05-09:25 GMT+7]
- Philippe approved proceeding with the recommended next Trading ticker source slice: wire better real data behind existing modules rather than adding more modules.
- Implemented and committed Lab-only SEC EDGAR fundamentals/filings enrichment:
  - Commit: `5ccfe1a Add Lab ticker SEC fundamentals`
  - Added `projects/mission-control-lab/lib/trading/sources/sec.ts`.
  - Resolves ticker to SEC CIK through SEC `company_tickers.json`.
  - Fetches SEC `companyfacts` and `submissions` with a proper user-agent.
  - Maps annual 10-K XBRL facts into existing ticker modules: revenue, net income, operating cash flow, capex-derived free cash flow, cash, debt, assets, liabilities, shareholder equity, current ratio, debt/equity, and revenue vs net-income chart data.
  - Replaces static Financial Statements resources with recent SEC 10-K/10-Q/8-K filing links when available.
  - Keeps Yahoo for quote/chart/search/profile metadata and Wikimedia for logos.
  - Keeps sample fallback for symbols/fields SEC cannot satisfy; TSM currently remains sample-backed for this slice despite SEC CIK resolution because the US-GAAP mapping did not produce enough clean annual module data.
  - Fixed financial overview bar rendering so negative net income no longer creates negative CSS heights; negative values are rendered with a red bar marker.
- Validation:
  - `npx tsc --noEmit --pretty false`
  - `npm run lint`
  - `npm run build` passed with the known unrelated Next/Turbopack warnings.
  - `scripts/lab-health.sh` passed.
  - Route/cache checks for NVDA, MSFT, AAPL, AMZN, TSM, and FAKEZZZ returned HTTP 200.
  - NVDA/MSFT/AAPL/AMZN showed `sourceMap.financials = sec/fresh`; TSM and FAKEZZZ remained `sample/sample` fallback.
  - AMZN HTML confirmed SEC source labels and SEC filing links render, and after restart no negative CSS heights remained.
- Runtime note: Lab restart again hit known EADDRINUSE/PID-capture noise. Recovered with port-scoped cleanup of Lab preview ports and `scripts/lab-start.sh`; final Lab health passed. Do not broad-kill unrelated processes.
- Follow-up direction: company profile narrative/facts and news are still sample-backed for unknown/non-fixture symbols. Next useful source slice is a company-profile facts adapter, likely Wikipedia/Wikidata plus SEC submissions/entity metadata, then a lightweight news provider.

## Mission Control Lab ticker structure / data availability phase 1 [10:22-10:45 GMT+7]
- Philippe clarified that financial decision modules should remain visible even when provider-backed data is unavailable. The page should say data is unavailable rather than silently removing graphs or showing fake/sample numbers.
- Implemented and committed Lab-only ticker structure/availability cleanup:
  - Commit: `163391b Add Lab ticker section anchors and availability states`.
  - Replaced the placeholder tab row with same-page section jump links: Overview, Price, Profile, Financials, Balance Sheet, News, Filings, Ratios, Estimates, Dividends, Ownership.
  - Added section anchors and unavailable placeholder sections for Estimates, Dividends, and Ownership.
  - Removed the redundant `[ticker] performance` title from Price History.
  - Added data-status support for financial highlights, financial overview, and key ratios.
  - Preserved key Financial Highlights slots (Revenue, Net Income, Gross Margin, Operating Margin, EPS, Free Cash Flow, ROE, ROIC, Debt/Equity). Missing provider-backed values now render as unavailable instead of disappearing.
  - Changed sample/unknown financial overview and key ratios to unavailable status instead of sample-looking numbers.
  - Fixed Balance Sheet legend/bar color mismatch by aligning bars to the visible legend colors.
  - Kept SEC-backed tickers showing available annual overview data while unsupported tickers show explicit unavailable messaging.
- Validation:
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run lint` passed.
  - `npm run build` passed with the known unrelated Next/Turbopack warnings.
  - Lab health passed after port-scoped restart recovery.
  - Route checks for AMZN, NVDA, TSM, and FAKEZZZ returned HTTP 200.
  - HTML checks confirmed section jump nav, unavailable messaging, anchors for price/estimates/dividends/ownership, no `[ticker] performance` title, and no negative CSS heights.
  - Cache inspection confirmed AMZN/NVDA remain SEC-backed with available financial overview; TSM/FAKEZZZ now show unavailable financial overview/key ratio states instead of sample-looking values.
- Runtime note: Lab preview restart again encountered EADDRINUSE/PID-capture noise. Recovered using Lab port-scoped cleanup on 3015/3016/3017/3022/3023 and `scripts/lab-start.sh`; final health passed.
- Next phased work remains: real interactive price chart/range switching, company profile provider, news provider, richer SEC filing labels, and investor presentation discovery.

## Mission Control Lab ticker availability/navigation phase [10:21-10:40 GMT+7]
- Philippe approved proceeding in a phased approach on ticker-page UX/data-source improvements after reviewing the current ticker page from top to bottom.
- Product decisions captured:
  - Replace the placeholder tab row with same-page section jump links because the ticker page is a single research dossier rather than separate tabbed screens.
  - Keep important financial graph/card slots visible even when no reliable provider-backed data is available yet.
  - Prefer explicit `Data unavailable` / `Provider required` states over hiding modules or silently showing sample data on real ticker pages.
  - This applies especially to Financial Highlights, Financial Overview, Key Ratios, Estimates, Dividends, Ownership, and future provider-backed modules.
  - Use model assistance, if needed later, only as a formatter/classifier over factual fetched data with source URLs, never as an invented data source.
- Implemented and committed Phase 1 Lab-only work:
  - Commit: `163391b Add Lab ticker section anchors and availability states`
  - Replaced fake ticker tabs with same-page section anchors: Overview, Price, Profile, Financials, Balance Sheet, News, Filings, Ratios, Estimates, Dividends, Ownership.
  - Removed the redundant `[ticker] performance` title from the Price History section.
  - Added placeholder sections for Estimates, Dividends, and Ownership with clear unavailable/provider-required copy.
  - Preserved required Financial Highlights slots: Revenue, Net Income, Gross Margin, Operating Margin, EPS, Free Cash Flow, ROE, ROIC, Debt / Equity.
  - Added unavailable sparkline/card treatment so missing metrics remain visible without fake numbers.
  - Updated Financial Overview and Key Ratios display to support availability states and avoid misleading sample-looking data on unsupported tickers.
  - Fixed Balance Sheet legend/bar color mismatch so legend and chart colors align.
- Validation reported:
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run lint` passed.
  - `npm run build` passed with only known unrelated Lab warnings.
  - `scripts/lab-health.sh` passed after preview recovery.
  - Route checks for AMZN, NVDA, TSM, and FAKEZZZ returned HTTP 200.
  - HTML checks confirmed section anchors, unavailable messaging, removed performance title, and no negative CSS heights.
- Runtime note: Lab preview again hit known port/PID noise during restart. It was recovered with port-scoped cleanup only; final Lab health was green. Do not broad-kill unrelated processes.
- Follow-up direction:
  - Phase 2 should likely add real interactive Price History with Lightweight Charts and provider-backed range switching from Yahoo chart data.
  - Later phases: company profile provider enrichment, news provider decision, richer SEC filing labels, investor-presentation discovery, and possibly model-assisted normalization over fetched factual data.

## Memory flush note [10:41 GMT+7]
- This pre-compaction flush appended only to canonical `memory/2026-04-29.md` as requested.

## Mission Control Lab ticker interactive price chart phase [10:46-10:56 GMT+7]
- Philippe approved Phase 2 after the availability/navigation phase: make Price History genuinely interactive instead of placeholder-like.
- Implemented and committed Lab-only interactive price chart work:
  - Commit: `1d2f28f Add Lab ticker interactive price chart`.
  - Added `lightweight-charts@5.2.0` to the Lab package.
  - Added client component `projects/mission-control-lab/components/pages/trading/TickerPriceChart.tsx`.
  - Replaced ticker detail `MiniLineChart` usage with Lightweight Charts for `/trading/ticker/[symbol]` only.
  - Extended ticker price contract with range-specific series data.
  - Yahoo adapter now fetches/cache-builds real chart ranges: `1D`, `5D`, `1M`, `6M`, `YTD`, `1Y`, `5Y`.
  - Range buttons now switch real client-side chart data where available.
  - Range stats update per selected range from Yahoo OHLCV data.
  - Source/freshness caption remains Yahoo-backed for supported tickers; sample/unsupported symbols keep disabled/unavailable range data behavior.
  - Disabled the Lightweight Charts attribution logo to avoid visible overlap in the compact product UI.
- Validation:
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run lint` passed.
  - `npm run build` passed with known unrelated Next/Turbopack warnings only.
  - `scripts/lab-health.sh` passed after Lab-scoped restart.
  - Route checks for AMZN, NVDA, TSM, and FAKEZZZ returned HTTP 200.
  - Cache inspection confirmed AMZN/NVDA/TSM have Yahoo-backed ranges `1D,5D,1M,6M,YTD,1Y,5Y`; AMZN example showed 1D=79 points and 1Y=251 points. FAKEZZZ remains sample fallback with no real range series.
  - Chromium screenshot visual QA confirmed the AMZN chart line renders, range buttons show, 1Y active state is visible, no attribution overlap remains, and the card layout is intact.
- Runtime/generated artifacts left uncommitted/ignored:
  - `projects/mission-control-lab/data/trading/ticker-profiles/*.json`
  - `projects/mission-control-lab/tmp/mobile-check/phase2-amzn-*.png`
- Next likely phases: company profile provider enrichment, ticker news provider, richer SEC filing labels, and investor-presentation discovery.

## Mission Control Lab ticker chart fix [11:08-11:20 GMT+7]
- Philippe reported chart bugs after Phase 2:
  - 1M through 5Y worked.
  - 5D showed no line.
  - 1D showed a vertical green line.
  - Repeated switching between 1D/5D/1M caused the date/time legend below the chart to accumulate several rows.
  - Screenshots were uploaded under `uploads/mission-control/2026-04-29T04-06-45-279Z-1D.jpg` and `uploads/mission-control/2026-04-29T04-06-45-284Z-5D.jpg`.
- Diagnosis:
  - Intraday Yahoo points were being passed to Lightweight Charts as ISO datetime strings, which collapsed/handled badly for 1D/5D. Intraday needs numeric Unix-second timestamps.
  - The custom axis labels under the chart were redundant with Lightweight Charts and created the visible multi-row/accumulation problem while switching ranges.
- Implemented and committed fix:
  - Commit: `e2d1412 Fix Lab ticker intraday chart rendering`.
  - `TickerPricePoint.time` now supports `string | number`.
  - Yahoo intraday range points (`1D`, `5D`) now emit sorted/deduped numeric Unix-second timestamps.
  - Daily/weekly ranges still use date strings.
  - `TickerPriceChart` sorts/validates incoming data before `series.setData()`.
  - Removed the custom `trading-ticker-chart-axis` block from the chart component so labels no longer accumulate under the chart.
  - Added `rightOffset: 4` so the rightmost x-axis label and price marker have breathing room.
- Validation:
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run lint` passed.
  - `npm run build` passed with known unrelated warnings only.
  - Lab health passed after port-scoped preview recovery.
  - Cache inspection for AMZN showed `1D` 79 unique numeric timestamps, `5D` 131 unique numeric timestamps, and `1M` date-string daily points.
  - CDP interaction test clicked `1D → 5D → 1M → 5D → 1D`; active range and stats updated correctly, and `customAxisRows` stayed `0`.
  - Screenshot QA confirmed the final 1D chart renders as a normal line, no vertical-only artifact remains, no repeated legend appears, and the rightmost `20:00` label is acceptable.
  - Route checks for AMZN, NVDA, TSM, and FAKEZZZ returned HTTP 200.
- Runtime/generated artifacts remain ignored/uncommitted: ticker profile cache files and `projects/mission-control-lab/tmp/mobile-check/chartfix-amzn-*.png`.

## Mission Control Lab ticker company profile enrichment [12:32-12:45 GMT+7]
- Philippe confirmed the chart fix worked and approved continuing with Company Profile provider enrichment as the next Lab-only ticker phase.
- Implemented and committed Lab-only profile enrichment:
  - Commit: `dc104fa Add Lab ticker company profile enrichment`.
  - Extended `projects/mission-control-lab/lib/trading/sources/wikipedia.ts` beyond logo lookup so it can fetch Wikipedia intro extracts and selected Wikidata facts for company profiles.
  - Yahoo-backed ticker profiles now use Wikipedia/Wikidata for `companyProfile.summary`, profile source metadata, and factual profile rows when available.
  - Profile facts now prefer source-backed, relatively stable fields: Founded, Headquarters, Website, Wikidata.
  - Removed stale fixture-style profile rows such as Next Earnings from provider-backed profiles.
  - Added a source caption under the Company Profile card so the profile clearly shows `wikipedia`/source metadata.
  - Kept unsupported symbols honest: `FAKEZZZ` remains sample/placeholder fallback.
  - Tightened Yahoo header fallback so missing market cap / P/E render as `Provider pending` with unavailable status instead of inheriting sample-looking values on Yahoo-backed tickers.
- Validation:
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run lint` passed.
  - `npm run build` passed with known unrelated warnings only.
  - Lab health passed after restart.
  - Route checks returned HTTP 200 for AMZN, NVDA, MSFT, AAPL, TSM, and FAKEZZZ.
  - Cache inspection showed AMZN/NVDA/MSFT/AAPL/TSM profile source as `wikipedia/fresh`; FAKEZZZ remained `sample/sample`.
  - Screenshot QA on AMZN confirmed the profile card is readable, uses factual sourced copy, and no longer shows placeholder text in the profile section.
- Known note:
  - Yahoo chart/search metadata did not supply market cap or trailing P/E for the tested tickers in this run, so header fields correctly show `Provider pending`; this is preferable to sample-looking numbers.
  - Runtime/generated artifacts remain ignored/uncommitted: ticker profile cache and `projects/mission-control-lab/tmp/mobile-check/profile-enrichment-amzn.png`.
- Next likely phase: SEC filing/resource label cleanup, then news provider decision/integration.

## Mission Control Lab ticker Wikipedia summary splitter fix [12:58-13:03 GMT+7]
- Philippe spotted a funny profile-summary bug: TSM's Wikipedia intro contains `U.S.`, and the ticker summary was truncating at `non-U.` instead of keeping `non-U.S. companies by market capitalization`.
- Implemented and committed fix:
  - Commit: `9747766 Fix Lab ticker Wikipedia summary splitting`.
  - Added abbreviation protection/restoration around the lightweight Wikipedia intro sentence splitter in `lib/trading/sources/wikipedia.ts`.
  - Protected common abbreviations/domains such as `U.S.`, `U.K.`, `U.A.E.`, `E.U.`, `.com`, `Inc.`, `Ltd.`, `Co.`, and `Corp.` before splitting.
- Validation:
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run lint` passed.
  - `npm run build` passed with known unrelated warnings only.
  - Lab health passed after restart.
  - Regenerated TSM profile cache and verified the summary now includes `non-U.S.` and `market capitalization`.

## Mission Control Lab ticker Phase 4 SEC filings/resources polish [13:09-13:31 GMT+7]
- Philippe approved making Phase 4 “beautiful,” focused on turning Resources/SEC filings from repeated generic `8-K` links into a useful investor-desk section.
- Implemented and committed Lab-only SEC filings/resource polish:
  - Commit: `2bd1d7b Polish Lab ticker SEC filings resources`.
  - Extended `TickerResourceItem` with optional filing metadata: `kind`, `form`, `filedAt`, `reportDate`, `accession`, and `document`.
  - SEC adapter now reads SEC submissions `primaryDocDescription` and `items` where available.
  - Filing labels now classify common filing types:
    - `10-K` → `Annual report · FY ####`
    - `10-Q` → `Quarterly report · YYYY-MM`
    - `8-K` item `2.02` → `Earnings release / results 8-K`
    - item `7.01` → `Investor update 8-K`
    - item `5.02` → `Leadership / governance 8-K`
    - item `5.07` → `Shareholder vote 8-K`
    - item `1.01` → `Material agreement 8-K`
    - item `8.01` → `Other material event 8-K`
  - Selection now surfaces a balanced set: latest annual report, recent quarterly reports, recent earnings 8-Ks, and a couple of notable current 8-Ks, instead of mostly old annual reports.
  - SEC-backed ticker resources now suppress stale/sample investor-presentation fixtures until a real IR presentation adapter exists.
  - Resources card title now reads `SEC filings` for source-backed pages.
  - UI now uses filing-type pills and clearer two-line row layout for form/title/metadata.
- Validation:
  - `npx tsc --noEmit --pretty false` passed.
  - `npm run lint` passed.
  - `npm run build` passed with known unrelated warnings only.
  - Lab health passed after clean restart.
  - Route checks returned HTTP 200 for AMZN, NVDA, TSM, and FAKEZZZ.
  - Cache inspection confirmed AMZN/NVDA show SEC-backed filing labels and no sample `GTC 2024` presentation leakage; TSM/FAKEZZZ remain sample fallback.
  - Screenshot QA after clean restart confirmed AMZN Resources shows `SEC filings`, no `8-KInvestor` concatenation, readable metadata, and no sample investor-presentation content.
- Runtime/generated artifacts remain ignored/uncommitted: ticker profile cache and `projects/mission-control-lab/tmp/mobile-check/resources-amzn-*.png`.
- Next likely phase: news provider decision/integration, then real IR/presentation discovery if still desired.

## Mission Control Lab ticker SEC filings typography follow-up [14:14 GMT+7]
- Philippe noticed that removing the duplicate SEC filings label also removed the small section label above the title, which made the panel less visually aligned with the neighboring Recent News section.
- Follow-up for the next ticker batch/news-provider phase, not worth a standalone build:
  - Change the current serif `SEC filings` heading treatment to the smaller sans/mono section-label style, matching `RECENT NEWS` in the adjacent news section.
  - Keep the fixed-height internal scroll behavior from commit `95f7e0a`.

## Mission Control Lab ticker news provider phase [14:25 GMT+7]
- Continued Phase 5 on Lab-only ticker pages after Philippe approved the next batch.
- Implemented Yahoo Finance search-news integration in `projects/mission-control-lab/lib/trading/sources/yahoo.ts`:
  - requests Yahoo search with `newsCount=6`;
  - maps provider-backed headline, publisher, link, publish time, type, and related tickers into `recentNews`;
  - marks `sourceMap.news` as `yahoo/fresh` when linked headlines are available;
  - if Yahoo returns no usable linked headlines for a real ticker, keeps the news slot visible with an explicit missing/provider-pending row instead of silently showing sample headlines.
- Extended `TickerNewsItem` with optional `relatedTickers` and `kind` metadata.
- Updated ticker news UI so provider-backed headlines render as external links without inventing article summaries.
- Folded in Philippe's SEC filings typography follow-up:
  - SEC filings card now uses the same small sans/mono section-label treatment as `Recent news` instead of the larger serif heading;
  - fixed-height internal filings scroll from `95f7e0a` remains.
- Validation:
  - `npx tsc --noEmit --pretty false` passed;
  - `npm run lint` passed;
  - `npm run build` passed with only known unrelated warnings (`middleware` deprecation, autonomous runner dynamic trace warning, NFT trace warning);
  - `scripts/lab-health.sh` passed;
  - after cache refresh, AMZN/NVDA/TSM routes returned Yahoo-linked news while FAKEZZZ stayed sample fallback;
  - AMZN HTML confirmed Yahoo links, no sample news title leakage, SEC label style present, and no serif SEC h2;
  - screenshot QA (`tmp/mobile-check/news-phase-amzn.png`) confirmed Recent news and SEC filings cards are visually aligned.
- Commit: `4261fb3 Add Lab ticker Yahoo news headlines`.

## Mission Control Lab ticker news/filings alignment follow-up [14:32 GMT+7]
- Philippe noticed the Yahoo-backed Recent News card grew taller while SEC Filings still had the fixed-height internal scroll, making the adjacent cards visually uneven.
- Reverted the SEC filings internal fixed-height/scroll CSS so the card can grow naturally and match the Recent News row height again.
- Kept the small sans/mono `SEC FILINGS` section-label treatment from the news-provider phase.
- Validation:
  - `npx tsc --noEmit --pretty false` passed;
  - `npm run lint` passed;
  - `npm run build` passed with only known unrelated warnings;
  - `scripts/lab-health.sh` passed;
  - AMZN route returned 200 with Yahoo news links and SEC label style present;
  - screenshot QA (`tmp/mobile-check/news-filings-align-amzn.png`) confirmed Recent News and SEC Filings align in row height and SEC Filings is readable without internal scroll.
- Commit: `b828a08 Align Lab ticker news and filings cards`.
