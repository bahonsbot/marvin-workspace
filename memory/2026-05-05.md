# 2026-05-05

## Mission Control Lab Yahoo 1D fallback + watchlist right-column alignment [00:25 GMT+8 / 23:25 Vietnam previous day]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched or promoted.
- Philippe reported that the right side of the watchlist panel was still misaligned: `Watch` pill too large/tight against icons, text not visually centered, move/remove icons and the thin row divider not lining up with the rest of the row.
- Screenshot analysis showed the action icons were sitting too high relative to the row and the alert/priority cell widths/padding were too generous. The divider issue was likely an action-cell box-height/padding mismatch rather than a separate divider asset.
- Watchlist CSS changes: reduced alert/priority select pills to fixed `64px x 22px`, centered text with `text-align-last: center`, tightened alert/action column widths (`Alert` 58px, actions 72px), added matched padding to Priority/Alert/Actions cells, and gave the action flex row a stable `min-height: 26px` so icons share the row's vertical center.
- Yahoo 1D probe: direct Yahoo chart endpoint returned fresh intraday points for `NVDA`, `CRWV`, and `AVGO`, with last points around 2026-05-04 16:12Z while EODHD 1D still ended at 2026-05-01 20:00Z. This confirms Yahoo 1D is suitable as a fallback and raises a real question about EODHD renewal value if alternate sources keep replacing its useful coverage.
- Implemented EODHD 1D stale fallback: `lib/trading/sources/yahoo.ts` now exports `fetchYahooChart`, `buildYahooRangeSeries`, `YAHOO_PRICE_RANGES`, and `yahooSource`; `lib/trading/sources/eodhd.ts` detects stale 1D intraday series and replaces that range with Yahoo Finance 1D intraday when Yahoo is fresher. Longer EODHD ranges/dividends/news remain unchanged for now.
- Live validation after cache clear and Lab restart: ticker profile caches for `NVDA.US`, `CRWV.US`, and `AVGO.US` now show `rangeSeries['1D'].source.source === 'yahoo'` and last 1D points around 2026-05-04 16:20Z. EODHD realtime quote remained around 15:42Z/15:43Z, so Yahoo 1D was fresher than EODHD quote at validation time.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab health; Lab lane smoke; custom CSS marker check; live ticker cache inspection for Yahoo 1D fallback.
- Known unrelated build warnings persisted: deprecated Next `middleware`, autonomous runner dynamic trace, and NFT `next.config.js` trace warning.

## Mission Control Lab final watchlist action alignment tweak [00:47 GMT+8 / 23:47 Vietnam previous day]
- Applied Philippe's final right-column watchlist tweak: moved the move/remove icon cluster down 5px via `transform: translateY(5px)` on `.trading-watchlist-row-actions`.
- Removed the thin divider segment under the action icon section by making the actions cell bottom border transparent (`td:nth-child(9)`), so the row divider visually stops after the Alert/Watch pill.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab health; Lab lane smoke. Served CSS confirmed `transform: translateY(5px)` and minified transparent border (`#0000`) for the action cell.
- Noted market-data observation from Philippe: Yahoo 1D intraday appears about 5 minutes delayed, while EODHD quote appears about 15 minutes delayed. This should be tracked when deciding whether to renew EODHD next month.

## Mission Control Lab closeout and tomorrow focus [00:42 GMT+8 / 23:42 Vietnam previous day]
- Philippe confirmed the final watchlist right-column alignment looks good after commit `770720a Tune Lab watchlist action alignment`.
- Treat the current Lab watchlist panel polish as accepted for tonight unless new visual drift appears later.
- Market-data observation to carry forward: Yahoo 1D intraday appears roughly 5 minutes delayed, while EODHD realtime quote appears roughly 15 minutes delayed. This should be considered in the next-month EODHD renewal/value review.
- Tomorrow's Trading Lab focus requested by Philippe:
  1. Price alerts for items on the watchlist.
  2. Define what action should be taken based on each Alert level.
  3. Start the first phase of the Portfolio page.
- Existing open Trading follow-ups still remain unless superseded: cache hygiene/debug refresh, ETF issuer/logo strategy, IREN.US richer profile/logo/CoreWeave-style logo hygiene, center/logo polish if new screenshots show drift, duplicate watchlist entries if any, and broader EODHD provider-value review.

## Workspace home-improvement: runbook index checker hardening [08:33 GMT+8]
- Changed `scripts/check_runbook_index.py` to derive the workspace root from the script location by default and accept `OPENCLAW_WORKSPACE` as an override instead of relying on a hard-coded absolute path.
- Why: this makes the helper script safer to reuse from other working directories or cloned workspace roots without silent path brittleness.
- Expected benefit: lower friction when validating runbook indexes in moved/copied workspaces and clearer operator guidance via `--help` epilog.
- Verification: `python3 scripts/check_runbook_index.py`; `python3 /data/.openclaw/workspace/scripts/check_runbook_index.py` from `/tmp`; `OPENCLAW_WORKSPACE=/data/.openclaw/workspace python3 scripts/check_runbook_index.py --help`.
- Rollback: restore the previous hard-coded `WORKSPACE = Path("/data/.openclaw/workspace")` constant if this dynamic resolution ever causes confusion.

## Morning Meeting Decision [11:13]
- Report: nightly-security-review
- Finding: 2026-05-04 nightly security review overall verdict
- Decision: Approved
- Problem: no actionable security issue was found; review surfaced only informational accepted-risk/control-state notes
- Risk: no new critical, high, or medium security risk detected
- Action taken: none
- Verification: reviewed `memory/security/2026-05-04-nightly-security-review.md`; verdict `CLEAN` with 0 critical / 0 high / 0 medium
- Rollback: n/a
- Notes: continue suppressing unchanged accepted-risk items unless state drifts

## Morning Meeting Decision [11:19]
- Report: platform-health-council
- Finding: lossless-claw warning noise / ownership complaint
- Decision: Accepted risk
- Problem: the overnight review re-raised `lossless-claw` warning noise without enough live evidence that the plugin is actually malfunctioning
- Risk: low; false-positive review noise could push unnecessary runtime/plugin ownership changes
- Action taken: none
- Verification: live check showed `/data/.openclaw/extensions/lossless-claw` exists and is `node:node`; `openclaw.json` still contains `lossless-claw` entries; durable memory already warns not to trust plugin ownership/trust complaints blindly on this Docker host
- Rollback: n/a
- Notes: only reopen if actual lossless-claw behavior breaks, not just because a review complains

## Morning Meeting Decision [11:28]
- Report: self-improvement
- Finding: stale TOOLS.md references and missing runbook-index helper note
- Decision: Approved
- Problem: `TOOLS.md` pointed at a non-existent runbook, used wrong workspace-relative paths for Mission Control chat helpers, and did not document `scripts/check_runbook_index.py`
- Risk: operator confusion and avoidable debugging friction
- Action taken: removed the stale missing runbook reference, corrected the three chat-helper paths to `projects/mission-control/components/chat/...`, and added `scripts/check_runbook_index.py` to Key Scripts
- Verification: `python3 scripts/check_runbook_index.py` returned `OK: docs/runbooks/README.md indexes 21 runbook files.`; `grep` confirmed corrected chat-helper paths and the new script note in `TOOLS.md`
- Rollback: revert the `TOOLS.md` edits if the shorter path style was intentionally being used as shorthand
- Notes: left broader self-improvement findings for later instead of expanding this into a larger docs sweep

## Morning Meeting Decision [11:36]
- Report: platform-health-council
- Finding: PHC-2026-0505-001 nightly-security-review apparent gap
- Decision: Approved
- Problem: PHC flagged a possibly missed May 4 nightly-security-review run because it evaluated before the 03:30 review finished and saved its report
- Risk: false missed-run alarms create noise and can distract from real regressions
- Action taken: none
- Verification: cron run history shows `nightly-security-review` finished `ok` at 03:30 and delivered normally; saved report exists at `memory/security/2026-05-04-nightly-security-review.md`
- Rollback: n/a
- Notes: suppress repeats unless the 03:30 security job actually fails or its report is still missing later in the morning

## Morning Meeting Decision [11:41]
- Report: self-improvement
- Finding: deterministic cron task registry workflow under-documented
- Decision: Approved
- Problem: `TOOLS.md` mentioned `cron_runner.py --list-tasks` but did not explain that `scripts/cron_runner_tasks.py` is the declarative registry or point operators to the scheduler host-service runbook
- Risk: operator confusion when adding script-only jobs or verifying deterministic scheduler changes
- Action taken: added `scripts/cron_runner_tasks.py` to Key Scripts, clarified the deterministic scheduler workflow in `TOOLS.md`, and explicitly referenced `docs/runbooks/deterministic-scheduler-host-service.md`
- Verification: `python3 scripts/cron_runner.py --list-tasks` listed registered tasks; `grep` confirmed the new registry/workflow/runbook notes in `TOOLS.md`; checked `docs/runbooks/deterministic-scheduler-host-service.md` and kept it as the right runbook to refer to
- Rollback: revert the `TOOLS.md` edits if we later move the deterministic scheduler workflow into a different canonical doc
- Notes: there is a relevant runbook, and we should refer to it rather than duplicate host-service procedures inside `TOOLS.md`

## Morning Meeting Decision [11:49]
- Report: self-improvement
- Finding: operator diagnostics scripts existed but were effectively invisible in TOOLS.md
- Decision: Approved
- Problem: several real operator-facing diagnostic helpers were present in `scripts/` but undocumented, making them easy to miss or misclassify as legacy leftovers
- Risk: slower troubleshooting and duplicated ad-hoc checks
- Action taken: added an `Operator diagnostics` note to `TOOLS.md` covering `autonomy_health_check.py`, `autonomy_operator_dashboard.py`, `operator_snapshot.py`, and `cron_run_summary.py`
- Verification: `grep` confirmed the new TOOLS.md entries; `python3 scripts/operator_snapshot.py --json` ran successfully; `python3 scripts/cron_run_summary.py --limit 3` produced a compact cron summary
- Rollback: revert the `TOOLS.md` diagnostic-helper note if we later split these into a dedicated operator runbook or formally deprecate any of the scripts
- Notes: documented them as diagnostics, not as first-line runtime truth sources over the existing health/cron guidance

## Morning Meeting Decision [11:55]
- Report: self-improvement
- Finding: nightly-memory-extraction verification workflow was implied by AGENTS.md but not documented compactly in TOOLS.md
- Decision: Approved
- Problem: operators had no short checklist for verifying whether the silent nightly-memory-extraction job succeeded versus intentionally making no daily-memory append
- Risk: silent-job delivery noise or a no-append run could be misread as failure
- Action taken: added a four-step nightly-memory-extraction verification quick check under the OpenClaw cron section in `TOOLS.md`
- Verification: `grep` confirmed the new checklist; cron run history for job `7201afb2-6ff2-4ce7-8c24-e12d4413b9f9` showed the latest run `ok`, explicitly noted that `/data/.openclaw/workspace/memory/2026-05-04.md` was already comprehensive, and recorded durable updates to `life/projects/mission-control/items.json`; delivery status was `not-requested`, which matches the documented silent-job expectation
- Rollback: revert the TOOLS.md checklist if nightly-memory-extraction moves to a dedicated runbook with a better canonical operator procedure
- Notes: documented the distinction between true failure and healthy silent/no-append behavior

## OpenClaw runtime hotfix: suppress context-engine maintenance chat spam [12:32 GMT+8]
- Investigated the disruptive 10-minute-looking "heartbeat" popups and confirmed they were not caused by the configured 180-minute `HEARTBEAT.md` cadence or any extra cron job.
- Root cause was installed OpenClaw runtime behavior under `/data/.npm-global/lib/node_modules/openclaw/dist/`: deferred `context_engine_turn_maintenance` tasks were being promoted to visible task updates, auto-delivered into chat, and coupled to heartbeat wakes.
- Applied a targeted hotfix in `dist/task-registry-DfxdgLn1.js` so `context_engine_turn_maintenance` tasks no longer auto-deliver state changes or terminal updates.
- Sent `SIGUSR1` to the live gateway to activate the patch. The first signal only queued restart authorization; the second completed after active work drained.
- Verification from `/tmp/openclaw/openclaw-2026-05-05.log`:
  - `12:26:42` `received SIGUSR1; restarting`
  - `12:27:36` `all active work drained`
  - `12:27:37` `restart mode: full process restart (spawned pid 621993)`
  - `12:27:51` gateway `ready`
  - heartbeat subsystem still starts at `intervalMs: 10800000` (3 hours), confirming the visible spam was separate from the real heartbeat interval.
- Post-restart checks: live process tree now shows the new gateway PID `621993`; no new `Background task update/done: Context engine turn maintenance` lines appeared in the log after reload; no new `context_engine_turn_maintenance` rows were created in `tasks/runs.sqlite` after the restart window during this verification pass.
- Caveat: this hotfix lives in installed runtime dist files, not workspace source control, so it is vulnerable to npm reinstall/update/rebuild and should be treated as a local operational patch until upstreamed or re-applied more durably.

## OpenClaw runtime hotfix follow-up: durable reapply note + session-save prep [12:54 GMT+8]
- Philippe approved leaving the maintenance-spam hotfix in place and monitoring quietly, but asked for a durable reapply note so recovery is easy after future OpenClaw updates.
- Added runbook `docs/runbooks/openclaw-context-maintenance-chat-spam-hotfix.md` covering symptom recognition, root cause, local patch location in `dist/task-registry-DfxdgLn1.js`, reapply procedure, activation via gateway-only `SIGUSR1`, and verification commands.
- Added the new runbook to `docs/runbooks/README.md` and recorded the issue as reusable error `ERR-20260505-1232` in `.learnings/errors.md` because this is a likely-to-recur installed-runtime regression after updates.
- Intent: end this session with the extended Morning Meeting actions, hotfix activation, and recovery procedure saved cleanly so a fresh session can resume without relying on transcript recall.

## Mission Control Lab price alert rules phase 1 [13:45 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched or promoted.
- Implemented Phase 1 of watchlist price alerts: the table now replaces the manual Priority column with editable `Price Alert` Min/Max controls and replaces manual Alert dropdowns with computed `Status` pills.
- UX behavior: clicking Min or Max opens an inline numeric input; entering a value saves it; clearing the value and blurring/pressing Enter removes that side of the alert rule. One or both bounds can be set.
- Computed statuses now derive from current metadata quote price: `No rule`, `Inside range`, `Near low`, `Near high`, `Below min`, `Above max`, and `No quote`; near-threshold is currently 2%.
- Added Convex fields for alert rules on watchlist items: `alertEnabled`, `alertMinPrice`, and `alertMaxPrice`; move/merge preserves these fields. Existing legacy `priority` and `alertLevel` fields remain in schema/types for compatibility but are no longer the primary Watchlist table controls.
- Extended `/api/trading/watchlist-metadata` items with numeric/freshness quote fields needed for evaluation: `rawPrice`, `priceTime`, `currency`, `quoteFreshness`, and `providerDelay`.
- Updated ticker-page watchlist add default so newly saved symbols no longer mark `alertLevel: watch` by default; explicit price bounds are now the actionable alert mechanism.
- Validation passed: `npx convex codegen`; `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab restart with lane-scoped scripts; `bash scripts/lab-health.sh`; `bash scripts/lab-lane-smoke.sh`; live API check for `NVDA.US` confirmed `rawPrice`, `priceTime`, `currency`, and freshness fields; CDP rendered check confirmed `Price Alert`, alert cells/status classes, and no loading deadlock.
- Visual check: screenshot at `projects/mission-control-lab/tmp/watchlist-price-alert-phase1-cdp.png` showed Price Alert/Status columns readable and far-right action buttons visible, with no severe clipping/overlap. Action area remains a little tight but acceptable.
- Known expected build warnings persisted unchanged: deprecated Next middleware convention, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning.
- Phase 2 remains: server-side scheduled alert evaluator plus Telegram notification on threshold crossings with dedupe/cooldown/re-entry logic to prevent spam.


## Watchlist alert input save fix [14:12 GMT+8]
- Philippe reported that entering a Min amount and pressing Enter did not appear to persist; switching to Max made the previously typed Min value disappear.
- Root cause was UX/state timing: Phase 1 relied on Enter plus blur-save and waited for live Convex query propagation before the row showed the saved bound, so moving between cells could briefly render old row state and make the input look lost.
- Fixed Lab Watchlist Price Alert editing to use explicit Save/Cancel controls, keep Enter as a save shortcut, remove blur-save, add `enterKeyHint=done` for mobile keyboards, and optimistically apply saved alert bounds locally while Convex catches up.
- Switching from Min to Max now first attempts to save the active Min edit; on success it opens Max with the optimistic row value still visible.
- Visual/mobile note: the edit cell now temporarily uses a single-column editing state instead of showing Min and Max side-by-side, giving the input and Save/Cancel controls enough room; this is more mobile-friendly than tiny blur-driven controls.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, trading smoke tests 22/22, `npm run build`, Lab lane-scoped restart after one transient port-still-busy retry, `lab-health`, `lab-lane-smoke`, and CDP click/render check confirming the input plus Save/Cancel controls appear with `enterkeyhint=done`.


## Convex watchlist alert validator deployment fix [14:30 GMT+8]
- Philippe reported live red Convex error on saving a Min alert: `ArgumentValidationError: Object contains extra field alertEnabled`.
- Diagnosis: local Lab source already had `alertEnabled`, `alertMinPrice`, and `alertMaxPrice` validators in `convex/watchlist.ts`, but the live Convex dev deployment function spec still showed the old `watchlist:update` validator with no alert fields. Browser/client code was correct; backend had not been pushed.
- Ran `npx convex dev --once --typecheck disable` in `projects/mission-control-lab` to push the updated Lab Convex functions.
- Verified with `npx convex function-spec --file`: `watchlist.js:update` now includes `alertEnabled`, `alertMinPrice`, and `alertMaxPrice` validators.
- Smoke-tested backend mutation directly with `npx convex run watchlist:update '{"id":"j57adg7yc0a5kaay10nj5mcg99863j6w","alertEnabled":true,"alertMinPrice":57}'`; Convex accepted it and `npx convex data watchlistItems --limit 1` showed `alertEnabled: true` and `alertMinPrice: 57`.
- Lab health and lane smoke remained OK after the backend push.
- Note: the direct smoke test set INTC.US Min alert to 57 in the Lab watchlist data.


## Mission Control Lab Portfolio Phase 1 [15:30 GMT+8]
- Philippe approved building Portfolio Phase 1 after screenshot analysis. Scope stayed Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched.
- Built a first real Portfolio page replacing the placeholder: KPI ribbon, benchmark performance chart scaffolding, interactive allocation donut with dimensions (ticker, sector, industry, country, asset type, strategy, currency, broker), holdings table, upcoming earnings placeholder, closed positions placeholder, and manual add-holding form.
- Added Convex portfolio storage: `portfolioHoldings` schema/table plus `convex/portfolio.ts` list/add/update/remove functions and `lib/convex/portfolio-api.ts` client references.
- Holdings support symbol/name/type/strategy/sector/industry/country/currency/broker/quantity/average cost/cost basis/min-max alerts. CSV import and full performance history remain future phases.
- The Portfolio page reuses the existing watchlist metadata endpoint for live quote overlays and uses local cache fallback.
- Pushed Convex dev functions with `npx convex dev --once --typecheck disable`; function spec confirmed `portfolio.js:list`, `portfolio.js:add`, `portfolio.js:update`, and `portfolio.js:remove`.
- Seeded Lab demo holdings directly in Convex for visible QA: CASH.EUR, AMZN.US, and GOOGL.US.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, `npx convex codegen`, trading provider smoke tests 22/22, `npm run build`, Lab health, and lane smoke.
- Render verification via Chromium/CDP confirmed portfolio table/donut/chart/add/earnings are present; screenshot QA of `tmp/portfolio-phase1-full-cdp.png` found KPI ribbon, performance chart, allocation donut/tabs, holdings table, and bottom sections readable/aligned with no severe clipping/overlap.
- Restart note: first Lab preview restart attempt hit existing port listeners; after PID-file scoped stop/start, Lab health passed with proxy/Next/ws-sidecar/Piper/Moonshine all OK.
# 2026-05-05

## Mission Control Lab Yahoo 1D fallback + watchlist right-column alignment [00:25 GMT+8 / 23:25 Vietnam previous day]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched or promoted.
- Philippe reported that the right side of the watchlist panel was still misaligned: `Watch` pill too large/tight against icons, text not visually centered, move/remove icons and the thin row divider not lining up with the rest of the row.
- Screenshot analysis showed the action icons were sitting too high relative to the row and the alert/priority cell widths/padding were too generous. The divider issue was likely an action-cell box-height/padding mismatch rather than a separate divider asset.
- Watchlist CSS changes: reduced alert/priority select pills to fixed `64px x 22px`, centered text with `text-align-last: center`, tightened alert/action column widths (`Alert` 58px, actions 72px), added matched padding to Priority/Alert/Actions cells, and gave the action flex row a stable `min-height: 26px` so icons share the row's vertical center.
- Yahoo 1D probe: direct Yahoo chart endpoint returned fresh intraday points for `NVDA`, `CRWV`, and `AVGO`, with last points around 2026-05-04 16:12Z while EODHD 1D still ended at 2026-05-01 20:00Z. This confirms Yahoo 1D is suitable as a fallback and raises a real question about EODHD renewal value if alternate sources keep replacing its useful coverage.
- Implemented EODHD 1D stale fallback: `lib/trading/sources/yahoo.ts` now exports `fetchYahooChart`, `buildYahooRangeSeries`, `YAHOO_PRICE_RANGES`, and `yahooSource`; `lib/trading/sources/eodhd.ts` detects stale 1D intraday series and replaces that range with Yahoo Finance 1D intraday when Yahoo is fresher. Longer EODHD ranges/dividends/news remain unchanged for now.
- Live validation after cache clear and Lab restart: ticker profile caches for `NVDA.US`, `CRWV.US`, and `AVGO.US` now show `rangeSeries['1D'].source.source === 'yahoo'` and last 1D points around 2026-05-04 16:20Z. EODHD realtime quote remained around 15:42Z/15:43Z, so Yahoo 1D was fresher than EODHD quote at validation time.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab health; Lab lane smoke; custom CSS marker check; live ticker cache inspection for Yahoo 1D fallback.
- Known unrelated build warnings persisted: deprecated Next `middleware`, autonomous runner dynamic trace, and NFT `next.config.js` trace warning.

## Mission Control Lab final watchlist action alignment tweak [00:47 GMT+8 / 23:47 Vietnam previous day]
- Applied Philippe's final right-column watchlist tweak: moved the move/remove icon cluster down 5px via `transform: translateY(5px)` on `.trading-watchlist-row-actions`.
- Removed the thin divider segment under the action icon section by making the actions cell bottom border transparent (`td:nth-child(9)`), so the row divider visually stops after the Alert/Watch pill.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab health; Lab lane smoke. Served CSS confirmed `transform: translateY(5px)` and minified transparent border (`#0000`) for the action cell.
- Noted market-data observation from Philippe: Yahoo 1D intraday appears about 5 minutes delayed, while EODHD quote appears about 15 minutes delayed. This should be tracked when deciding whether to renew EODHD next month.

## Mission Control Lab closeout and tomorrow focus [00:42 GMT+8 / 23:42 Vietnam previous day]
- Philippe confirmed the final watchlist right-column alignment looks good after commit `770720a Tune Lab watchlist action alignment`.
- Treat the current Lab watchlist panel polish as accepted for tonight unless new visual drift appears later.
- Market-data observation to carry forward: Yahoo 1D intraday appears roughly 5 minutes delayed, while EODHD realtime quote appears roughly 15 minutes delayed. This should be considered in the next-month EODHD renewal/value review.
- Tomorrow's Trading Lab focus requested by Philippe:
  1. Price alerts for items on the watchlist.
  2. Define what action should be taken based on each Alert level.
  3. Start the first phase of the Portfolio page.
- Existing open Trading follow-ups still remain unless superseded: cache hygiene/debug refresh, ETF issuer/logo strategy, IREN.US richer profile/logo/CoreWeave-style logo hygiene, center/logo polish if new screenshots show drift, duplicate watchlist entries if any, and broader EODHD provider-value review.

## Workspace home-improvement: runbook index checker hardening [08:33 GMT+8]
- Changed `scripts/check_runbook_index.py` to derive the workspace root from the script location by default and accept `OPENCLAW_WORKSPACE` as an override instead of relying on a hard-coded absolute path.
- Why: this makes the helper script safer to reuse from other working directories or cloned workspace roots without silent path brittleness.
- Expected benefit: lower friction when validating runbook indexes in moved/copied workspaces and clearer operator guidance via `--help` epilog.
- Verification: `python3 scripts/check_runbook_index.py`; `python3 /data/.openclaw/workspace/scripts/check_runbook_index.py` from `/tmp`; `OPENCLAW_WORKSPACE=/data/.openclaw/workspace python3 scripts/check_runbook_index.py --help`.
- Rollback: restore the previous hard-coded `WORKSPACE = Path("/data/.openclaw/workspace")` constant if this dynamic resolution ever causes confusion.

## Morning Meeting Decision [11:13]
- Report: nightly-security-review
- Finding: 2026-05-04 nightly security review overall verdict
- Decision: Approved
- Problem: no actionable security issue was found; review surfaced only informational accepted-risk/control-state notes
- Risk: no new critical, high, or medium security risk detected
- Action taken: none
- Verification: reviewed `memory/security/2026-05-04-nightly-security-review.md`; verdict `CLEAN` with 0 critical / 0 high / 0 medium
- Rollback: n/a
- Notes: continue suppressing unchanged accepted-risk items unless state drifts

## Morning Meeting Decision [11:19]
- Report: platform-health-council
- Finding: lossless-claw warning noise / ownership complaint
- Decision: Accepted risk
- Problem: the overnight review re-raised `lossless-claw` warning noise without enough live evidence that the plugin is actually malfunctioning
- Risk: low; false-positive review noise could push unnecessary runtime/plugin ownership changes
- Action taken: none
- Verification: live check showed `/data/.openclaw/extensions/lossless-claw` exists and is `node:node`; `openclaw.json` still contains `lossless-claw` entries; durable memory already warns not to trust plugin ownership/trust complaints blindly on this Docker host
- Rollback: n/a
- Notes: only reopen if actual lossless-claw behavior breaks, not just because a review complains

## Morning Meeting Decision [11:28]
- Report: self-improvement
- Finding: stale TOOLS.md references and missing runbook-index helper note
- Decision: Approved
- Problem: `TOOLS.md` pointed at a non-existent runbook, used wrong workspace-relative paths for Mission Control chat helpers, and did not document `scripts/check_runbook_index.py`
- Risk: operator confusion and avoidable debugging friction
- Action taken: removed the stale missing runbook reference, corrected the three chat-helper paths to `projects/mission-control/components/chat/...`, and added `scripts/check_runbook_index.py` to Key Scripts
- Verification: `python3 scripts/check_runbook_index.py` returned `OK: docs/runbooks/README.md indexes 21 runbook files.`; `grep` confirmed corrected chat-helper paths and the new script note in `TOOLS.md`
- Rollback: revert the `TOOLS.md` edits if the shorter path style was intentionally being used as shorthand
- Notes: left broader self-improvement findings for later instead of expanding this into a larger docs sweep

## Morning Meeting Decision [11:36]
- Report: platform-health-council
- Finding: PHC-2026-0505-001 nightly-security-review apparent gap
- Decision: Approved
- Problem: PHC flagged a possibly missed May 4 nightly-security-review run because it evaluated before the 03:30 review finished and saved its report
- Risk: false missed-run alarms create noise and can distract from real regressions
- Action taken: none
- Verification: cron run history shows `nightly-security-review` finished `ok` at 03:30 and delivered normally; saved report exists at `memory/security/2026-05-04-nightly-security-review.md`
- Rollback: n/a
- Notes: suppress repeats unless the 03:30 security job actually fails or its report is still missing later in the morning

## Morning Meeting Decision [11:41]
- Report: self-improvement
- Finding: deterministic cron task registry workflow under-documented
- Decision: Approved
- Problem: `TOOLS.md` mentioned `cron_runner.py --list-tasks` but did not explain that `scripts/cron_runner_tasks.py` is the declarative registry or point operators to the scheduler host-service runbook
- Risk: operator confusion when adding script-only jobs or verifying deterministic scheduler changes
- Action taken: added `scripts/cron_runner_tasks.py` to Key Scripts, clarified the deterministic scheduler workflow in `TOOLS.md`, and explicitly referenced `docs/runbooks/deterministic-scheduler-host-service.md`
- Verification: `python3 scripts/cron_runner.py --list-tasks` listed registered tasks; `grep` confirmed the new registry/workflow/runbook notes in `TOOLS.md`; checked `docs/runbooks/deterministic-scheduler-host-service.md` and kept it as the right runbook to refer to
- Rollback: revert the `TOOLS.md` edits if we later move the deterministic scheduler workflow into a different canonical doc
- Notes: there is a relevant runbook, and we should refer to it rather than duplicate host-service procedures inside `TOOLS.md`

## Morning Meeting Decision [11:49]
- Report: self-improvement
- Finding: operator diagnostics scripts existed but were effectively invisible in TOOLS.md
- Decision: Approved
- Problem: several real operator-facing diagnostic helpers were present in `scripts/` but undocumented, making them easy to miss or misclassify as legacy leftovers
- Risk: slower troubleshooting and duplicated ad-hoc checks
- Action taken: added an `Operator diagnostics` note to `TOOLS.md` covering `autonomy_health_check.py`, `autonomy_operator_dashboard.py`, `operator_snapshot.py`, and `cron_run_summary.py`
- Verification: `grep` confirmed the new TOOLS.md entries; `python3 scripts/operator_snapshot.py --json` ran successfully; `python3 scripts/cron_run_summary.py --limit 3` produced a compact cron summary
- Rollback: revert the `TOOLS.md` diagnostic-helper note if we later split these into a dedicated operator runbook or formally deprecate any of the scripts
- Notes: documented them as diagnostics, not as first-line runtime truth sources over the existing health/cron guidance

## Morning Meeting Decision [11:55]
- Report: self-improvement
- Finding: nightly-memory-extraction verification workflow was implied by AGENTS.md but not documented compactly in TOOLS.md
- Decision: Approved
- Problem: operators had no short checklist for verifying whether the silent nightly-memory-extraction job succeeded versus intentionally making no daily-memory append
- Risk: silent-job delivery noise or a no-append run could be misread as failure
- Action taken: added a four-step nightly-memory-extraction verification quick check under the OpenClaw cron section in `TOOLS.md`
- Verification: `grep` confirmed the new checklist; cron run history for job `7201afb2-6ff2-4ce7-8c24-e12d4413b9f9` showed the latest run `ok`, explicitly noted that `/data/.openclaw/workspace/memory/2026-05-04.md` was already comprehensive, and recorded durable updates to `life/projects/mission-control/items.json`; delivery status was `not-requested`, which matches the documented silent-job expectation
- Rollback: revert the TOOLS.md checklist if nightly-memory-extraction moves to a dedicated runbook with a better canonical operator procedure
- Notes: documented the distinction between true failure and healthy silent/no-append behavior

## OpenClaw runtime hotfix: suppress context-engine maintenance chat spam [12:32 GMT+8]
- Investigated the disruptive 10-minute-looking "heartbeat" popups and confirmed they were not caused by the configured 180-minute `HEARTBEAT.md` cadence or any extra cron job.
- Root cause was installed OpenClaw runtime behavior under `/data/.npm-global/lib/node_modules/openclaw/dist/`: deferred `context_engine_turn_maintenance` tasks were being promoted to visible task updates, auto-delivered into chat, and coupled to heartbeat wakes.
- Applied a targeted hotfix in `dist/task-registry-DfxdgLn1.js` so `context_engine_turn_maintenance` tasks no longer auto-deliver state changes or terminal updates.
- Sent `SIGUSR1` to the live gateway to activate the patch. The first signal only queued restart authorization; the second completed after active work drained.
- Verification from `/tmp/openclaw/openclaw-2026-05-05.log`:
  - `12:26:42` `received SIGUSR1; restarting`
  - `12:27:36` `all active work drained`
  - `12:27:37` `restart mode: full process restart (spawned pid 621993)`
  - `12:27:51` gateway `ready`
  - heartbeat subsystem still starts at `intervalMs: 10800000` (3 hours), confirming the visible spam was separate from the real heartbeat interval.
- Post-restart checks: live process tree now shows the new gateway PID `621993`; no new `Background task update/done: Context engine turn maintenance` lines appeared in the log after reload; no new `context_engine_turn_maintenance` rows were created in `tasks/runs.sqlite` after the restart window during this verification pass.
- Caveat: this hotfix lives in installed runtime dist files, not workspace source control, so it is vulnerable to npm reinstall/update/rebuild and should be treated as a local operational patch until upstreamed or re-applied more durably.

## OpenClaw runtime hotfix follow-up: durable reapply note + session-save prep [12:54 GMT+8]
- Philippe approved leaving the maintenance-spam hotfix in place and monitoring quietly, but asked for a durable reapply note so recovery is easy after future OpenClaw updates.
- Added runbook `docs/runbooks/openclaw-context-maintenance-chat-spam-hotfix.md` covering symptom recognition, root cause, local patch location in `dist/task-registry-DfxdgLn1.js`, reapply procedure, activation via gateway-only `SIGUSR1`, and verification commands.
- Added the new runbook to `docs/runbooks/README.md` and recorded the issue as reusable error `ERR-20260505-1232` in `.learnings/errors.md` because this is a likely-to-recur installed-runtime regression after updates.
- Intent: end this session with the extended Morning Meeting actions, hotfix activation, and recovery procedure saved cleanly so a fresh session can resume without relying on transcript recall.

## Mission Control Lab price alert rules phase 1 [13:45 GMT+8]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched or promoted.
- Implemented Phase 1 of watchlist price alerts: the table now replaces the manual Priority column with editable `Price Alert` Min/Max controls and replaces manual Alert dropdowns with computed `Status` pills.
- UX behavior: clicking Min or Max opens an inline numeric input; entering a value saves it; clearing the value and blurring/pressing Enter removes that side of the alert rule. One or both bounds can be set.
- Computed statuses now derive from current metadata quote price: `No rule`, `Inside range`, `Near low`, `Near high`, `Below min`, `Above max`, and `No quote`; near-threshold is currently 2%.
- Added Convex fields for alert rules on watchlist items: `alertEnabled`, `alertMinPrice`, and `alertMaxPrice`; move/merge preserves these fields. Existing legacy `priority` and `alertLevel` fields remain in schema/types for compatibility but are no longer the primary Watchlist table controls.
- Extended `/api/trading/watchlist-metadata` items with numeric/freshness quote fields needed for evaluation: `rawPrice`, `priceTime`, `currency`, `quoteFreshness`, and `providerDelay`.
- Updated ticker-page watchlist add default so newly saved symbols no longer mark `alertLevel: watch` by default; explicit price bounds are now the actionable alert mechanism.
- Validation passed: `npx convex codegen`; `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab restart with lane-scoped scripts; `bash scripts/lab-health.sh`; `bash scripts/lab-lane-smoke.sh`; live API check for `NVDA.US` confirmed `rawPrice`, `priceTime`, `currency`, and freshness fields; CDP rendered check confirmed `Price Alert`, alert cells/status classes, and no loading deadlock.
- Visual check: screenshot at `projects/mission-control-lab/tmp/watchlist-price-alert-phase1-cdp.png` showed Price Alert/Status columns readable and far-right action buttons visible, with no severe clipping/overlap. Action area remains a little tight but acceptable.
- Known expected build warnings persisted unchanged: deprecated Next middleware convention, dynamic autonomous runner trace warning, and NFT `next.config.js` trace warning.
- Phase 2 remains: server-side scheduled alert evaluator plus Telegram notification on threshold crossings with dedupe/cooldown/re-entry logic to prevent spam.


## Watchlist alert input save fix [14:12 GMT+8]
- Philippe reported that entering a Min amount and pressing Enter did not appear to persist; switching to Max made the previously typed Min value disappear.
- Root cause was UX/state timing: Phase 1 relied on Enter plus blur-save and waited for live Convex query propagation before the row showed the saved bound, so moving between cells could briefly render old row state and make the input look lost.
- Fixed Lab Watchlist Price Alert editing to use explicit Save/Cancel controls, keep Enter as a save shortcut, remove blur-save, add `enterKeyHint=done` for mobile keyboards, and optimistically apply saved alert bounds locally while Convex catches up.
- Switching from Min to Max now first attempts to save the active Min edit; on success it opens Max with the optimistic row value still visible.
- Visual/mobile note: the edit cell now temporarily uses a single-column editing state instead of showing Min and Max side-by-side, giving the input and Save/Cancel controls enough room; this is more mobile-friendly than tiny blur-driven controls.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, trading smoke tests 22/22, `npm run build`, Lab lane-scoped restart after one transient port-still-busy retry, `lab-health`, `lab-lane-smoke`, and CDP click/render check confirming the input plus Save/Cancel controls appear with `enterkeyhint=done`.


## Convex watchlist alert validator deployment fix [14:30 GMT+8]
- Philippe reported live red Convex error on saving a Min alert: `ArgumentValidationError: Object contains extra field alertEnabled`.
- Diagnosis: local Lab source already had `alertEnabled`, `alertMinPrice`, and `alertMaxPrice` validators in `convex/watchlist.ts`, but the live Convex dev deployment function spec still showed the old `watchlist:update` validator with no alert fields. Browser/client code was correct; backend had not been pushed.
- Ran `npx convex dev --once --typecheck disable` in `projects/mission-control-lab` to push the updated Lab Convex functions.
- Verified with `npx convex function-spec --file`: `watchlist.js:update` now includes `alertEnabled`, `alertMinPrice`, and `alertMaxPrice` validators.
- Smoke-tested backend mutation directly with `npx convex run watchlist:update '{"id":"j57adg7yc0a5kaay10nj5mcg99863j6w","alertEnabled":true,"alertMinPrice":57}'`; Convex accepted it and `npx convex data watchlistItems --limit 1` showed `alertEnabled: true` and `alertMinPrice: 57`.
- Lab health and lane smoke remained OK after the backend push.
- Note: the direct smoke test set INTC.US Min alert to 57 in the Lab watchlist data.


## Mission Control Lab Portfolio Phase 1 [15:30 GMT+8]
- Philippe approved building Portfolio Phase 1 after screenshot analysis. Scope stayed Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched.
- Built a first real Portfolio page replacing the placeholder: KPI ribbon, benchmark performance chart scaffolding, interactive allocation donut with dimensions (ticker, sector, industry, country, asset type, strategy, currency, broker), holdings table, upcoming earnings placeholder, closed positions placeholder, and manual add-holding form.
- Added Convex portfolio storage: `portfolioHoldings` schema/table plus `convex/portfolio.ts` list/add/update/remove functions and `lib/convex/portfolio-api.ts` client references.
- Holdings support symbol/name/type/strategy/sector/industry/country/currency/broker/quantity/average cost/cost basis/min-max alerts. CSV import and full performance history remain future phases.
- The Portfolio page reuses the existing watchlist metadata endpoint for live quote overlays and uses local cache fallback.
- Pushed Convex dev functions with `npx convex dev --once --typecheck disable`; function spec confirmed `portfolio.js:list`, `portfolio.js:add`, `portfolio.js:update`, and `portfolio.js:remove`.
- Seeded Lab demo holdings directly in Convex for visible QA: CASH.EUR, AMZN.US, and GOOGL.US.
- Validation passed: `npm run lint`, `npx tsc --noEmit`, `npx convex codegen`, trading provider smoke tests 22/22, `npm run build`, Lab health, and lane smoke.
- Render verification via Chromium/CDP confirmed portfolio table/donut/chart/add/earnings are present; screenshot QA of `tmp/portfolio-phase1-full-cdp.png` found KPI ribbon, performance chart, allocation donut/tabs, holdings table, and bottom sections readable/aligned with no severe clipping/overlap.
- Restart note: first Lab preview restart attempt hit existing port listeners; after PID-file scoped stop/start, Lab health passed with proxy/Next/ws-sidecar/Piper/Moonshine all OK.

## Mission Control Lab Portfolio Phase 1 closeout [16:00 GMT+8]
- Portfolio Phase 1 was committed as `8dfb25e Add Lab portfolio phase one`.
- Final scope remained Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched or promoted.
- Final validation reported to Philippe: `npm run lint`, `npx tsc --noEmit`, `npx convex codegen`, Convex dev push/function-spec check, trading provider smoke tests 22/22, `npm run build`, Lab health/lane smoke, and rendered screenshot QA all passed.
- Phase 1 caveat: Portfolio totals currently treat quote currencies as-is in the EUR base view. Full FX conversion is the honest next accounting fix before treating multi-currency totals as financially precise.
- Next likely Portfolio follow-ups: add/edit row support beyond initial add/remove, FX conversion/base-currency accounting, real performance history, real earnings provider dates, CSV/import flow, realized vs unrealized detail, and closed positions archive.

## Mission Control Lab Portfolio polish follow-up [15:28 Asia/Ho_Chi_Minh]
- Continued Lab-only Portfolio Phase 1 after Philippe's UX feedback.
- Implemented lighter Portfolio typography, brighter/positive allocation donut colors, circular `+` add trigger, Holdings-local sort control, and Edit + Remove row actions.
- Reworked the holding form into add/edit modes. Symbol/name autocomplete now reuses `/api/trading/search`; selecting a result autofills canonical symbol, name, currency, and asset type while keeping currency editable.
- Removed manual Sector/Country inputs from the form; schema fields remain for later provider enrichment/import. Added Transaction Fee input and Convex/API support so cost basis includes `quantity * averageCost + transactionFee` while preserving the fee field.
- Pushed Convex functions after schema/function changes to avoid stale validator drift.
- Validation: lint passed, TypeScript passed, Convex push passed, trading provider smoke passed 23/23 with a new Portfolio guard, build passed with only known pre-existing Turbopack/middleware warnings, Lab health/lane smoke passed, and CDP render checks confirmed plus trigger/Edit buttons/Holdings sort/form/search panel presence. Screenshot artifact: `projects/mission-control-lab/tmp/portfolio-polish-final-cdp.png`.
- Operational note: a first preview restart attempt encountered stale Lab port listeners/PID mismatch after build; recovered with the Lab PID-scoped `preview-stop.sh`/`preview-start.sh` path, no broad process kill.

## Mission Control Lab Portfolio ticker search menu fix [16:26 Asia/Ho_Chi_Minh]
- Philippe reported that after selecting a ticker in the Portfolio add-holding autocomplete, the fields filled but the search suggestion menu stayed open and blocked other form elements.
- Root cause: the search effect kept running after `applySearchResult()` set `searchOpen(false)` because the effect only depended on `mode` and `searchTerm`; the selected symbol/name changed `searchTerm`, retriggered the fetch, and reopened the menu.
- Fix: Portfolio add-holding search now only fetches/reopens suggestions while `searchOpen` is true. Selecting a result closes the menu and clears results without an immediate refetch.
- Validation: `npm run lint`, `npx tsc --noEmit`, trading provider smoke tests 23/23, `npm run build`, Lab health/lane smoke, and CDP browser check. CDP reproduced the prior state (`open:true`, 10 ASML suggestions) and verified after selecting first result: `open:false`, symbol `ASML.US`, name `ASML Holding NV ADR`.
- Known related roadmap from Philippe: dividends/total-return P&L, holdings logos, real FX/base-currency conversion, and cash balance management.

## Mission Control Lab Portfolio ticker search race follow-up [16:42 Asia/Ho_Chi_Minh]
- Philippe showed the Portfolio add-holding autocomplete still stuck open after selecting a ticker; screenshot showed selected `UNH.US`/UnitedHealth fields filled while stale unrelated suggestions remained over the form.
- Deeper root cause: beyond the previous `searchOpen` dependency fix, the search effect itself could still set state after selection, and focus could reopen the menu for the already-selected canonical symbol.
- Fix: Portfolio add-holding now tracks `selectedSearchSymbol`; while the current symbol still matches the selected canonical symbol, `searchTerm` is suppressed, focus does not reopen suggestions, stale results are cleared, and the fetch callback no longer forces `searchOpen(true)`.
- Validation: lint, TypeScript, trading provider smoke 23/23, build, Lab health/lane smoke, and CDP user-flow check. CDP typed `UNH`, waited for 10 suggestions, clicked the first result, waited 3.5s, and verified `open:false`, `buttons:0`, symbol `UNH.US`, name `UnitedHealth Group Incorporated`, no stale suggestion text.

## Mission Control Lab Portfolio FX conversion [17:08 Asia/Ho_Chi_Minh]
- Philippe confirmed the Portfolio add/search fix and asked to continue with FX conversion.
- Implemented Lab-only base-currency conversion for Portfolio with EUR as the base view.
- Added `/api/trading/fx` and `lib/trading/fx.ts`; live rates come from Frankfurter with static fallback rates for common currencies if the provider is unavailable. Identity rates handle EUR/EUR.
- Portfolio now fetches/caches FX rates client-side, converts market value, total value, total P/L, cash value, allocation weights, and holdings sort-by-value/sort-by-P&L into EUR base values.
- Holdings table now shows EUR value/P&L as primary and shows the original holding currency underneath for non-EUR rows, with a small FX note such as `1 USD = 0.8547 EUR` in the Weight column.
- Validation: `npm run lint`, `npx tsc --noEmit`, trading provider smoke tests 24/24 including a new FX guard, `npm run build`, Lab preview restart, `lab-health`, `lab-lane-smoke`, direct `/api/trading/fx?base=EUR&currencies=USD,EUR,GBP` check, and CDP render check. CDP saw FX notes and EUR-primary value cells with USD secondary values.
- Caveat: cost basis conversion currently uses today's FX rate, not transaction-date FX. This fixes current weighting/value display, but historically precise P/L should later store transaction-date FX rates per lot/transaction.

## Mission Control Lab Portfolio cash management + FX tooltip [17:24 Asia/Ho_Chi_Minh]
- Philippe preferred moving per-currency FX rates out of the Holdings Weight column and into a hover/focus detail on the Base currency KPI. Agreed because Weight should stay allocation-only.
- Implemented Lab-only UI changes: Base currency KPI now has a hover/focus tooltip listing relevant FX rates with source/date, while the Holdings Weight column no longer shows FX notes.
- Added first-pass Cash management panel separate from ticker holdings: Add cash button, add/edit cash form with Currency / Amount / Broker-account, dedicated cash rows, and remove/edit actions for cash balances.
- Cash balances use `CASH.<currency>` symbols internally, assetType `cash`, quantity as balance amount, averageCost 1, and continue flowing through EUR base conversion and allocation math.
- Holdings table now filters out cash rows; cash is managed in the dedicated panel while still included in total value, cash KPI, allocation, and base-currency conversion.
- Validation: `npm run lint`, `npx tsc --noEmit`, Convex codegen/dev push, trading provider smoke tests 25/25 including cash/FX guards, `npm run build`, Lab preview restart, `lab-health`, `lab-lane-smoke`, and CDP render checks. CDP confirmed no `.trading-portfolio-fx-note` under Weight, Base currency tooltip visible on focus with `1 USD = 0.8547 EUR`, Cash management panel exists, Add cash opens a cash-only form with Currency/Amount/Broker fields and no ticker search dropdown.

## Mission Control Lab Portfolio holdings logos + buy-more flow [17:51 Asia/Ho_Chi_Minh]
- Philippe reported the Base currency FX tooltip was clipped under the page shell; screenshot confirmed only the lower/edge of the tooltip was visible below the KPI row. Fixed by allowing KPI overflow and opening the tooltip upward above the Base currency KPI.
- Added provider-backed logos/initials to Portfolio Holdings rows, reusing the metadata logo URL already returned by the watchlist-metadata/profile path. Rows fall back to ticker initials when no logo exists.
- Added a row-level buy-more `+` action between Edit and Remove for non-cash holdings. It opens a compact Add purchase form with Amount, Purchase price, and Transaction costs only.
- Buy-more updates the existing holding instead of creating a duplicate: new quantity = old quantity + added amount; new average cost is recalculated from prior cost basis plus the new purchase and fee; cumulative transaction fee is preserved and updated so existing portfolio cost-basis logic still applies.
- Validation: screenshot inspected, `npm run lint`, `npx tsc --noEmit`, trading smoke tests 26/26 including new logo/buy-more guards, `npm run build`, Lab preview restart, `lab-health`, `lab-lane-smoke`, and CDP render checks. CDP confirmed tooltip renders above the KPI without clipping, holdings logos render, buy-more buttons exist, Weight has no FX note, and Add purchase form exposes only Amount / Purchase price / Transaction costs.
