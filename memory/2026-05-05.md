# 2026-05-05

## Mission Control Lab Yahoo 1D fallback + watchlist right-column alignment [00:25 GMT+8 / 23:25 Vietnam previous day]
- Scope: Lab-only under `projects/mission-control-lab`; Dashboard/live lane was not touched or promoted.
- Philippe reported that the right side of the watchlist panel was still misaligned: `Watch` pill too large/tight against icons, text not visually centered, move/remove icons and the thin row divider not lining up with the rest of the row.
- Screenshot analysis showed the action icons were sitting too high relative to the row and the alert/priority cell widths/padding were too generous. The divider issue was likely an action-cell box-height/padding mismatch rather than a separate divider asset.
- Watchlist CSS changes: reduced alert/priority select pills to fixed `64px x 22px`, centered text with `text-align-last: center`, tightened alert/action column widths (`Alert` 58px, actions 72px), added matched padding to Priority/Alert/Actions cells, and gave the action flex row a stable `min-height: 26px` so icons share the row's vertical center.
- Yahoo 1D probe: direct Yahoo chart endpoint returned fresh intraday points for `NVDA`, `CRWV`, and `AVGO`, with last points around 2026-05-04 16:12Z while EODHD 1D still ended at 2026-05-01 20:00Z. This confirms Yahoo 1D is suitable as a fallback and raises a real question about EODHD renewal value if alternate sources keep replacing its useful coverage.
- Implemented EODHD 1D stale fallback: `lib/trading/sources/yahoo.ts` now exports `fetchYahooChart`, `buildYahooRangeSeries`, `YAHOO_PRICE_RANGES`, and `yahooSource`; `lib/trading/sources/eodhd.ts` detects stale 1D intraday series and replaces that range with Yahoo Finance 1D intraday when Yahoo is fresher. Longer EODHD ranges/dividends/news remain unchanged for now.
- Live validation after cache clear and Lab restart: ticker profile caches for `NVDA.US`, `CRWV.US`, and `AVGO.US` now show `rangeSeries['1D'].source.source === 'yahoo'` and last 1D points around 2026-05-04 16:20Z. EODHD realtime quote remained around 15:42Z/15:43Z, so Yahoo 1D was fresher than EODHD quote at validation time.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab health; Lab lane smoke; custom CSS marker check; live ticker cache inspection for Yahoo 1D fallback.
- Known unrelated build warnings persisted: deprecated Next `middleware`, autonomous runner dynamic trace, and NFT `next.config.js` trace warning.

## Mission Control Lab final watchlist action alignment tweak [00:47 GMT+8 / 23:47 Vietnam previous day]
- Applied Philippe's final right-column watchlist tweak: moved the move/remove icon cluster down 5px via `transform: translateY(5px)` on `.trading-watchlist-row-actions`.
- Removed the thin divider segment under the action icon section by making the actions cell bottom border transparent (`td:nth-child(9)`), so the row divider visually stops after the Alert/Watch pill.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py -v` 22/22; `npm run build`; Lab health; Lab lane smoke. Served CSS confirmed `transform: translateY(5px)` and minified transparent border (`#0000`) for the action cell.
- Noted market-data observation from Philippe: Yahoo 1D intraday appears about 5 minutes delayed, while EODHD quote appears about 15 minutes delayed. This should be tracked when deciding whether to renew EODHD next month.

## Mission Control Lab closeout and tomorrow focus [00:42 GMT+8 / 23:42 Vietnam previous day]
- Philippe confirmed the final watchlist right-column alignment looks good after commit `770720a Tune Lab watchlist action alignment`.
- Treat the current Lab watchlist panel polish as accepted for tonight unless new visual drift appears later.
- Market-data observation to carry forward: Yahoo 1D intraday appears roughly 5 minutes delayed, while EODHD realtime quote appears roughly 15 minutes delayed. This should be considered in the next-month EODHD renewal/value review.
- Tomorrow's Trading Lab focus requested by Philippe:
  1. Price alerts for items on the watchlist.
  2. Define what action should be taken based on each Alert level.
  3. Start the first phase of the Portfolio page.
- Existing open Trading follow-ups still remain unless superseded: cache hygiene/debug refresh, ETF issuer/logo strategy, IREN.US richer profile/logo/CoreWeave-style logo hygiene, center/logo polish if new screenshots show drift, duplicate watchlist entries if any, and broader EODHD provider-value review.

## Workspace home-improvement: runbook index checker hardening [08:33 GMT+8]
- Changed `scripts/check_runbook_index.py` to derive the workspace root from the script location by default and accept `OPENCLAW_WORKSPACE` as an override instead of relying on a hard-coded absolute path.
- Why: this makes the helper script safer to reuse from other working directories or cloned workspace roots without silent path brittleness.
- Expected benefit: lower friction when validating runbook indexes in moved/copied workspaces and clearer operator guidance via `--help` epilog.
- Verification: `python3 scripts/check_runbook_index.py`; `python3 /data/.openclaw/workspace/scripts/check_runbook_index.py` from `/tmp`; `OPENCLAW_WORKSPACE=/data/.openclaw/workspace python3 scripts/check_runbook_index.py --help`.
- Rollback: restore the previous hard-coded `WORKSPACE = Path("/data/.openclaw/workspace")` constant if this dynamic resolution ever causes confusion.

## Morning Meeting Decision [11:13]
- Report: nightly-security-review
- Finding: 2026-05-04 nightly security review overall verdict
- Decision: Approved
- Problem: no actionable security issue was found; review surfaced only informational accepted-risk/control-state notes
- Risk: no new critical, high, or medium security risk detected
- Action taken: none
- Verification: reviewed `memory/security/2026-05-04-nightly-security-review.md`; verdict `CLEAN` with 0 critical / 0 high / 0 medium
- Rollback: n/a
- Notes: continue suppressing unchanged accepted-risk items unless state drifts

## Morning Meeting Decision [11:19]
- Report: platform-health-council
- Finding: lossless-claw warning noise / ownership complaint
- Decision: Accepted risk
- Problem: the overnight review re-raised `lossless-claw` warning noise without enough live evidence that the plugin is actually malfunctioning
- Risk: low; false-positive review noise could push unnecessary runtime/plugin ownership changes
- Action taken: none
- Verification: live check showed `/data/.openclaw/extensions/lossless-claw` exists and is `node:node`; `openclaw.json` still contains `lossless-claw` entries; durable memory already warns not to trust plugin ownership/trust complaints blindly on this Docker host
- Rollback: n/a
- Notes: only reopen if actual lossless-claw behavior breaks, not just because a review complains

## Morning Meeting Decision [11:28]
- Report: self-improvement
- Finding: stale TOOLS.md references and missing runbook-index helper note
- Decision: Approved
- Problem: `TOOLS.md` pointed at a non-existent runbook, used wrong workspace-relative paths for Mission Control chat helpers, and did not document `scripts/check_runbook_index.py`
- Risk: operator confusion and avoidable debugging friction
- Action taken: removed the stale missing runbook reference, corrected the three chat-helper paths to `projects/mission-control/components/chat/...`, and added `scripts/check_runbook_index.py` to Key Scripts
- Verification: `python3 scripts/check_runbook_index.py` returned `OK: docs/runbooks/README.md indexes 21 runbook files.`; `grep` confirmed corrected chat-helper paths and the new script note in `TOOLS.md`
- Rollback: revert the `TOOLS.md` edits if the shorter path style was intentionally being used as shorthand
- Notes: left broader self-improvement findings for later instead of expanding this into a larger docs sweep

## Morning Meeting Decision [11:36]
- Report: platform-health-council
- Finding: PHC-2026-0505-001 nightly-security-review apparent gap
- Decision: Approved
- Problem: PHC flagged a possibly missed May 4 nightly-security-review run because it evaluated before the 03:30 review finished and saved its report
- Risk: false missed-run alarms create noise and can distract from real regressions
- Action taken: none
- Verification: cron run history shows `nightly-security-review` finished `ok` at 03:30 and delivered normally; saved report exists at `memory/security/2026-05-04-nightly-security-review.md`
- Rollback: n/a
- Notes: suppress repeats unless the 03:30 security job actually fails or its report is still missing later in the morning

## Morning Meeting Decision [11:41]
- Report: self-improvement
- Finding: deterministic cron task registry workflow under-documented
- Decision: Approved
- Problem: `TOOLS.md` mentioned `cron_runner.py --list-tasks` but did not explain that `scripts/cron_runner_tasks.py` is the declarative registry or point operators to the scheduler host-service runbook
- Risk: operator confusion when adding script-only jobs or verifying deterministic scheduler changes
- Action taken: added `scripts/cron_runner_tasks.py` to Key Scripts, clarified the deterministic scheduler workflow in `TOOLS.md`, and explicitly referenced `docs/runbooks/deterministic-scheduler-host-service.md`
- Verification: `python3 scripts/cron_runner.py --list-tasks` listed registered tasks; `grep` confirmed the new registry/workflow/runbook notes in `TOOLS.md`; checked `docs/runbooks/deterministic-scheduler-host-service.md` and kept it as the right runbook to refer to
- Rollback: revert the `TOOLS.md` edits if we later move the deterministic scheduler workflow into a different canonical doc
- Notes: there is a relevant runbook, and we should refer to it rather than duplicate host-service procedures inside `TOOLS.md`

## Morning Meeting Decision [11:49]
- Report: self-improvement
- Finding: operator diagnostics scripts existed but were effectively invisible in TOOLS.md
- Decision: Approved
- Problem: several real operator-facing diagnostic helpers were present in `scripts/` but undocumented, making them easy to miss or misclassify as legacy leftovers
- Risk: slower troubleshooting and duplicated ad-hoc checks
- Action taken: added an `Operator diagnostics` note to `TOOLS.md` covering `autonomy_health_check.py`, `autonomy_operator_dashboard.py`, `operator_snapshot.py`, and `cron_run_summary.py`
- Verification: `grep` confirmed the new TOOLS.md entries; `python3 scripts/operator_snapshot.py --json` ran successfully; `python3 scripts/cron_run_summary.py --limit 3` produced a compact cron summary
- Rollback: revert the `TOOLS.md` diagnostic-helper note if we later split these into a dedicated operator runbook or formally deprecate any of the scripts
- Notes: documented them as diagnostics, not as first-line runtime truth sources over the existing health/cron guidance

## Morning Meeting Decision [11:55]
- Report: self-improvement
- Finding: nightly-memory-extraction verification workflow was implied by AGENTS.md but not documented compactly in TOOLS.md
- Decision: Approved
- Problem: operators had no short checklist for verifying whether the silent nightly-memory-extraction job succeeded versus intentionally making no daily-memory append
- Risk: silent-job delivery noise or a no-append run could be misread as failure
- Action taken: added a four-step nightly-memory-extraction verification quick check under the OpenClaw cron section in `TOOLS.md`
- Verification: `grep` confirmed the new checklist; cron run history for job `7201afb2-6ff2-4ce7-8c24-e12d4413b9f9` showed the latest run `ok`, explicitly noted that `/data/.openclaw/workspace/memory/2026-05-04.md` was already comprehensive, and recorded durable updates to `life/projects/mission-control/items.json`; delivery status was `not-requested`, which matches the documented silent-job expectation
- Rollback: revert the TOOLS.md checklist if nightly-memory-extraction moves to a dedicated runbook with a better canonical operator procedure
- Notes: documented the distinction between true failure and healthy silent/no-append behavior
