# 2026-04-10

## Workspace Home-Improvement

**Script updated:** `projects/_ops/scripts/autonomous-status.py`

**What changed:** Updated `BOARD_JSON` path from the legacy `projects/autonomous-kanban/public/board.json` to the current `projects/mission-control/data/autonomous-tasks.json`. Rewrote the board-counting logic to map the new store's `status` / `run.status` fields to the old kanban column semantics (todo/inprogress/done). Also added `run.status` checking for "needs_input" / "blocked" tasks, and improved the `updated` timestamp to use actual task `updatedAt` millisecond epoch values.

**Why:** The legacy board.json is no longer updated (superseded by Mission Control task store), making the script report stale/zero counts. It was still partially functional for executor-last-result, but blind on the board side.

**Expected benefit:** `autonomous-status.py` now reports accurate live counts from the current canonical task store. Still a lightweight operator tool for quick status checks.

**Rollback:** Revert to previous version from git history. The old board.json path still exists if needed for comparison.

## Morning Meeting Fixes

**Queue cleanup:** Removed 2 stale resolved `blocked` entries from `memory/executor-subagent-queue.json`.

**Why:** Platform Health Council correctly flagged them as resolved-but-still-listed operator noise.

**Expected benefit:** Cleaner queue truth, less Morning Meeting/report noise, no change to active execution behavior.

**Rollback:** Restore the two removed entries from git history if needed.

**Pre-market brief fix:** Updated `projects/manual-trading-brief/src/brief_generator.py` to use `^VIX` instead of `VIX` in the Yahoo Finance watchlist.

**Why:** yfinance treats `VIX` as invalid for the volatility index in this path and emits a false `$VIX: possibly delisted` warning even when the brief succeeds. `^VIX` returns valid data cleanly.

**Expected benefit:** `pre-market-brief` should now run without the false stderr warning, so successful deliveries can be recorded as clean OK runs instead of warn-noise.

**Verification:** `./.venv/bin/python` confirmed `^VIX` returns 2 days of data; full `python3 projects/manual-trading-brief/src/brief_generator.py` run exited 0, delivered to Telegram, and produced empty stderr.

**Rollback:** Change `^VIX` back to `VIX` in the watchlist.

**TOOLS.md skills alignment:** Added `language-learning`, `sportsbet-advisor`, and `trading-advisor` to the `Workspace Local Skills` section in `TOOLS.md`.

**Why:** Self-Improvement correctly flagged that the specialist workspace-local skills existed on disk but were missing from the operator skill inventory.

**Expected benefit:** Better operator visibility into real installed local skills, less confusion when tracing Mission Control specialist-seat behavior and skill sources.

**Rollback:** Remove the three skill entries from `TOOLS.md`.

## Repo / history hygiene follow-up

**openclaw.json history cleanup:** Verified `openclaw.json` was not gitignored and still existed in remote git history even though it was not present in current HEAD/workspace. Rewrote remote history from a temporary clone to remove the file, added `openclaw.json` to `.gitignore` upstream, and verified a fresh clone shows `git rev-list --all -- openclaw.json` = `0`.

**Why:** Morning Meeting surfaced that the finding was half-right in the wrong way: not an active workspace file problem, but still a real history-hygiene issue.

**Expected benefit:** Removes stale historical exposure, prevents accidental re-tracking, and makes future review findings more precise.

**Rollback:** History rewrite rollback would require deliberate git/admin intervention; no local workspace rollback was applied.

**Push-state note:** Because the history rewrite left the main local checkout on pre-rewrite ancestry, the two approved Morning Meeting commits were pushed from a clean temporary clone built from the rewritten remote instead of from the dirty workspace checkout.

**Commits pushed upstream today:**
- `014196c` — `Fix pre-market brief VIX symbol and clean queue`
- `04554ff` — `Document local specialist skills in TOOLS`
- rewritten remote head after patch replay: `480df40`

## Mission Control specialist-seat follow-through

Completed a Johan + Milou continuity pass so they now match Japin's structural standard even though they remain Marvin-routed seats for now.

**What changed:**
- added `memory/continuity.md` for both Johan and Milou as the active handoff anchor
- added durable profile files:
  - Johan → `memory/bettor-profile.md`
  - Milou → `memory/trader-profile.md`
- added recurring-pattern files:
  - `agent-workspaces/sportsbet-advisor/.learnings/corrections.md`
  - `agent-workspaces/trading-advisor/.learnings/corrections.md`
- added history lanes:
  - Johan → `memory/research/README.md`
  - Milou → `memory/analyses/README.md`
- updated each workspace `WORKSPACE.md`, `MEMORY.md`, and `memory/README.md` to document the new continuity contract
- updated Mission Control seat definitions so Johan and Milou now point Files/seat guidance at `memory/continuity.md` and instruct the runtime to review/update the right continuity files
- updated Mission Control runbooks to record the new specialist continuity structure

**Why:** Johan and Milou previously had named seats, local skills, and seeded workspaces, but not the fuller Japin-style continuity/update discipline.

**Verification:** `npm run build` passed in `projects/mission-control`.
