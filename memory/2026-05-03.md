# Nightly Memory Extraction — 2026-05-03 02:00 MYT

## Durable Facts Extracted from 2026-05-02 Conversations

### Project: Mission Control Lab (Trading Watchlist)
- **Lab Watchlist feature completed** (Lab-only, projects/mission-control-lab): Full Convex-backed live Watchlist page with create/rename/delete watchlist controls, Convex loading-state fix (no more sample-row flash), product copy cleanup, priority labels changed from Core/Radar/Speculative to High/Medium/Low (internal Convex enum values preserved), and Add Symbol autocomplete/picker that reuses `/api/trading/search?q=` for company/ticker search with metadata persistence in Convex. All committed to Lab lane only.
- **Convex project initialized**: Philippe created `watchlists` Convex project with dev deployment `basic-lion-601`. `.env.local` in `projects/mission-control-lab` now has `CONVEX_DEPLOYMENT`, `NEXT_PUBLIC_CONVEX_URL`, `NEXT_PUBLIC_CONVEX_SITE_URL`. Operational caveat: `convex/_generated/*` files were created root-owned inside container; may need ownership attention for future regeneration.
- **Watchlist symbol search picker**: Add Symbol field on `/trading/watchlist` now uses debounced EODHD search endpoint (same as top-bar ticker search) with keyboard navigation, result selection locking symbol+name+exchange+currency into the form, and manual-entry fallback.
- **Watchlist next goals**: (1) Item-level inline editing for priority/alert/thesis. (2) Provider enrichment/backfill for missing row metadata after manual symbol entry. (3) Ticker-page add-to-watchlist integration with list picker. (4) List-level polish deferred until core CRUD is stable.

### Morning Meeting Decisions (2026-05-02)
- Nightly security review: clean, 0 critical/0 high/0 medium findings — accepted risk.
- PHC-2026-0502-001 workspace-home-improvement delivery contract issue: accepted risk as stale wrapper/reporting artifact; live job execution is healthy with `delivery.mode=none` and `lastRunStatus=ok`.
- PHC-2026-0502-002 Lab trading provider smoke tests: deferred during meeting, then implemented immediately after; added `tests/trading-provider-smoke.test.py` using stock Python `unittest` covering yfinance bridge output shape, ticker profile contract, and recent source-repair behavior. 11 tests OK after final extension.
- PHC-2026-0501-002 stale lossless-claw config-warning entries: deferred until a future OpenClaw update or deliberate config-cleanup window.
- Self-improvement review: clean — accepted risk.

### Home Improvement
- `scripts/queue_state.py` hardened with `argparse`, built-in `--help`, and validated command choices.

### Nightly Security Review
- 2026-05-02 report clean: 0 critical, 0 high, 0 medium findings. Standing accepted-risk items unchanged (watchdog log rotation gap, Control UI host-header/device-auth posture, QMD vector timeout).

### Operational Notes
- Scope stayed Lab-only throughout May 2 evening session; live Dashboard was not touched or promoted.
- Known root-owned file issue in `projects/mission-control-lab/convex/_generated/*` noted for future ownership cleanup if regeneration fails as `node`.
- `.env.local` contains Convex credentials and must not be committed.
- `/data/.convex/config.json` contains Convex credentials and must not be exposed.

## Entity Updates
- Updated `mcs-043` through `mcs-044` access counts (Lab Trading MarketTape, quote refresh, Estimates redesign, yfinance backfill, Financial Highlights polish, ESEF filings adapter, Wikipedia profile fixes, yfinance US enrichment, end-of-day checkpoint) — all from 2026-05-01 extraction carried forward and confirmed active through May 2 session wrap-up.
- Bumped `mcs-043` accessCount from 1 to 2.
- Bumped `mcs-044` accessCount from 1 to 2.
- Added new durable facts `mcs-045` through `mcs-050` covering: Lab Trading Watchlist feature completion (Convex-backed, Lab-only), Convex project initialization by Philippe (deployment basic-lion-601, root-owned _generated files caveat), Watchlist priority label simplification (High/Medium/Low UI with preserved Convex enum), Convex loading state fix (no sample-row flash), Watchlist next goals (item-level editing, provider enrichment, ticker-page integration), and May 2 evening wrap-up confirmation.

## Nightly Extraction Complete
Extraction timestamp: 2026-05-03 02:00 MYT / 2026-05-02 18:00 UTC

## Autonomous Home Improvement — 2026-05-03 08:30 MYT
- Changed: Hardened `scripts/check_token_age.py` with `argparse`, built-in `--help`, manifest override support, and validation for invalid day values.
- Why: The script previously parsed `sys.argv` at import time, making help/usage brittle and raising avoidable errors on bad input.
- Expected benefit: Safer operator UX for a local maintenance helper and easier troubleshooting without changing control-plane behavior.
- Verification: `python3 -m py_compile scripts/check_token_age.py`; `python3 scripts/check_token_age.py --help`; `python3 scripts/check_token_age.py 30` (script ran successfully and returned the expected non-zero alert status because current manifest entries exceed the threshold).
- Rollback: Revert `scripts/check_token_age.py` to the previous version.

## Lab Watchlist table-first redesign [17:35 MYT]
- Philippe shared a Watchlists reference screenshot and asked for the Lab Watchlist page to move closer to the main table structure: tab-based lists, table-first workspace, no market-cap requirement, no performance/sector blocks, optional active-list news, Add Symbol moved into Manage Watchlist, and Pin to Overview support.
- Implemented Lab-only redesign in `projects/mission-control-lab`:
  - tightened the top area so the watchlist table is the main working surface;
  - replaced the side Add Symbol panel with a Manage watchlist popover containing Add Symbol, Save details, New list, Delete list, and Pin/Unpin to Overview;
  - kept tab-style watchlist switching and made tabs more compact;
  - reshaped the table toward the screenshot structure with Symbol, Company, Price, 1D, Priority, Alert, Watch note, and action columns;
  - kept market cap out;
  - added a quieter Watchlist news placeholder scoped to the selected symbols, explicitly avoiding fabricated headlines until real provider-backed news is wired;
  - preserved existing Convex schema/state, including pinned watchlist support and internal priority enum compatibility.
- Visual review: first pass was too dashboard/card-like; tightened after screenshot review so the table starts higher and dominates the page. Final rendered screenshot check was commit-worthy with no blocking visual issues.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` now 12 tests OK; `npm run build` with only known existing warnings; Chromium visual smoke saved to `projects/mission-control-lab/tmp/watchlist-visual-check/watchlist-redesign.png`.
- Commit: `8d5906e Redesign Lab watchlist table workspace`.
- Follow-ups: real provider-backed watchlist news, real quote/sparkline data instead of deterministic placeholder quote movement, Overview page consuming Convex pinned watchlists during the future Overview redo, and item-level inline editing.

## Lab Watchlist provider metadata columns [18:05 MYT]
- Philippe asked to add logo marks left of watchlist symbols, add `P/E` and `52W` columns matching the reference screenshot, and asked how the Alert field should work.
- Implemented Lab-only provider metadata support:
  - added `app/api/trading/watchlist-metadata/route.ts` to batch-read active watchlist symbols through existing `getTickerProfile()` so Watchlist can reuse ticker-page provider data for company logos, quote, P/E, and 1Y range low/high;
  - Watchlist rows now render a logo/initials mark left of the symbol;
  - table columns now include `P/E` and `52W`;
  - removed the fake-looking sparkline strip and replaced 1D with compact provider change pills;
  - tightened fixed table column widths so logos, P/E, 52W, Priority, Alert, and Watch note fit without visible horizontal overflow at the checked viewport;
  - expanded smoke assertions so the Watchlist redesign checks for logo/metadata route, P/E, 52W, and no Market Cap column.
- Visual review: initial logo/metric pass was too cramped and produced horizontal overflow. Tightened symbol/logo alignment, numeric column sizing, and watch-note wrapping. Final screenshot review found no blocking visual issues.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 12 tests OK; `npm run build` with only known existing warnings; visual smoke passed.
- Commit: `a861f0f Add Lab watchlist provider metadata columns`.
- Alert design answer to user: `No alert` should mean passive tracking only; `Watch` should mean show it in attention/watch queues or soft reminders when notable provider events/price conditions later exist; `Urgent` should be the high-signal lane for explicit user-defined triggers or major provider/news changes. Current UI stores the state only; actual notifications/rules are not wired yet.

## Lab Watchlist table controls polish [18:50 MYT]
- Philippe requested the Watchlist table move closer to the screenshot behavior:
  - `Price` already shows price plus percentage change, so `1D` should become a sparkline rather than duplicate the percentage;
  - `52W` should become a low-to-high rail with current-price marker instead of min-max text;
  - remove `Watch note` from the active table and move it into a row `…` details menu;
  - make remove a red circular `−` pill;
  - replace `Not pinned` text with a grey/active pin icon;
  - replace the `Convex live` pill with a `Sort by` menu.
- Implemented Lab-only polish:
  - metadata API now returns compact 1D point arrays plus 52-week low/high/current-position values;
  - Watchlist table renders mini 1D SVG sparklines;
  - 52W renders as low label, thin rail with current marker, high label;
  - row details live behind a `…` action menu containing Watch note, market, updated time, and provider source;
  - remove is now a red circular `−` action;
  - table header uses a pin icon state instead of pinned/not-pinned text;
  - top command actions now use `Sort by` with Name, Price, Price change, Priority, Alert, Recently updated;
  - removed visible `Convex live` copy.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 12 OK; `npm run build` with known existing warnings only.
- Visual checks passed: desktop screenshot showed no blocking layout issues; mobile/narrow screenshot passed with table-scoped horizontal scrolling and no blocking overlap.
- Commit: `6db5934 Polish Lab watchlist table controls`.

## Lab Watchlist hierarchy cleanup [19:35 MYT]
- Philippe requested top-to-bottom UI cleanup after the table-controls polish:
  - simplify title section to `Watchlists` only;
  - lighten Sort by options, Manage watchlist button, and watchlist tabs;
  - change main section heading from `Tracked names / AI / Tech` to just the active list label;
  - rebalance table column spacing around 1D, P/E, 52W, Priority, and actions;
  - make the row `…` menu float above neighboring sections instead of being clipped under News;
  - simplify Watchlist news by removing `Headlines for this list` and symbol count;
  - add small logo/symbol chips to the Watchlist news placeholder.
- Implemented Lab-only changes in `projects/mission-control-lab`:
  - removed top eyebrow/subtitle and hard-set title to `Watchlists`;
  - made Sort select value, Manage button, and tab title weights regular/lighter;
  - main panel label now uses the active watchlist name only;
  - table min width/column widths rebalanced for more even spacing;
  - row details menu now uses fixed positioning and opens above the trigger to avoid lower-section clipping;
  - Watchlist news now shows only the section label, logo chips for list symbols, and cleaner no-linked-headlines copy.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 12 OK; `npm run build` with known existing warnings only.
- Visual check passed via Chromium screenshot; review found no blocking issue, only minor note that the horizontal scrollbar remains visible because the table intentionally preserves wide-column behavior.
- Commit: `c54a357 Tighten Lab watchlist UI hierarchy`.

## Lab Watchlist row layout tightening [20:05 MYT]
- Philippe requested final row-level cleanup:
  - move Priority, Alert, `…`, and `−` closer together;
  - use saved space to give 52W more room before Priority;
  - avoid the occasional desktop horizontal scrollbar;
  - make the `−` remove symbol regular/light instead of bold;
  - simplify the `…` menu to only Watch note for now;
  - remove duplicate ticker and market/update subtext from active table rows.
- Implemented Lab-only changes:
  - table widths tightened to fit desktop without the visible horizontal scrollbar in the checked viewport;
  - 52W column widened while Priority/Alert/action columns were compacted;
  - Symbol row now shows the ticker once only;
  - Company row now shows the company name only;
  - row menu now contains only Watch note;
  - remove pill font weight reduced to regular.
- Validation passed: `npm run lint`; `npx tsc --noEmit`; `python3 tests/trading-provider-smoke.test.py` 12 OK; `npm run build` with known existing warnings only.
- Visual check passed: no duplicate ticker/subtext, no desktop scrollbar visible, 52W/Priority spacing improved, actions compact and acceptable.
- Commit: `5e5804a Tighten Lab watchlist row layout`.

## Lab Watchlist final Sunday function pass [20:50 MYT]
- Philippe requested the last two Sunday Watchlist items:
  - change Watchlist table sparkline column from `1D` to `5D` and use the 5D Price History series;
  - make ticker-page `+ Watchlist` functional with a watchlist picker.
- Implemented Lab-only changes:
  - `/api/trading/watchlist-metadata` now derives compact row sparkline points from `priceSeries.rangeSeries['5D']` instead of `1D`;
  - Watchlist table header and aria text now say `5D`, with `FiveDaySparkline` rendering the row mini-chart;
  - ticker detail page now wraps a new client component `TickerWatchlistButton` in the existing Convex provider;
  - `TickerWatchlistButton` loads Convex watchlists, detects whether the current symbol is already tracked, opens a picker, and saves/updates the ticker into the selected watchlist through `watchlistApi.add` with symbol/name/exchange/currency metadata;
  - fallback remains disabled/non-mutating when Convex is not configured.
- Validation passed: `npx convex dev --once`, `npm run lint`, `npx tsc --noEmit`, `python3 tests/trading-provider-smoke.test.py` 12 OK, and `npm run build` with known existing warnings only.
- Dev/browser verification was partially noisy because repeated temporary dev servers conflicted; source/test/build gates confirm the integration, and earlier CDP checks did show the ticker watchlist menu rendering with existing lists once Convex hydrated.
- Commit: `5be821f Connect Lab ticker watchlist actions`.
